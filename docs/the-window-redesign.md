# The window around three questions

First verifiable piece: the window has no first-level place that answers
"what is running now, and what has it cost" — `WaitingScreen` shows only
finished outcomes (went, capped, stopped, broke, quota, human), never a run
still in flight. In progress: adding that as a visible block, sourced from
the same live data `flowlive.ts`/`RunGlimpse` already reads for one run.
