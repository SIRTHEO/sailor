# I guasti incontrati mentre si costruiva

Ogni riga qui è un guasto **vero**, capitato davvero, con la data. Nessuno è
ipotetico e nessuno è stato scritto per fare esempio.

**A cosa serve questo file, e perché non è un diario.** Un guasto riparato a mano
resta nella testa di chi l'ha riparato. La volta dopo ci ricasca un altro — è
successo il 29/08/2026 con lo stesso processo orfano, a due persone diverse,
nella stessa notte, e la seconda non sapeva della prima. Questo file è il
materiale grezzo di ciò che Sailor deve imparare a fare da solo: leggere com'è
andata e ricavarne un controllo. Finché quell'anello non è chiuso, la lista si
tiene a mano.

La forma di ogni voce viene da un lavoro che il flusso di ricerca ha trovato in
letteratura il 29/08/2026 — *Sillito & Kutomi, «Failures and Fixes: A Study of
Software System Incident Response»* — e dalla sua conclusione, che qui è la
regola: **il seguito di un guasto è un controllo, non un compito assegnato a
qualcuno.** Una voce senza la colonna «cosa lo impedirebbe» non è finita.

| # | data | cosa è successo | come si è visto | cosa lo impedirebbe | stato |
|---|---|---|---|---|---|
| 1 | 28/08 | Un motore esterno invocato con gli argomenti nell'ordine sbagliato: il prompt finiva ignorato | Solo eseguendolo. Le opzioni erano state lette dalla documentazione e mai provate | Una prova che esegue davvero ogni riga di comando prima che finisca in un flusso | **chiuso** — riga corretta e misurata |
| 2 | 28/08 | Un passo che falliva veniva registrato come riuscito, col codice d'errore sepolto nel risultato | Leggendo il deposito a mano | Un'uscita non-zero rompe il passo, con tolleranza da chiedere esplicitamente | **chiuso** — con mutante |
| 3 | 28/08 | Il controllo statico chiudeva senza errori su un flusso che nominava uno strumento inesistente | Provando a mano un flusso rotto | Il controllo distingue «non c'è qui» da «non esiste in nessun catalogo» | **chiuso** — con mutante |
| 4 | 29/08 | Un processo di sviluppo orfano occupava una porta e impediva l'avvio | Due volte, a due persone diverse, nella stessa notte | Sailor deve sapere quali processi ha avviato, per spegnerli e riprenderli | **chiuso in parte** il 31/08, riaperto il 01/09 — il deposito ha una tabella `processes`, ogni processo che Sailor accende ci passa (`supervisor::child::Process::start` è l'unica strada, e registra), e `sailor-live --list` / `--stop` sono le due metà che mancavano. Verificato accendendo davvero la modalità viva: il registro nominava finestra e servitore della pagina con pid, porta e chi li aveva accesi, e `--stop` da un'invocazione **separata** — cioè la seconda persona — li ha spenti e ha liberato la 5183. Chi è vivo lo conferma `kill(pid, 0)` su un pid che il deposito ha già scritto, mai un elenco chiesto al sistema: un elenco vuoto è la forma in cui il guasto 12 si nasconde. **Limite dichiarato**: un figlio morto e non raccolto risulta vivo, visto scrivendo la prova; non tocca gli orfani veri, che il processo iniziale raccoglie. — **RIAPERTO il 01/09**, e la prova è un orfano trovato vivo: `./target/release/sailor ui`, pid 77243, genitore `1`, acceso il **31/08 alle 11:52** — cioè *lo stesso giorno* in cui questa riga è stata dichiarata chiusa — e ancora in ascolto su `127.0.0.1:47831` dopo **21 h 51 m**. Il comando `sailor ui` era stato rimosso dai sorgenti il 31/08 e c'è perfino una prova che afferma che non raggiunge più niente: il processo veniva dal binario di prima e nessuno l'ha mai spento. Nel frattempo la tabella `processes` ha **zero righe** e `events.db` **zero eventi di processo** su 614: non è la proiezione indietro, è che non si è registrato nessuno — nemmeno la finestra. **La cura copre solo ciò che passa da `Process::start`, e niente obbliga a passare di lì**: è una cura volontaria, e una cura volontaria vale per chi già si comportava bene. Il controllo che manca non è un altro registro: è che **avviare un processo lungo per una strada diversa non compili**, o che qualcosa confronti periodicamente le porte in ascolto col deposito e dichiari la differenza. Trovato mentre si cercava se la finestra fosse accesa — e lì ho sbagliato allo stesso modo: `launchctl` elencava `sailor-desktop.89222` con «active count = 1» per un pid **morto**, cioè il guasto 12 daccapo, una fotografia scaduta creduta un fatto. Orfano spento il 01/09, porta liberata |
| 5 | 28/08 | Prove che leggono un file di configurazione della macchina di chi le esegue | Diventate rosse dopo una pulizia, a codice invariato | Le prove non leggono lo stato della macchina; ciò che serve si versiona | **aperto** |
| 6 | 29/08 | Uno script ha stampato «rimossi 1975 file» senza rimuoverne nessuno | Guardando che il peso della cartella non era cambiato | Il messaggio di successo sta dentro il controllo dell'esito, mai accanto | **chiuso** — rifatto |
| 7 | 28/08 | Due passi dichiarati «in parallelo» giravano in fila | Cronometrando: due passi da 6 secondi ne impiegavano 12 | Una prova in cui ogni passo aspetta gli altri del fronte e fallisce se resta solo: non cronometra, quindi non mente su una macchina carica | **chiuso** il 30/08 — misurato: 2 passi da 6s in 6,07s (erano 12,07), 3 in 6,05s (erano 18,14) |
| 8 | 28/08 | Un descrittore con un campo che questa versione non conosce viene scartato intero | Scrivendone uno apposta | Un campo ignoto è un avviso sul campo, non il rifiuto del componente | **chiuso** il 31/08. `Catalog` distingue `problems` (voci **perse**) da `notes` (voci **tenute**, con qualcosa di ignorato): metterle insieme avrebbe fatto contare come guasti degli strumenti vivi. E i campi ignoti sopravvivono a una riscrittura, che era il difetto opposto |
| 9 | 28/08 | Un elemento grafico invisibile: disegnato dello stesso colore dello sfondo | Solo da un'immagine dello schermo. Né i tipi né le prove lo vedevano | Lo schermo come oracolo: un'immagine confrontata, non solo i tipi | **chiuso** — quel caso |
| 10 | 28/08 | La stessa lista di componenti scritta in due punti del programma | Si sono disallineati in un'ora, nello stesso giorno di lavoro | Una sola fonte, e le altre la chiedono invece di ricopiarla | **aperto** |
| 11 | 29/08 | In modalità viva la finestra spariva. **La causa scritta qui era sbagliata, e la correzione conta quanto la riparazione**: non è l'errore di compilazione a uccidere la finestra. `cargo tauri dev` la uccide a **ogni** file toccato, *prima* di compilare — `tauri-cli 2.11.4`, `src/interface/rust.rs`, `run_dev_watcher`: `child.kill()`, poi `child.wait()`, poi `run(...)`. L'errore di compilazione è solo il motivo per cui non ne ritorna una. Il difetto non era nella gestione dell'errore: era **nell'ordine** | La finestra è sparita mentre un altro cantiere aveva un crate a metà | La finestra sopravvive a una compilazione fallita, e lo dice invece di chiudersi | **chiuso** il 31/08 — `sailor-live` (`crates/supervisor`) prende il posto di `cargo tauri dev` e rovescia l'ordine: costruisce, e tocca ciò che è acceso **solo se** la costruzione è riuscita. **Visto, non solo scritto**: rompendo `crates/ledger` con la finestra vera aperta, il pid 89610 è rimasto lo stesso, il titolo è diventato «ricostruzione FALLITA: stai guardando l'ultima versione buona» e l'errore del compilatore è arrivato intero; rimessa a posto la riga, la finestra è stata sostituita. Il mutante che rimette l'ordine di `tauri-cli` uccide un processo vero dentro la prova. **Trovato di passaggio, e non riparato**: `get_in_workspace_dependency_paths` segue solo le dipendenze che sono membri dello stesso workspace, e `desktop/src-tauri` ne dichiara uno vuoto apposta — quindi con `cargo tauri dev` una modifica a `crates/` non fa ricostruire niente e la finestra mostra il motore vecchio **senza dirlo**. `sailor-live` guarda anche `crates/` |
| 12 | 29/08 | `pgrep` dentro il perimetro **non vede i processi e risponde vuoto**, senza errore | Una sorveglianza ha detto «nessun flusso in esecuzione» mentre due giravano | Chiedere lo stato al deposito, non al sistema operativo: chi è vivo lo dice il dato, non un comando che il perimetro può zittire | **chiuso** — sorveglianza riscritta |
| 13 | 29/08 | `for x in $lista` in zsh **non spezza la variabile in parole**: l'intera lista entra in un giro solo | La sorveglianza riscritta ha rifatto lo stesso falso allarme del guasto 12, per una causa diversa | Leggere riga per riga (`while read`), mai iterare una variabile non quotata. **Era già scritto in una memoria e non è stato consultato** | **chiuso** — riscritta di nuovo |
| 14 | 29/08 | Un motore esaurito (limite settimanale) è stato registrato come guasto qualunque, e la corsa si è fermata | Leggendo il messaggio del motore: «hai raggiunto il limite settimanale, si azzera alle 7» | Distinguere «finito fino a un'ora nota» da «rotto»: il primo si aspetta o si instrada su un altro profilo, il secondo no. Serve prima che il consumo dichiarato dal motore entri nel deposito | **chiuso in parte** il 31/08 — con mutante. La distinzione c'è ovunque: classe `engine_exhausted` e specie `exhausted` nel deposito, **anche con un motore solo**, che era il caso in cui è capitato. **Resta l'attesa**: nessuno legge «si azzera alle 7» per riprovare a quell'ora, e non si legge apposta — estrarre quell'ora con un'espressione regolare da una frase vista una volta sola sarebbe un dato inventato che ha l'aria di una misura |
| 15 | 29/08 | Per cambiare l'innesco di un flusso è stato usato uno script Python che riscrive il JSON: Sailor non ha nessun comando per operare sui propri flussi | Theo lo ha visto nel terminale e ha chiesto perché la riga di comando non chiami Sailor. Verificato: `sailor flow` ha solo `list`, `due`, `check`, `run` | Ogni cosa che una persona deve fare su un flusso è un comando di Sailor. Finché si aggira con `python3`, il sistema non si usa da sé e nessuno se ne accorge dai suoi controlli | **aperto** |
| 16 | 29/08 | Il flusso di ricerca non è partito e il lavoro l'ha fatto una persona a mano. Tutti e sei i passi hanno `claude-code` scritto dentro, senza ripiego — mentre `agy` rispondeva | Provando i tre motori uno per uno: claude esaurito, codex 401, **agy vivo**. Nessuno li aveva provati | Un passo dichiara una **catena** di motori, non uno. E chi lancia vede su quale è finito | **aperto** |
| 17 | 29/08 | Due passi di quel flusso chiedono competenze di Claude Code (`advise-project-approach`, `neuroarxiv`) che stanno in `~/.claude/skills` di una sola persona: il flusso non è portabile e non lo dichiara | Cercando perché non bastasse cambiare motore. `flow check` lo dà per buono: controlla gli strumenti per identificativo, **le competenze no** | Ciò che un passo chiede va dichiarato tutto, e il controllo statico lo verifica — è il guasto 3 su un'altra dimensione. Chi non ha quella capacità deve funzionare peggio, non in silenzio | **aperto** |
| 18 | 29/08 | La casa di Sailor (`~/.config/sailor`) contiene **un solo file**, una firma. Ogni server MCP e ogni gancio che Sailor conosce li scopre leggendo la configurazione di Claude Code, Claude Desktop o Cursor: la sua dotazione è quella dei vicini | Cercando dove fossero installate le competenze del guasto 17. Non c'era nessun posto dove installarle | Una dotazione propria sotto la casa di Sailor, che viaggia col prodotto. Il rilevatore torna a dire cosa c'è sulla macchina — che è un'altra domanda | **chiuso in parte** 01/09 — la dotazione ora **arriva ai motori**: `actions` prende `profiles`, e `ExternalEngineAction` sovrappone l'ambiente del profilo attivo (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `GEMINI_CLI_HOME`) **sotto** `spec.env`. Fino a ieri quella sovrapposizione la chiamava un posto solo, `sailor run`: un motore lanciato da un passo di flusso ereditava l'ambiente di chi aveva aperto il terminale. Il profilo risolto finisce nella riga del deposito, dove si scriveva sempre vuoto. *Mutante*: rimettere `env: spec.env.clone()` — il motore vero riparte con `CODEX_HOME` vuota e la prova diventa rossa. **Resta aperto ciò che la cura chiedeva per primo**: `~/.config/sailor/equipment/` esiste su questa macchina ma **non viaggia col prodotto** — non è versionata e nessun codice la legge — e il rilevatore risponde ancora alla domanda dei vicini |
| 19 | 30/08 | La casa di Sailor era **in due posti**: chi cercava deposito e listino risolveva `~/.config/sailor`, chi cercava i descrittori dell'utente `~/.sailor`, e la documentazione spedita mandava tutti nella seconda | Scrivendoci dentro il listino dei prezzi seguendo la documentazione, e vedendo che nessuno lo leggeva | Una funzione sola che dice dove sta la casa, e i chiamanti che la chiedono invece di riscriverla. Le due copie sbagliavano **insieme**, quindi si confermavano a vicenda | **chiuso** — con mutante |
| 20 | 30/08 | Un `with` che nomina un campo inesistente viene **ignorato in silenzio**: il passo parte, il motore riceve una riga di comando monca, e l'errore che arriva è suo | Scrivendo `prompt` invece di `stdin` in un flusso di prova. Il messaggio era «Input must be provided either through stdin» — di Claude, non di Sailor | Un campo sconosciuto in un `with` è un avviso su quel campo, non silenzio: è il guasto 8 visto dall'altro lato, e `flow check` è il posto dove dirlo prima di spendere | **chiuso** il 31/08 — con tre mutanti. `flow check` nomina passo e campo. La strada ovvia (`deny_unknown_fields`) era **sbagliata e l'avrebbe peggiorato**: a esecuzione l'ingresso di un passo *è* l'uscita della dipendenza, quindi ogni passo con una dipendenza sarebbe morto con «unknown field `status`» — il mutante lo mostra |
| 21 | 30/08 | Le prove di `profiles` si rubavano la cartella temporanea a vicenda: **una esecuzione su venti falliva**, ogni volta su una prova diversa | Eseguendo la batteria molte volte di fila. Misurato: 1 su 20 prima, 0 su 50 dopo | Un contatore che cresce nel nome, invece del solo orologio: `cargo test` manda le prove sullo stesso processo e l'orologio di macOS non ha la risoluzione del nanosecondo | **chiuso** — misurato prima e dopo |
| 24 | 31/08 | Un deposito **già esistente** non veniva migrato: la migrazione aveva imparato quattro colonne nuove ma `PROJECTION_SCHEMA_VERSION` era rimasta a 4, quindi `4 < 4` era falso e ogni lettura moriva con «no such column» | Usando Sailor: il primo comando che legge il consumo di una corsa (`sailor flow cost`) è morto sul deposito vero di questa macchina. **517 prove verdi non l'avevano visto** | Il numero di versione si alza **insieme** alle colonne, e serve una prova che apra un deposito che si dichiara già aggiornato e non lo è — un deposito creato in una prova nasce completo e non passa mai dalla migrazione | **chiuso** — con mutante che riproduce l'errore parola per parola |
| 23 | 31/08 | **136 identificatori in italiano** — variabili, moduli, costanti, due file di prova, classi CSS — contro una regola scritta in `AGENTS.md` dal 28/08. Quasi tutti nati dopo di essa | Theo, leggendo il codice: «inaccettabile vedere ancora funzioni, variabili o codice, nomi di file scritti in italiano». Nessun controllo li aveva mai visti | La regola va dove si rilegge (`docs/decisioni.md`) **e** ha un controllo che la rende rossa. Serve anche che sia completa: quella vecchia elencava «funzione, tipi, campi, opzioni» e i nomi di file non c'erano — chi scriveva `smista_il_lavoro.rs` non trovava il proprio caso, e la direttiva di sessione «rispondi in italiano» vinceva | **chiuso** — il controllo è nato rosso su 64 e ora è verde |
| 26 | 31/08 | Una prova che dichiarava di provare la crescita dell'attesa **passava col difetto rimesso**: chiedeva una sequenza «non decrescente, mai sopra il tetto, e ferma sul tetto», e `[50, 50, 50…]` — l'attesa fissa di prima — soddisfa tutte e tre | Un motore che non aveva scritto né la prova né il codice, leggendo due riparazioni **senza sapere quale fosse quale**. L'autore non l'aveva visto; il verificatore nemmeno, e aveva fatto le mutazioni davvero | **Rompere una prova non basta: va rotta nel punto dov'era il difetto originale.** Il verificatore aveva mutato *il tetto* (togliendolo) e visto rosso; nessuno aveva mutato *la crescita* rimettendo l'attesa fissa, che era il difetto vero | **chiuso** — la prova ora chiede una salita *prima* del tetto, e con l'attesa fissa rimessa diventa rossa. Nata da lì una terza prova che interroga la regola di crescita da sola, cosa che un commento prometteva e nessuno faceva |
| 25 | 31/08 | Il flusso `sviluppa-sailor` ha `"workdir": "/Users/theo/personal/sailor"` **in chiaro su ogni passo**: lanciato da un clone lavora — e commette — nel repository principale, senza dire niente | Eseguendolo da un clone per l'A/B fra flusso e prompt, e trovando il repository principale sporco. L'isolamento dichiarato nel disegno dell'esperimento non c'è stato | Un flusso non deve sapere dove sta il repository: la radice viene da chi lancia, e un percorso assoluto in un flusso è un flusso che si può eseguire in un posto solo. Riguarda anche il guasto 17: ciò che un passo dà per scontato va dichiarato, e `flow check` è il posto per dirlo | **chiuso** il 31/08 — con quattro mutanti. La radice viene da chi lancia (`workspace.root` nello stato condiviso, che arriva a **ogni** azione per costruzione — un rinvio sarebbe passato da `resolve_references`, che il guasto 28 misura chiamata da due azioni su nove) e i percorsi si risolvono in `step_input`, **dove l'ingresso si compone**, così ogni azione futura la eredita. `flow check` distingue un campo di posizione — errore, il flusso gira in un posto solo — da un percorso dentro un testo, che è un avviso perché il flusso gira e a sbagliare è l'istruzione. **La prova che l'ha misurato è nata rossa con 7 errori e 2 avvisi** sul flusso vero, ed è verde perché la migrazione è avvenuta, non perché è stata scritta dopo. I sette `workdir` li ha tolti `sailor flow relocate`, non una mano: è anche un morso al guasto 15 |
| 27 | 31/08 | **Il guasto 1 è tornato da un'altra porta.** Gli argomenti del blocco `usage` di un descrittore si accodavano **dopo** quelli di `ask`: per `agy` la riga diventava `--mode plan --print --output-format json «prompt»`, e `--print` prendeva `--output-format` come propria domanda. Il prompt veniva ignorato. **Il secondo motore di ogni catena non ha mai funzionato** | Provando la riga a mano: `agy` stampa il difetto da solo — «Error: --print took "--output-format" as its prompt, so the intended prompt was left as an argument and ignored». Nessun flusso mette `agy` per primo, quindi su questa macchina quel ramo non era mai stato eseguito | I due blocchi erano giusti separatamente e sbagliati **insieme**: una prova per blocco non può vederlo. Un ordine globale non basta — `codex` ha `exec` come sottocomando e vuole stare primo, `agy` vuole `--print` ultimo: due vincoli opposti sullo stesso posto, quindi li dichiara il descrittore (`ask.args_before_prompt`). Resta scoperta la cura scritta accanto al guasto 1: **eseguire davvero** ogni riga composta, per **ogni combinazione** di blocchi che la compongono e non per la sola forma nominale. E resta che una catena di motori dovrebbe **provare il successivo** quando il primo muore per una riga malformata, invece di fermare il passo | **chiuso** — con mutante che mostra la riga sbagliata parola per parola |
| 28 | 31/08 | **Solo due azioni su nove risolvono i rinvii, e il deposito non è fra quelle.** `reference::resolve_references` è chiamato in due punti soli di tutto l'albero — dentro `external_engine` e `shell_check` (`crates/actions/src/lib.rs:1613` e `:1760`). `store_write`, `store_read`, `store_list` e `history_ask` non lo chiamano mai: un `{"$from": "/x"}` arriva loro come oggetto, e `serde_json::from_value` muore perché `key` vuole una stringa. **Conseguenza: il deposito non può ricevere niente da un passo precedente**, cioè non può fare il testimone che `docs/decisioni.md` gli attribuisce — accetta solo valori scritti a mano nel flusso | Con due fixture a costo zero, identiche tranne una cosa: `flows/prova-deposito.flow.json` (chiave presa con un rinvio) **fallisce**, `prova-deposito-letterale.flow.json` (stessa chiave scritta a mano) **passa**. Prima di quelle, la corsa che l'ha rivelato aveva speso 8,95 $ per morire sull'ultimo passo, buttando una diagnosi che tutti e cinque i motori avevano già prodotto | La risoluzione dei rinvii non appartiene alla singola azione: è come i passi si passano le informazioni, e va fatta **una volta sola dove l'ingresso si compone**, così ogni azione registrata — comprese quelle che nessuno ha ancora scritto — la eredita. Finché sta dentro le azioni, ogni azione nuova nasce senza, e nessun controllo lo dice. Serve anche che `flow check` veda un rinvio dentro il `with` di un'azione che non lo risolve: è il guasto 20 su un'altra dimensione | **aperto** |
| 29 | 31/08 | Il numero grande dei token sulla plancia era **ingresso + uscita** e si chiamava «Token»: 89.402 contro 5.687.230 davvero consumati, **63,6 volte meno**. La cache scritta — la voce a 10 $/M, il 44,8% del costo di una corsa misurata — non compariva da nessuna parte, mentre il commento del suo campo in `dashboard.rs:94` la dichiarava «tenuta visibile» | Guardando la pagina, e poi ricontando dal deposito con `sqlite3`. Il file stesso porta scritta la regola che violava: «un totale parziale che dichiara di essere parziale è una misura; uno che tace si presenta come completo, ed è la bugia da cui il conteggio dei consumi è nato» | Il totale somma **tutte e quattro** le voci, e le quattro restano separate accanto perché hanno prezzi diversi fino a venti volte. Il costo era già nel dato servito (`cost_micros`) e nessuno lo mostrava: ora ha il suo riquadro. E chi legge decide se può lanciare un'altra corsa: lo decideva sul falso | **chiuso** — verificato sullo schermo prima e dopo, e la prima correzione **sbordava dalla cella**: il numero giusto reso illeggibile vale quanto quello sbagliato |
| 30 | 31/08 | **La tela diceva «in attesa» su ogni nodo di ogni flusso vero, anche mentre il motore lavorava.** Non nascondeva: raccontava il falso. `App.tsx:527-530` passava al disegno una mappa di stati che è quella d'esempio solo fuori dal guscio nativo, e `new Map()` dentro — mentre `executions`, i fatti veri che arrivano per evento ogni secondo, non era dipendenza né del disegno né della tela | Una lettura del codice partita dalla domanda «cosa vede l'utente finale», e poi guardando la finestra vera aperta sui dodici flussi dal disco | È la violazione più netta possibile del vincolo permanente «un'interfaccia che nasconde cosa succede è il contrario del prodotto», e nessun tipo poteva vederla: la tela compilava. Serve che i fatti arrivino ai nodi **da un contesto** e non dai loro `data` — i `data` fanno parte dell'elenco dei nodi, e un elenco ricostruito a ogni fatto ha già mandato questa tela in ciclo infinito una volta | **chiuso** — con cinque mutanti sul calcolo e due sul collegamento, fra cui il difetto originale rimesso. E la finestra ha le **prime prove della sua vita**: `npm test`, 12 verdi, ora dentro `npm run build` |
| 31 | 31/08 | **Una catena di motori senza ripiego.** `agy` non dichiara nessun `unusable_when`, e `says_it_cannot_work` su un elenco vuoto è `false`: il suo esaurirsi passa per un fallimento qualunque, il passo muore lì, e nella catena `claude-code → agy → codex` **`codex` non parte mai**. È il guasto 16 un piano più sotto — allora mancava la catena, adesso c'è e il ripiego no — e vale su tutti e tre i flussi dell'albero che nominano una catena | Leggendo il descrittore spedito, e poi rendendolo un fatto a costo zero: due finti motori identici tranne l'elenco delle parole. Elenco popolato, il secondo parte; elenco vuoto, il passo muore e il secondo non parte (`an_engine_that_declares_no_exhaustion_words_kills_the_chain`, in coppia con `an_engine_that_says_it_is_out_hands_the_work_to_the_next_one`) | La regola scritta: **ogni motore che compare in una catena e non è l'ultimo dichiara almeno una parola di `unusable_when`** — `crates/sailor/tests/a_chain_can_fall_back.rs`, `every_engine_that_is_not_last_in_a_chain_says_how_it_is_exhausted`. È `#[ignore]` con la ragione scritta, perché la cura è **misurare** come `agy` dice di aver finito la quota e inventarla sarebbe peggio del difetto: una parola sbagliata lì manda un mandato malformato giù per tutta la catena. Accanto, due prove che girano sempre: che esistano ancora catene da controllare, e che **nessun motore oltre ad `agy`** sia muto in mezzo a una — o un quarto entrerebbe dietro un `#[ignore]` che parla d'altro | **aperto** |
| 32 | 31/08 | **La stessa verità in due blocchi, di cui uno inservibile.** `gemini-cli` dichiara `capabilities.ask_without_interaction` e **non ha blocco `ask`**: la riga per interrogarlo non si compone, nessuna catena lo nomina, e in 23 chiamate registrate non è mai stato invocato. Una capacità dichiarata che non serve a niente somiglia in tutto a una che c'è | Confrontando i due blocchi dello stesso file, cosa che nessun controllo faceva: `ask` e `capabilities` rispondono alla stessa domanda e potevano dire cose diverse senza che niente diventasse rosso | Una prova che pretende che i due blocchi dicano la stessa cosa, in tutte e due le direzioni — `crates/toolbox/src/descriptor.rs`. La direzione «chi ha la riga dichiara anche la capacità, con le stesse opzioni» è verde e gira sempre; la direzione «chi dichiara la capacità ha una riga» è `#[ignore]`, perché scrivere l'`ask` di `gemini` oggi vorrebbe dire inventare come dichiara di essere esaurito — cioè creare un quarto guasto 31 per chiudere il 32. Accanto, sempre attiva, `no_engine_beyond_the_registered_one_claims_an_ask_it_cannot_compose` | **aperto** |
| 22 | 31/08 | Il costo totale di una corsa era la **costante `0`**, scritta a mano in due punti, mentre le sue chiamate portavano il costo giusto una per una. La finestra mostrava lo zero accanto alla somma vera | Cercando su quale numero appoggiare un tetto di spesa. Era già registrato in `docs/da-fare.md` dal 29/08 — «costo totale sulle 16 corse: 0 su tutte» — e nessuno l'aveva letto come un guasto | Un totale non si dichiara: si chiede a chi tiene le righe. E le due copie di quella scrittura diventano una, nel crate che le due strade condividono già | **chiuso** — con mutante |
| 33 | 31/08 | **Il deposito non sa distinguere «uscita nulla» da «nessuna uscita».** `StepRecord::output` è un `Option<Value>`: `Some(Value::Null)` si serializza nel registro degli eventi come `"output": null`, identico a `None`, e rileggendo torna `None`. Un passo chiuso `Went` con uscita nulla risulta senza uscita, e il passo che ne dipende muore con «non ha uscita tipata» — tre passi dopo, su un passo innocente | Alla **prima corsa vera** del passo consegnato: `sailor step close --outcome went` senza `--output-file`, poi `sailor flow resume`, che è morto con «step implementa has no typed output». Nessuna delle 520 prove lo vedeva, perché nessun'altra strada produce `Some(Value::Null)`: `run_one` scrive l'uscita che l'azione dichiara, e nessuna azione registrata dichiara `null` | La distinzione va **rappresentata**, non dedotta: finché due valori diversi si scrivono uguali, nessun controllo a valle può separarli. Nel frattempo il difetto non deve poter nascere — chi chiude un passo a mano viene fermato **subito** se qualcuno a valle pretende quell'uscita, invece di scoprirlo dove il difetto non è. È la stessa regola dell'uscita validata contro lo schema: un difetto che si manifesta lontano da dove nasce costa il doppio | **chiuso in parte** — `sailor step close` rifiuta di chiudere «andato» senza uscita quando un passo non saltabile dipende da quello, con mutante. **Resta aperto il formato**: `Some(Value::Null)` continua a non sopravvivere al registro degli eventi, e il giorno in cui un'azione lo produce il difetto torna |
| 34 | 31/08 | **`cargo` e `git` finivano in `model_calls`**, la tabella delle chiamate ai modelli: `record_the_call` scriveva una riga per qualunque strumento chiesto per identificativo. Nessuno dei due consuma quota, e tutti e due arrivano senza costo — quindi `Spend::is_complete()` era **falso su ogni corsa vera**, e la frase d'onestà del tetto («la spesa vera è più alta») si accendeva sempre. Un avviso sempre acceso non lo legge nessuno, e a perdersi è quello vero: la riga di codex, che il costo davvero non lo dichiara | Contando le righe del deposito per flusso mentre si cercava su quali corse tarare un tetto: su ventiquattro righe, due `git` e una `cargo`. La corsa `sviluppa-sailor` ne aveva sette, tre senza costo, e tutte e tre erano queste | **Chi è un motore lo dice il descrittore, non un elenco di nomi nel codice**: uno strumento è un motore se dichiara come gli si fa una domanda (`ask`). Un elenco a mano invecchierebbe al primo strumento nuovo e nessun controllo lo direbbe — è il guasto 3 dall'altro lato. Serve anche la prova gemella: un motore vero interrogato con le opzioni scritte dal passo **resta** nel conto, o il filtro potrebbe svuotare la tabella e sembrare giusto | **chiuso** — la prova nasce rossa sulla riga vera, e la gemella tiene fermo che i motori restino |
| 35 | 31/08 | **Il listino dei prezzi non è versionato, e senza di lui nessun tetto scatta mai — in silenzio.** `load_pricing()` legge `~/.config/sailor/pricing.json`; in git c'è solo `crates/models/pricing.example.json`. Su una macchina dove quel file manca ogni `cost_micros` è `None`, la spesa di ogni corsa resta zero, e un flusso con `spend_cap_micros` gira fino in fondo senza nessun errore e senza nessun avviso | Leggendo `load_pricing` mentre si costruiva il tetto: il suo stesso commento dichiara che «un listino assente non è un guasto: lascia il costo sconosciuto», e quella frase era vera prima che esistesse un tetto appoggiato su quel numero | **Il listino va versionato, non il tetto ammorbidito.** La cura sbagliata e ovvia sarebbe far fallire una corsa senza listino: punirebbe chi non c'entra e non direbbe comunque quanto costa. La cura giusta è che il listino spedito col prodotto sia quello vero — come i descrittori degli strumenti e i flussi di sistema, incorporati nel binario — e che il file in casa lo **sovrascriva** invece di essere l'unica copia. Serve anche che `flow check` dica «tetto dichiarato, ma su questa macchina nessun listino: il tetto non scatterà», perché un freno che non frena si deve vedere prima di lanciare | **chiuso** 01/09 — `crates/models/pricing.default.json` è incorporato con `include_str!` come i descrittori, e il file di casa lo sovrascrive **per `id`**, la voce intera. Due valute non si mescolano: un listino di casa in un'altra valuta vale da solo, invece di tirarsi dietro voci che finirebbero sommate nella stessa colonna. La frase che il tetto voleva è cambiata di forma perché un listino c'è sempre: `flow check` e `flow cost` nominano ora i **modelli** che il listino non sa prezzare, col perché (voce assente / voce senza prezzi), e con un tetto dichiarato aggiungono che quelle chiamate il tetto non le conterà. *Mutante*: svuotare `models` in `pricing.default.json` — sei prove rosse in tre crate, compresa la catena intera del descrittore spedito |
| 36 | 31/08 | **Nove agenti in parallelo: quattro conflitti che git segnala e quattro che non vede.** Quelli segnalati erano tutti banali — due moduli sulla stessa riga, due funzioni nello stesso punto, l'elenco dei membri del workspace. Gli altri quattro no: *(a)* `spend-cap` costruisce un `ModelCallRecord`, `session-resume` gli aggiunge un campo — zero righe in comune, albero che non compila; *(b)* `workspace-root` scrive il controllo sui percorsi in chiaro, e il file che lo viola sta su un altro ramo; *(c)* due rami sono **avanzati mentre li fondevo**, e uno dei commit nuovi era proprio l'anello che mancava; *(d)* `subflow` e `workspace-root` aggiungono ciascuno una `[dev-dependencies]` a `crates/registry/Cargo.toml`, in due punti diversi: git le fonde, cargo rifiuta l'intero workspace con «duplicate key» | Fondendo gli otto rami uno per uno in un albero usa-e-getta e lanciando la batteria dopo ognuno. Nessuno dei quattro era visibile dal ramo che lo conteneva, e ognuno dei nove agenti aveva la propria batteria verde | **Il confine sui file non basta, perché i legami passano da un tipo, da un file di dati e da un manifesto** — è il 33 di questa tabella su un'altra dimensione: là era la numerazione condivisa, qui sono le strutture condivise. Le tre difese che mancano: la batteria si lancia **sull'albero fuso**, non sul ramo; prima di fondere si ricontrolla che il ramo non sia avanzato, perché un agente vivo committa mentre fondi; e un manifesto TOML non ha nessuna prova che lo guardi. Ma la difesa vera è **che gli agenti si vedano**, ed è `work/coordination` — scritto lo stesso giorno, e ancora fuori servizio mentre questo guasto accadeva | **chiuso** — l'albero fuso è verde, e la difesa del manifesto è scritta dentro il manifesto |
| 37 | 31/08 | **`sailor flow cost` mente di 4,3 volte su una corsa con passi consegnati, e non lo dice abbastanza forte.** Un passo `handed_to_agent` non viene misurato: il consumo lo **dichiara l'agente** con `--turns`, e l'agente non sa contare ciò che il proprio harness consuma per lui. Misurato sull'A/B del 31/08: la corsa ha dichiarato **1,6674 $**, ne è costati **7,2080**; i turni dichiarati erano 33 su **75** veri, cioè il **44%**. Il comando stampa «parziale: 3 chiamate senza costo noto», che è vero e non basta: chi legge un totale legge il numero, non la nota | Rifacendo l'A/B del passo consegnato e sommando a mano il consumo della sessione viva (`claude -p --output-format json`) con quello che il flusso ha avviato da sé. Nessuna prova lo vedeva: tutte usano depositi dove ogni chiamata ha un costo | Un totale che contiene una chiamata senza costo **non è un totale**: o si mostra come intervallo («almeno X»), o si rifiuta di sommare e si elencano le voci. E finché il consumo di un passo consegnato è autodichiarato, `flow cost` deve dirlo **al posto del numero**, non accanto. La cura vera è misurare invece di credere: leggere il consumo della sessione che ha eseguito, non chiederglielo | **chiuso in parte** il 01/09 — con mutante. **La bugia è chiusa**: `Spend` distingueva i tre casi dal giorno in cui è nato e la distinzione non arrivava a chi legge, perché l'unico modo di interrogarla era un booleano che chi stampa poteva scrivere *accanto* al numero. Adesso `Spend::reading()` restituisce il caso, e `flow cost` non ha più dove mettere la cifra secca: sui numeri veri di quella corsa dice «almeno 1,6674, e il vero è più alto — 3 chiamate su 4 non sono misurate», e senza nemmeno una misura dice «sconosciuto» invece di «almeno 0,0000», che sarebbe vero e si leggerebbe come una spesa piccola. Il mutante che conta — `is_complete()` sempre vero, cioè il difetto originale — rimette la cifra secca e fa cadere quattro prove. **Resta aperta la misura**, ed è la metà che vale: un passo consegnato continua a dichiarare i propri turni con `--turns`, e la dichiarazione è ora marcata come tale invece di essere confusa con un conto. Aperto il primo canale che legge invece di chiedere — `models::remaining` legge le finestre di quota dal canale OAuth di Claude Code, HTTP 200 misurato il 01/09, solo lettura e nessun costo — ma **è quota della persona, non consumo di una corsa**, e le due non si sostituiscono: leggere quanto è costata *quella sessione* vuol dire leggerne il diario, che nessuno fa ancora | 
| 38 | 31/08 | **Il grafo delle dipendenze di SocratiCode dà falsi orfani su Rust, e noi ci abbiamo appoggiato una decisione.** Misurato oggi su questo repo: **58 archi per 101 file** (0,6 dipendenze per file), e fra i «file orfani, nessuno dipende da questo» c'è `crates/actions/src/lib.rs`, che è il file più centrale del sistema. Non legge `mod` e `use` di Rust. Il prompt del passo `piano` avvisa già di verificare aprendo il file, ma l'avviso sta in un prompt: chi interroga il grafo da fuori non lo legge | Interrogando `codebase_graph_stats` sul repo mentre si mettevano in ordine le voci aperte, e riconoscendo un nome che non poteva essere orfano | Uno strumento esterno si misura prima di appoggiarci una decisione, e la misura si scrive dove la leggerà chi lo usa — non dentro il prompt di un passo. Il canarino è il sopravvissuto noto che sparisce: qui è `actions/src/lib.rs`, e chiedere il grafo su di lui dice in un colpo se lo strumento è affidabile. **La ricerca semantica dello stesso strumento invece regge** e va usata: 1466 pezzi, sorveglianza attiva | **aperto** |
| 39 | 01/09 | **Il vaglio a secco provava la riga in un mondo diverso da quello in cui gira.** Chiusa la strada del guasto 18, un passo fa partire il motore nella casa del profilo attivo; `RealDryProbe` — quello che `sailor flow check` usa per dire «riga di comando sana» — passava `env: BTreeMap::new()`, cioè ereditava la casa di chi aveva aperto il terminale. Le due strade divergono proprio dove fa più male: il controllo prova un motore **autenticato**, la corsa parte in una casa che può non avere nessuna credenziale. Il verde chiude, la corsa fallisce, e chi ha letto il verde non ha sbagliato niente. Non è un caso di scuola: lo stesso giorno i due profili `codex` dichiarati su questa macchina puntavano tutti e due a cartelle **senza `auth.json`** | Guardando cosa cambiava per il vaglio dopo aver fatto arrivare la dotazione ai motori — cioè cercando l'effetto collaterale della riparazione, non un difetto. Nessuna prova lo vedeva: quelle del vaglio iniettano un finto eseguibile e non guardano mai l'ambiente | **Un controllo che prova un mondo diverso da quello in cui si lavora è peggio di nessun controllo, perché rassicura.** La cura NON è un controllo nuovo sulle credenziali: sarebbe una seconda copia della verità, un «esiste `auth.json`?» da riscrivere per ogni motore. I descrittori dichiarano **già** con quali parole un motore dice di non poter lavorare (`unusable_when`, che nomina proprio le credenziali mancanti). Serve la prova end-to-end con un processo vero che stampa la variabile ricevuta — una prova sulla regola resterebbe verde con la regola scollegata, che è il guasto 18 daccapo | **chiuso** il 01/09, in due tempi, e il secondo dice più del primo. **Primo tempo, la divergenza**: `env: BTreeMap::new()` rimette il difetto e la prova torna rossa con `CASA=` vuota — il vaglio e la corsa partono dalla stessa casa. **Secondo tempo, ciò che restava**: la ragione con cui avevo dichiarato inutile un controllo sulle credenziali — «la diagnosi la dà il motore con le sue parole» — era stata scritta prima di misurarla, ed è falsa. Il vaglio a secco toglie la domanda apposta, quindi non vede niente di ciò che il motore controlla DOPO averla letta, e le credenziali stanno di là: rimisurato il 01/09 nelle due case, `codex exec < /dev/null` risponde «No prompt provided via stdin.» **parola per parola la stessa cosa**, e ne esce 1 tutte e due le volte — *fino a questa riscrittura questa riga diceva «esce zero», ed era falso; la conclusione regge lo stesso perché si appoggia all'identità delle due risposte, non al numero*. **LA CURA NON ERA UN VAGLIO MIGLIORE: ERA UNA DOMANDA IN PIÙ.** I descrittori dichiarano ora `login_status` — le opzioni con cui si chiede, e le parole con cui quel motore dice sì e dice no — e `sailor flow check` e `sailor profiles list` la fanno prima di lanciare, gratis, senza chiamare nessun fornitore. Chi non dichiara il blocco non fa scattare niente: **vuoto vuol dire «nessuno ha guardato», mai «è autenticato»**. Misurato sul comando vero, con i due profili `codex` di questa macchina senza `auth.json`: `flow check smista-il-lavoro` è passato da «righe di comando sane: codex» e nient'altro a «CASE SENZA CREDENZIALI … codex (profilo «codex/prove», CODEX_HOME=…): «Not logged in»», e `profiles list` da due righe di soli nomi a due righe che dicono NON AUTENTICATO tutte e due. **Cinque mutanti eseguiti**, e il primo è il difetto originale: leggere le parole del sì prima di quelle del no fa risultare piena una casa vuota — «Not logged in» contiene «logged in»; togliere la chiamata dal rapporto rimette il vaglio muto; togliere `login_status` dal descrittore fa dire «nessuno ha guardato» e mai «autenticato»; far rispondere «autenticato» a un `LoggedOut` in `profiles list`; togliere il ramo booleano al lettore di puntatori, e claude non si legge più |
| 40 | 01/09 | **Uno stato di mezzo toglieva il guasto dal conto degli aperti.** La tabella usa «chiuso in parte» per una riga in cui metà della cura è fatta; `the_fault_table_holds_together` contava aperto **solo** ciò che dice esattamente «aperto», quindi ogni «chiuso in parte» spariva dal totale. La prosa diceva undici quando erano quattordici: il 14 e il 33 erano usciti dal conto il 31/08 e nessuno se n'era accorto, e il 37 stava per uscirne il 01/09 | Aggiungendo un campo `partly` al controllo per verificare il lavoro di un agente, e scoprendo che il campo **esisteva già, calcolato e mai interrogato**. Il conto vero l'ha detto la prova stessa quando le si è chiesto di contarli: 14, non 11 | **La direzione dell'errore non è casuale: è sempre quella che tranquillizza.** Un guasto conta come aperto finché la cura che *dichiara* non è fatta, e lo stato di mezzo serve solo a raccontare quale metà è fatta — la regola sta in `Fault::still_open()`, non nella testa di chi aggiorna la prosa. E un campo calcolato che nessuno interroga non è una difesa: è la forma di una difesa, e passa una revisione | **chiuso** il 01/09 — la prova nasce rossa sul conto vecchio e nomina il numero giusto |
| 44 | 01/09 | **`supervisor::child::Process::start` non sovrappone nessun ambiente al processo che accende, perché `Spec` non ha un campo `env`.** È il guasto 18 in un secondo posto: `Command::new(&spec.command).args().current_dir().stdin().spawn()`, e nient'altro — il figlio eredita in blocco l'ambiente di chi ha aperto il terminale, e non c'è nessuna strada per dargliene un altro. Oggi è **innocuo**, perché da lì partono solo `npm run dev` e la finestra, che l'ambiente di chi li lancia lo vogliono davvero. Smette di esserlo il giorno che il supervisore accende una riga di comando con un profilo: il profilo non arriverà, e il motore dirà «non sono autenticato» invece di «nessuno mi ha passato la casa» | Leggendo `crates/supervisor/src/child.rs`: sette campi in `Spec` e nessuno è `env`; `grep` di `.env(`, `.envs(` ed `env_clear` su tutto il crate non trova niente fuori dai `std::env::temp_dir()` delle prove | Un campo `env` nello `Spec` e **una prova end-to-end che accende un processo vero e legge la variabile che il figlio ha ricevuto** — non una prova sulla regola, che resterebbe verde con la regola scollegata: è esattamente il modo in cui il guasto 18 era sopravvissuto la prima volta. Finché `Spec` non ha il campo quella prova non compila, ed è il modo giusto di nascere rossa | **aperto** |
| 45 | 01/09 | **`sailor session list` e `sailor session census` uscivano 1 pretendendo un tty che non usano.** Le due forme non parlano del terminale da cui le si invoca — parlano di tutti — ma il tty veniva risolto per ogni forma, prima del dispatch. Ovunque l'uscita sia catturata, cioè in ogni script, in ogni gancio e in ogni prova, nessuno dei tre descrittori è un tty: il comando falliva sempre, e il messaggio mandava a cercare l'opzione `--tty` invece del difetto | **Solo eseguendo il binario vero.** Le 133 prove del crate erano verdi: provano `act`, che riceve il tty già in mano, e non `dispatch`, che lo deduce dal processo. Il pezzo non provato era esattamente il pezzo che non si può provare senza eseguire | Una prova che passa da `dispatch` — non dal corpo sotto — per ogni forma che **non** nomina un terminale. Gira dentro la batteria, dove l'uscita è catturata, quindi il tty manca davvero e la prova non deve simularne l'assenza. Più in generale: il confine fra ciò che una prova costruisce e ciò che il programma deduce dal proprio processo è dove i difetti si nascondono, e va attraversato almeno una volta per comando | **chiuso** il 01/09 — con `NEEDS_A_TERMINAL`, e la prova nasce rossa col messaggio del difetto vero |

| 41 | 01/09 | **Il vocabolario delle azioni della finestra e il registro del motore divergevano nei due versi contemporaneamente, e nessuno dei due sapeva dell'altro.** Verso uno: `ACTION_KIND` (`desktop/src/flow.ts`) nominava **sei azioni che non esistono in nessun crate** — `pane_until_idle`, `signal_is_gone`, `deposit_write`, `pane_send`, `hand_to_human`, `pane_read` — e quattro stavano nella cassetta dei passi, quindi premere «attesa», «deposito», «gesto» o «a una persona» creava un nodo che poi **non si salvava**. Quattro famiglie su nove erano bottoni che non funzionano. Verso due, peggiore: `flows.rs` costruiva **un quinto registro a mano** — `register_default` più due righe scelte — invece di chiamare `registry::default_registry`, e perciò **rifiutava al salvataggio cinque azioni che dal terminale girano**: `detect_tools`, `tool_needs`, `history_ask`, `subflow` e `handed_to_agent`, cioè il passo che consegna il lavoro a una persona — una funzione centrale del prodotto non si poteva comporre dalla finestra. Verso tre, silenzioso: `kindOf` ripiega su «verifica» per un nome che non conosce, quindi i sette passi `trigger` dei flussi veri si disegnavano da sempre come nodi di controllo | Censendo la superficie del prodotto per rifarne l'interfaccia, non cercando un difetto. **Nessuna prova lo vedeva, e due commenti dichiaravano il contrario di ciò che il codice faceva**: `flow.ts` diceva «il motore ne registra due» (vero fino al 28/08, poi sedici) e `flows.rs` diceva «la stessa lista che usa `sailor flow check`, mai una copia riscritta qui» proprio sopra la copia | **Una regola scritta in un commento e non interrogata da niente non è una difesa: è la forma di una difesa, e supera una revisione.** L'ancora deve stare **fuori da tutte e due le copie** — una prova che legge il sorgente della finestra e lo confronta col registro del motore, **nei due versi**: nessun nome inventato di qua, nessuna azione del motore lasciata senza famiglia di là. Confrontare due mappe scritte a mano le lascia sbagliare insieme, ed è il guasto 19. E il ripiego silenzioso va tolto insieme alla causa: `kindOf` che ricade su un valore plausibile è ciò che ha nascosto il terzo verso per quattro giorni | **chiuso** il 01/09 — con mutante. Le due prove (`the_window_knows_every_action_the_engine_can_run`, `the_window_vocabulary_names_only_actions_the_engine_registers`, in `desktop/src-tauri/src/flows.rs`) sono nate rosse nominando i dieci nomi esatti, cinque per verso. `flows.rs` ora delega a `registry::default_registry`; il vocabolario elenca le sedici azioni vere e la cassetta ne offre sette. Il mutante che rimette un nome inventato — `pane_send` — riporta la seconda prova al rosso. **Restano dichiarati e non riparati** due limiti: i dati d'esempio (`sample.ts`) descrivono ancora la staffetta con cinque azioni che non esistono, ed è scritto in testa al file perché quel flusso racconta come dovrà essere, non come il motore la sa fare; e le due famiglie «attesa» e «ramo» non hanno nessuna azione, quindi la cassetta non le offre — è la lista della spesa vera, ed è corta |
| 42 | 01/09 | **Una risorsa che tutte le sessioni scrivono e nessuna sorveglia fa danno in silenzio, e in un giorno solo ne abbiamo viste quattro facce.** *(a)* **I numeri di questa tabella**: il 31/08 quattro agenti in alberi isolati hanno rivendicato gli stessi due numeri (guasto 33), e l'isolamento non protegge un contatore. *(b)* **La `user_version` del deposito**: un cantiere ha migrato `~/.claude/state/flussi/state.db` alla versione 8 sul file **vero** invece che su una copia, e da quel momento il binario installato — fermo alla 7 — si è rifiutato di aprirlo. Nessun dato perso (`events.db` intatto, la proiezione si ricostruisce), ma la macchina è rimasta senza il suo registro finché non è stata fusa la versione 8. *(c)* **L'albero di lavoro condiviso**: trenta file non committati in `/Users/theo/personal/sailor` contenevano il lavoro mescolato di **tre** sessioni — +227 righe in `crates/actions/src/lib.rs` che nessuna delle due sessioni presenti aveva scritto, e in `flow_cmd.rs` un pezzo non più separabile perché sulle stesse righe di contesto. **L'attribuzione è persa per sempre**, e un ramo verde è rimasto fermo ventiquattr'ore perché nessuno poteva fondere sopra quello sporco. *(d)* **E la scoperta che le lega**: la numerazione **non è nemmeno rappresentabile** fra rami. Provando a usare il 43 su un ramo nuovo, `every_fault_has_its_own_number_and_none_is_missing` è nata rossa con «numeri saltati: [41]» — perché su quel ramo la tabella arriva a 40. Un numero «riservato» per accordo fra sessioni vive solo nei messaggi che si scambiano: **non esiste nessun posto, visibile a tutti i rami, dove scrivere che è preso**, e la prova che guarda il file lo rifiuta giustamente | Tre volte per averle sbattute contro: i numeri contesi il 31/08, l'errore `unsupported projection schema version 8` sul deposito vivo, e il diff letto prima di committare che conteneva codice di altri. La quarta — la non rappresentabilità — l'ha trovata una prova esistente, nata rossa su un numero riservato a voce | **Le prime due difese sono di procedura e valgono da subito.** (1) **Nessuna sessione lavora nell'albero condiviso**: ognuna in una copia propria, e si fonde solo da lì — è la difesa che il caso *(c)* rende non negoziabile, perché il suo danno è già irreversibile. (2) **Nessun cantiere tocca lo stato vero della macchina**: il deposito si apre con `SAILOR_LEDGER` puntato a una copia, e chi migra lo fa lì. **La terza è codice, e la parte difficile è che deve guardare fuori dal proprio ramo.** Per la versione dello schema: una prova che confronti `PROJECTION_SCHEMA_VERSION` con le migrazioni presenti, così due rami che definiscono la stessa versione successiva si scontrano **in rosso** invece che in silenzio sul disco di chi lavora. Per i numeri dei guasti la prova interna **non basta e non può bastare** — un numero preso da un altro ramo è invisibile —: serve un registro fuori dalla tabella, o l'ammissione che il numero si assegna **al momento della fusione** e non prima. Finché non c'è, chi rivendica un numero lo scriva **dentro la riga**: alla fusione git mostra un conflitto vero invece di un silenzio | **aperto** — le due difese di procedura sono in vigore dal 01/09; il codice no |
| 43 | 01/09 | **IL NUMERO È RIVENDICATO E CONTESO, come chiede il guasto 42: anche `work/session-tracking` tiene un 43 e un 44, e chi fonde rinumeri.** Detto questo: l'inventario rispondeva «zero repo» con uscita 0 su qualunque macchina che non fosse quella di chi l'aveva scritto. Due metà che si nascondevano a vicenda — `default_roots` cercava i repo sotto `~/gyver/work` e `~/personal` **compilate nel binario**, e `repos_under` incontrava una base che non si apre, faceva `continue` e restituiva un elenco più corto. Qui quelle cartelle esistono, quindi il difetto non si vedeva; altrove «non ce ne sono» e «non ho potuto guardare» arrivavano a chi legge nella stessa forma. È la **terza faccia del guasto 12 in un giorno solo**, dopo `launchctl` dentro il perimetro e `ps` incanalato che risponde `1` con uscita 0 | Nel censimento di ciò che non rispetta la regola fissata da Theo il 01/09 — nessun codice specifico di prodotto, nessun presupposto sulla macchina. Né `default_roots` né `repos_under` avevano una sola prova: erano scoperte tutte e due | **Le due cure devono viaggiare insieme, perché da sole non bastano.** Togliere le cartelle di una persona senza saper dire «nessuna base dichiarata» spegne l'inventario in silenzio; saper dire «non ho potuto guardare» tenendo le cartelle compilate lascia il difetto dov'è. Le basi si dichiarano (`SAILOR_WORK_ROOTS`, o il file `work-roots` nella casa di Sailor) e `Survey` porta `roots`, `unreadable` e `bases_declared` fino all'`Inventory`, così un elenco magro dice **perché** è magro. La guardia che impedisce il ritorno non prova il comportamento ma **il sorgente**: `no_ones_personal_folders_are_compiled_into_the_binary` legge `lib.rs` dal disco e cerca `join("gyver")` — la violazione non era un difetto di logica ma di abitudine, e le abitudini rientrano dalla porta | **chiuso** il 01/09 — con mutante: rimettere `home.join("gyver").join("work")` fa tornare rossa la guardia col nome della cartella nel messaggio |
| 48 | 01/09 | **Il nome di una persona decideva cosa Sailor considera installato.** In `crates/inventory/src/lib.rs` la raggiungibilità di una competenza si calcolava con `on.contains(&plugin) \|\| plugin.contains("mattpocock")`: chi pubblicava la raccolta era un ramo del codice. Gli altri due siti dello stesso nome erano il percorso da cui leggerla (`discovery.rs:26`) e il prefisso con cui invocarla (`discovery.rs:95`), quindi **ogni altra raccolta installata allo stesso modo era invisibile all'inventario**. Su questa macchina la riga dormiva perché quella cartella non esiste — verificato con `ls -d` — ma sarebbe tornata viva da sé il giorno che comparisse | Nel censimento del non-agnostico chiesto da Theo il 01/09. **Dormiva anche per un secondo motivo, ed è quello che conta**: stava dentro un `if` i cui primi due rami tornavano tutti e due `Reach::Active`. Una condizione il cui esito non cambia niente non si legge, si scorre — ed è per questo che è sopravvissuta a ogni lettura di quel file | **Sotto il nome c'era un principio vero, e il principio è sull'origine.** Una raccolta installata come cartella sotto `.claude/skills/` non è un plugin: non compare in `enabledPlugins`, e chiedere a quell'elenco se sia accesa dà «no» a una domanda che non le si applica — una risposta falsa, non negativa. Ora c'è `discovery::Origin` con tre casi (`Home`, `Plugin`, `Collection`) e un `match` di tre righe: **il ramo morto non è stato tolto, è diventato impossibile**. La guardia che impedisce il ritorno legge i sorgenti e rifiuta quel nome in `lib.rs` e `discovery.rs`, perché la violazione non era un errore di logica ma un'abitudine — far entrare nel codice il nome di ciò che si ha sotto mano | **chiuso** il 01/09 — con mutante (rimettere `\|\| name.contains("mattpocock")` fa tornare rossa la guardia) e con A/B contro la base sulla macchina vera: 55 voci prima, 55 dopo, nessuna raggiungibilità cambiata |
| 46 | 01/09 | **Il comando che esiste per dire «non lo so» moriva prima di poterlo dire.** `sailor session census` risponde `NON LO SO: non mi è stato permesso guardare la macchina` ed esce 3 — è la cura del `ps` negato in silenzio, cioè il guasto 12. Ma `dispatch` apriva `sessions.db` **prima** di sapere quale forma fosse stata chiesta, e `census` il deposito non lo tocca affatto: un deposito illeggibile — permessi, disco pieno, un file di una versione più nuova — lo faceva uscire **1** con l'errore del file al posto della propria risposta. Chi legge quell'uscita non distingue più tre fatti diversi: «il deposito non si apre», «non ho potuto guardare la macchina», «non c'è nessun terminale». **Distruggere un «non lo so» utile con un «non lo so» che parla d'altro è il modo peggiore di sbagliare** | Segnalato da chi aveva appena scritto il comando, eseguendolo dentro il perimetro; verificato scrivendo la prova, che è nata rossa nominando la forma e l'errore di SQLite. Le prove del crate erano tutte verdi: passano da `act`, che riceve il deposito già aperto, non da `dispatch`, che decide se aprirlo | **È IL GUASTO 45 UNO SCHERMO PIÙ SOTTO, NELLO STESSO FILE, LO STESSO GIORNO.** Il 45 aveva stabilito il principio — «solo chi parla di un terminale ne pretende uno», scritto in maiuscolo sopra la riga del tty — e la riga immediatamente successiva, quella che apre il deposito, non l'aveva ricevuto. Una cura scritta come commento accanto a una riga non si estende da sola alla riga dopo: **un principio vale solo dove c'è un elenco che lo applica**. Quindi `NEEDS_THE_STORE` accanto a `NEEDS_A_TERMINAL`, e `Request::store` diventa un `Option` così che una forma dimenticata sia un difetto nominato invece di un panico. E la prova non guarda `census`: gira su **ogni** forma non elencata, perché quella aggiunta domani nessuno se la ricorderà | **chiuso** il 01/09 — con mutante: aprire il deposito incondizionatamente in `dispatch` fa tornare rossa la prova con «non legge il deposito, eppure è morto perché non si apriva» |
| 47 | 01/09 | **Una prova generica stava per riconfigurare la macchina di chi lancia la batteria, e a ogni `cargo test`.** La prova del guasto 46 gira su **ogni** forma di `sailor session` che non dichiara di leggere il deposito, chiamando `dispatch` per davvero: finché quell'insieme era `["census"]` era innocua. Poche ore dopo è entrata `install`, che senza `--settings` **scrive nella casa vera** — misurato con un `HOME` finto: quattro ganci innestati in `settings.json` e due comandi scritti in `commands/`. Tre file nella `~/.claude` di chi esegue la batteria, ganci compresi, a ogni corsa. **Il danno non è avvenuto**: l'ha visto chi fondeva, prima che le due metà si incontrassero su un albero solo | Fondendo, e provando la cosa con un `HOME` finto invece di ipotizzarla. Nessuna prova poteva vederlo: le due metà erano giuste ognuna per conto suo, e la pericolosa non esisteva ancora quando è stata scritta quella generica | **UNA PROVA GENERICA COPRE ANCHE CIÒ CHE NON ESISTEVA QUANDO È STATA SCRITTA — NEL BENE E NEL MALE.** È la stessa proprietà per cui vale più di una specifica: prende la forma che qualcuno aggiunge domani. E per la stessa ragione prende il **danno** che quella forma porta con sé. La cura non è rendere la prova specifica — sarebbe buttare via il motivo per cui esiste — ma **confinare nella cartella usa-e-getta tutti gli effetti, non solo quelli che si conoscevano**: qui `--settings` accanto a `--store`, dichiarato sopra il ciclo. La regola generale: una prova che invoca un dispatch per nome deve dargli **ogni** ancora al mondo che quel dispatch potrebbe toccare, perché non sa quali toccherà. È il gemello della lezione del 46 — là un principio scritto in un commento non si estendeva alla riga dopo; qui una prova giusta non si estende da sola alle conseguenze di ciò che copre | **chiuso** il 01/09 — `--settings` nel ciclo, col perché scritto sopra la prova; verificato che `~/.claude/commands` non contiene niente di scritto dalla batteria |

## Cosa dice questa tabella, letta tutta insieme

**Quasi la metà si è vista solo eseguendo o guardando.** Non dal codice, non
dai tipi, non dalle prove: cronometrando, aprendo il deposito, facendo uno
screenshot, controllando il peso di una cartella. È la ragione per cui una prova
che non poteva venire diversa non è una prova.

**Sedici sono ancora aperti** su quarantotto, e tre di quelli (10, 28, 32)
sono la stessa cosa vista da tre lati: **la stessa verità scritta in più posti, e
nessun controllo che li confronti** — due copie di un elenco di componenti, sei
copie della risoluzione dei rinvii, due blocchi di un descrittore che dicono
cose diverse sulla stessa bandiera.

*Quattro di quei tredici (14, 18, 33, 37) dicono «chiuso in parte», e fino
al 01/09/2026 uno stato di mezzo li toglieva dal conto: il controllo contava solo
la parola «aperto», e la prosa diceva undici. Nessuno l'aveva deciso — il 14 e
il 33 erano usciti dal conto il 31/08 senza che nessuno se ne accorgesse, e la
direzione dell'errore era quella che tranquillizza. Adesso un guasto conta come
aperto finché la cura che dichiara non è fatta, e lo stato di mezzo serve solo a
raccontare quale metà è fatta.*

*Erano cinque, e il quinto era il 39, entrato fra i mezzi lo stesso giorno in cui
era nato chiuso: la riparazione reggeva, la **ragione** con cui dichiarava chiuso
anche il resto era stata misurata dopo ed era falsa. **È uscito dallo stato di
mezzo il 01/09**, ed è la prima volta che uno ne esce. Vale la pena dire come:
non riparando meglio ciò che c'era, ma accorgendosi che la metà mancante non
stava lì dentro. Il vaglio a secco era sano; gli mancava una domanda, e la
domanda si fa al motore — mai al disco, che sarebbe stata la seconda copia della
verità di cui questa tabella parla in tre righe diverse.*

*Fino al 31/08 questa riga nominava il 4, il 10 e l'11 e diceva «il sistema non
sa cosa sta facendo di sé». Il 4 e l'11 sono stati chiusi lo stesso giorno — il
deposito ha una tabella `processes`, e `crates/supervisor` tiene viva la
finestra — quindi la frase è stata riscritta su chi è aperto adesso. **Il
conteggio lo sorveglia una prova; quali numeri nomina, no**: è invecchiata per
un giorno senza che nulla diventasse rosso.*

**Il 31 e il 32 dicono che la seconda copia non si controlla da sola.** Tutti e
due sono la stessa verità scritta in due posti dello stesso file, con uno dei
due inservibile: `agy` dichiara la catena e non il ripiego, `gemini-cli`
dichiara la capacità e non la riga. Nessuno dei due rompe niente — è questa la
parte da leggere. Un ripiego che non scatta somiglia a un ripiego che non è
servito; una capacità mai usata somiglia a una capacità che c'è. Il decimo dice
la stessa cosa sul codice; questi due la dicono sui dati, che è dove il prodotto
è andato a mettere la sua parte modificabile senza ricompilare.

**E tutti e due restano aperti per la stessa ragione, che va detta.** La cura di
entrambi è un dato che nessuno ha misurato — come `agy` dichiara di aver finito
la quota, come `gemini` dichiara la stessa cosa — e i due dati si possono
scrivere in trenta secondi indovinandoli. Sarebbe peggio del difetto: una parola
sbagliata in `unusable_when` fa scendere un mandato malformato per tutta la
catena finché qualcuno risponde comunque, e nessun controllo lo vedrebbe più.
Un guasto aperto con la sua misura mancante scritta accanto vale più di un
guasto chiuso su un'invenzione.

**Quattro guasti nuovi hanno litigato per due numeri, ed è il decimo di nuovo.**
Il 31, il 32, il 33, il 34 e il 35 sono nati lo stesso giorno in quattro alberi
isolati, e tre di quegli alberi hanno scritto «31» e «32» senza vedersi.
L'isolamento serviva a non far sovrascrivere il codice, e ha funzionato: ma la
numerazione di questa tabella è una risorsa **condivisa e sequenziale**, e
l'isolamento non la protegge — la peggiora, perché dentro un albero nessuno vede
gli altri. Le prove che sorvegliano questo file hanno preso il duplicato al
primo montaggio, che è la ragione per cui esistono. Chi ripartisce del lavoro in
alberi separati deve chiedersi **quali risorse restano comuni anche là dentro**,
e questa è la prima.

**L'undicesimo dice quanto costa una causa scritta male.** La voce diceva «un
errore di compilazione uccide la finestra», e chiunque l'avesse letta avrebbe
cercato di gestire meglio l'errore — dove non c'era niente da gestire. Il difetto
era l'ordine di due righe dentro `tauri-cli`, e si vede solo leggendo il
sorgente di uno strumento di cui ci si fida. **Una voce di questo file è una
diagnosi, e una diagnosi sbagliata manda a riparare il posto sbagliato**: qui è
costata la riga «cosa è successo», riscritta il 31/08 insieme alla riparazione.

**Il ventiduesimo era già scritto, e non contava come guasto.** Il numero «costo
totale sulle 16 corse: 0 su tutte» stava in `docs/da-fare.md` dal 29/08, in una
tabella di stato del deposito. Nessuno l'ha letto come un difetto finché non è
servito appoggiarci sopra una decisione: uno zero in una colonna di misure
sembra un sistema che non ha ancora speso, non un sistema che non sa contare. Un
dato mancante si traveste da dato tranquillo, e l'unico modo per smascherarlo è
provare a usarlo per decidere qualcosa.

**Il 24 è il primo trovato usando il prodotto invece di leggerlo.** Non da una
lettura del codice, non da una prova: dal primo comando che chiedeva a Sailor una
cosa che nessuno gli aveva mai chiesto — quanto è costata una corsa. Il deposito
di questa macchina era illeggibile da un giorno intero e nessuno lo sapeva,
perché nessuno lo leggeva. È l'argomento più forte a favore dell'usare Sailor per
sviluppare Sailor: **le prove coprono il codice, non l'installazione.**

**Nessuno è stato trovato da un controllo automatico.** Tutti da una persona che
guardava. Questo è il numero che deve cambiare — e il 23 è il primo che lascia
dietro di sé un controllo capace di ritrovarlo: `cargo test -p sailor --test
identifiers_are_in_english` è nato **rosso su 64 identificatori** ed è diventato
verde riparandoli. Non è una prova scritta dopo per certificare il lavoro: è la
misura che ha detto quanto lavoro c'era.

**Il 30/08 ne sono arrivati tre in un giorno solo, e tutti e tre eseguendo.** Il
19 si è visto scrivendo un file dove la documentazione diceva e guardando che
nessuno lo leggeva; il 20 lanciando un flusso con un campo sbagliato; il 21
rieseguendo la batteria venti volte di fila invece di una. Nessuno dei tre
sarebbe uscito leggendo il codice: due erano d'accordo con se stessi, e il terzo
si vedeva solo ripetendo.

**Il quindicesimo dice perché.** Sailor non ha comandi per operare su se stesso,
quindi chi ci lavora lo aggira con script esterni — e uno strumento aggirato non
registra niente di ciò che gli succede intorno. È lo stesso difetto del quarto e
del decimo, alla radice: **il sistema non sa cosa sta facendo di sé perché non
passa da sé.**

**Il ventiseiesimo dice quanto è lunga la catena.** Tre passaggi per trovarlo:
l'autore ha scritto la prova, il verificatore l'ha mutata — davvero, su una
copia — e ha visto rosso, e solo un terzo che leggeva **senza sapere di chi
fosse il lavoro** si è accorto che la mutazione non era quella giusta. Il
verificatore aveva tolto il tetto; il difetto vero era l'attesa fissa, e su
quella la prova restava verde. «Chi crea non giudica» non basta: **anche chi
giudica va giudicato**, e il modo è togliergli il nome dell'autore.

**Il tredicesimo era già scritto, e non è servito.** La trappola di zsh stava in
una memoria da giorni: chi ha riscritto la sorveglianza — cioè chi aveva appena
finito di riparare il guasto 12 — non l'ha consultata e ci è ricaduto dentro
venti minuti dopo. È la prova che una lezione scritta non vale niente se non c'è
il momento in cui qualcuno la va a leggere. Per i flussi quel momento adesso
esiste, ed è `docs/decisioni.md`, letto in tre punti del flusso di sviluppo. Per
chi scrive a mano no.
