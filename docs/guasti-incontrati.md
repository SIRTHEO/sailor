# I guasti incontrati mentre si costruiva

Ogni riga qui è un guasto **vero**, capitato davvero, con la data. Nessuno è
ipotetico e nessuno è stato scritto per fare esempio.

**A cosa serve questo file, e perché non è un diario.** Un guasto riparato a mano
resta nella testa di chi l'ha riparato. La volta dopo ci ricasca un altro — è
successo il 29/08/2026 con lo stesso processo orfano, a due persone diverse,
nella stessa notte, e la seconda non sapeva della prima. Questo file è il
materiale grezzo di ciò che Sailor deve imparare a fare da solo: leggere com'è
andata e ricavarne un controllo. Finché quell'anello non è chiuso, la lista si
tiene a mano.

La forma di ogni voce viene da un lavoro che il flusso di ricerca ha trovato in
letteratura il 29/08/2026 — *Sillito & Kutomi, «Failures and Fixes: A Study of
Software System Incident Response»* — e dalla sua conclusione, che qui è la
regola: **il seguito di un guasto è un controllo, non un compito assegnato a
qualcuno.** Una voce senza la colonna «cosa lo impedirebbe» non è finita.

| # | data | cosa è successo | come si è visto | cosa lo impedirebbe | stato |
|---|---|---|---|---|---|
| 1 | 28/08 | Un motore esterno invocato con gli argomenti nell'ordine sbagliato: il prompt finiva ignorato | Solo eseguendolo. Le opzioni erano state lette dalla documentazione e mai provate | Una prova che esegue davvero ogni riga di comando prima che finisca in un flusso | **chiuso** — riga corretta e misurata |
| 2 | 28/08 | Un passo che falliva veniva registrato come riuscito, col codice d'errore sepolto nel risultato | Leggendo il deposito a mano | Un'uscita non-zero rompe il passo, con tolleranza da chiedere esplicitamente | **chiuso** — con mutante |
| 3 | 28/08 | Il controllo statico chiudeva senza errori su un flusso che nominava uno strumento inesistente | Provando a mano un flusso rotto | Il controllo distingue «non c'è qui» da «non esiste in nessun catalogo» | **chiuso** — con mutante |
| 4 | 29/08 | Un processo di sviluppo orfano occupava una porta e impediva l'avvio | Due volte, a due persone diverse, nella stessa notte | Sailor deve sapere quali processi ha avviato, per spegnerli e riprenderli | **chiuso in parte** il 31/08, riaperto il 01/09 — il deposito ha una tabella `processes`, ogni processo che Sailor accende ci passa (`supervisor::child::Process::start` è l'unica strada, e registra), e `sailor-live --list` / `--stop` sono le due metà che mancavano. Verificato accendendo davvero la modalità viva: il registro nominava finestra e servitore della pagina con pid, porta e chi li aveva accesi, e `--stop` da un'invocazione **separata** — cioè la seconda persona — li ha spenti e ha liberato la 5183. Chi è vivo lo conferma `kill(pid, 0)` su un pid che il deposito ha già scritto, mai un elenco chiesto al sistema: un elenco vuoto è la forma in cui il guasto 12 si nasconde. **Limite dichiarato**: un figlio morto e non raccolto risulta vivo, visto scrivendo la prova; non tocca gli orfani veri, che il processo iniziale raccoglie. — **RIAPERTO il 01/09**, e la prova è un orfano trovato vivo: `./target/release/sailor ui`, pid 77243, genitore `1`, acceso il **31/08 alle 11:52** — cioè *lo stesso giorno* in cui questa riga è stata dichiarata chiusa — e ancora in ascolto su `127.0.0.1:47831` dopo **21 h 51 m**. Il comando `sailor ui` era stato rimosso dai sorgenti il 31/08 e c'è perfino una prova che afferma che non raggiunge più niente: il processo veniva dal binario di prima e nessuno l'ha mai spento. Nel frattempo la tabella `processes` ha **zero righe** e `events.db` **zero eventi di processo** su 614: non è la proiezione indietro, è che non si è registrato nessuno — nemmeno la finestra. **La cura copre solo ciò che passa da `Process::start`, e niente obbliga a passare di lì**: è una cura volontaria, e una cura volontaria vale per chi già si comportava bene. Il controllo che manca non è un altro registro: è che **avviare un processo lungo per una strada diversa non compili**, o che qualcosa confronti periodicamente le porte in ascolto col deposito e dichiari la differenza. Trovato mentre si cercava se la finestra fosse accesa — e lì ho sbagliato allo stesso modo: `launchctl` elencava `sailor-desktop.89222` con «active count = 1» per un pid **morto**, cioè il guasto 12 daccapo, una fotografia scaduta creduta un fatto. Orfano spento il 01/09, porta liberata |
| 5 | 28/08 | Prove che leggono un file di configurazione della macchina di chi le esegue | Diventate rosse dopo una pulizia, a codice invariato | Le prove non leggono lo stato della macchina; ciò che serve si versiona | **chiuso in parte** il 05/09: le prove non leggono più la macchina. `default_registry` era chiamata da ~50 prove e leggeva `HOME`, la casa, il registro dei guasti, le sessioni e `Tools::current()`; ora è l'involucro di `registry::registry_in(House, …)` e le prove passano `House::empty()` o `House::under(scratch)`; `Machine::current()` → `Machine::bare(…)`, `PathLookup::on(…)`, i repository di prova con `GIT_CONFIG_GLOBAL=/dev/null`. Un giudice, `tests_read_no_state_of_this_machine`, conta le porte verso la macchina nel codice di prova (venti forme, commenti e stringhe esclusi): **8 oggi**, solo in discesa — tre nel cancello di pubblicazione, che la macchina la deve leggere, due nella prova di cablaggio della finestra, tre nel guscio della finestra. **Resta** ciò che legge la macchina *a tempo di esecuzione* dentro le prove di `system_flows` (`detect_tools`). *Prima, aperto:*  con un'istanza nuova, curata, e una misura sul controllo che manca. **L'istanza (04/09/2026):** le prove di `sailor session` scrivevano un annuncio nel deposito **vero** di questa macchina, perché la forma cercava il deposito da sé invece di riceverlo; l'ho visto leggendo il censimento della ciurma e trovandoci dentro un dato di prova, `/qui/dentro`, pid 1500. Curato come lo era già il registro delle sessioni: il deposito entra nella richiesta, e `None` vuol dire «qui non serve». **La misura sul controllo:** cercare le porte verso la macchina (`default_directory`, `sailor_home`, `default_path`, `default_sources`, `Machine::current`, `Catalog::load`) dentro le regioni di prova dà 73 ricorrenze su questo albero, ma la maggior parte sono prove *sulla* casa, che quelle funzioni le nominano per forza e con un ambiente dichiarato. Un cricchetto su quel conto sarebbe rumore, cioè un cancello che dichiara più di quanto legge. La domanda vera è empirica — la suite gira con `HOME` in una cartella vuota? — e chiede di cambiare come si lancia la suite, non di contare parole. |
| 6 | 29/08 | Uno script ha stampato «rimossi 1975 file» senza rimuoverne nessuno | Guardando che il peso della cartella non era cambiato | Il messaggio di successo sta dentro il controllo dell'esito, mai accanto | **chiuso** — rifatto |
| 7 | 28/08 | Due passi dichiarati «in parallelo» giravano in fila | Cronometrando: due passi da 6 secondi ne impiegavano 12 | Una prova in cui ogni passo aspetta gli altri del fronte e fallisce se resta solo: non cronometra, quindi non mente su una macchina carica | **chiuso** il 30/08 — misurato: 2 passi da 6s in 6,07s (erano 12,07), 3 in 6,05s (erano 18,14) |
| 8 | 28/08 | Un descrittore con un campo che questa versione non conosce viene scartato intero | Scrivendone uno apposta | Un campo ignoto è un avviso sul campo, non il rifiuto del componente | **chiuso** il 31/08. `Catalog` distingue `problems` (voci **perse**) da `notes` (voci **tenute**, con qualcosa di ignorato): metterle insieme avrebbe fatto contare come guasti degli strumenti vivi. E i campi ignoti sopravvivono a una riscrittura, che era il difetto opposto |
| 9 | 28/08 | Un elemento grafico invisibile: disegnato dello stesso colore dello sfondo | Solo da un'immagine dello schermo. Né i tipi né le prove lo vedevano | Lo schermo come oracolo: un'immagine confrontata, non solo i tipi | **chiuso** — quel caso |
| 10 | 28/08 | La stessa lista di componenti scritta in due punti del programma | Si sono disallineati in un'ora, nello stesso giorno di lavoro | Una sola fonte, e le altre la chiedono invece di ricopiarla | **chiuso in parte** il 05/09: le due istanze trovate senza guardia sono chiuse alla fonte. I verbi di `faults`, `terminal` e `session` si leggono dall'uso stampato (`verbs_of(USAGE)`), non da una seconda lista; i quattro momenti di sessione sono la lista di `toolbox::descriptor::MOMENTS`, e `session_cmd` la chiede. Mutanti: la terza parola spostata, un momento tolto. **Resta** la coppia fra linguaggi: `inventory::Kind` in Rust e `FAMILIES` in `desktop/src/engine.ts`, senza giudice. |
| 11 | 29/08 | In modalità viva la finestra spariva. **La causa scritta qui era sbagliata, e la correzione conta quanto la riparazione**: non è l'errore di compilazione a uccidere la finestra. `cargo tauri dev` la uccide a **ogni** file toccato, *prima* di compilare — `tauri-cli 2.11.4`, `src/interface/rust.rs`, `run_dev_watcher`: `child.kill()`, poi `child.wait()`, poi `run(...)`. L'errore di compilazione è solo il motivo per cui non ne ritorna una. Il difetto non era nella gestione dell'errore: era **nell'ordine** | La finestra è sparita mentre un altro cantiere aveva un crate a metà | La finestra sopravvive a una compilazione fallita, e lo dice invece di chiudersi | **chiuso** il 31/08 — `sailor-live` (`crates/supervisor`) prende il posto di `cargo tauri dev` e rovescia l'ordine: costruisce, e tocca ciò che è acceso **solo se** la costruzione è riuscita. **Visto, non solo scritto**: rompendo `crates/ledger` con la finestra vera aperta, il pid 89610 è rimasto lo stesso, il titolo è diventato «ricostruzione FALLITA: stai guardando l'ultima versione buona» e l'errore del compilatore è arrivato intero; rimessa a posto la riga, la finestra è stata sostituita. Il mutante che rimette l'ordine di `tauri-cli` uccide un processo vero dentro la prova. **Trovato di passaggio, e non riparato**: `get_in_workspace_dependency_paths` segue solo le dipendenze che sono membri dello stesso workspace, e `desktop/src-tauri` ne dichiara uno vuoto apposta — quindi con `cargo tauri dev` una modifica a `crates/` non fa ricostruire niente e la finestra mostra il motore vecchio **senza dirlo**. `sailor-live` guarda anche `crates/` |
| 12 | 29/08 | `pgrep` dentro il perimetro **non vede i processi e risponde vuoto**, senza errore | Una sorveglianza ha detto «nessun flusso in esecuzione» mentre due giravano | Chiedere lo stato al deposito, non al sistema operativo: chi è vivo lo dice il dato, non un comando che il perimetro può zittire | **chiuso** — sorveglianza riscritta |
| 13 | 29/08 | `for x in $lista` in zsh **non spezza la variabile in parole**: l'intera lista entra in un giro solo | La sorveglianza riscritta ha rifatto lo stesso falso allarme del guasto 12, per una causa diversa | Leggere riga per riga (`while read`), mai iterare una variabile non quotata. **Era già scritto in una memoria e non è stato consultato** | **chiuso** — riscritta di nuovo |
| 14 | 29/08 | Un motore esaurito (limite settimanale) è stato registrato come guasto qualunque, e la corsa si è fermata | Leggendo il messaggio del motore: «hai raggiunto il limite settimanale, si azzera alle 7» | Distinguere «finito fino a un'ora nota» da «rotto»: il primo si aspetta o si instrada su un altro profilo, il secondo no. Serve prima che il consumo dichiarato dal motore entri nel deposito | **chiuso in parte** il 31/08 — con mutante. La distinzione c'è ovunque: classe `engine_exhausted` e specie `exhausted` nel deposito, **anche con un motore solo**, che era il caso in cui è capitato. **Resta l'attesa**: nessuno legge «si azzera alle 7» per riprovare a quell'ora, e non si legge apposta — estrarre quell'ora con un'espressione regolare da una frase vista una volta sola sarebbe un dato inventato che ha l'aria di una misura |
| 15 | 29/08 | Per cambiare l'innesco di un flusso è stato usato uno script Python che riscrive il JSON: Sailor non ha nessun comando per operare sui propri flussi | Theo lo ha visto nel terminale e ha chiesto perché la riga di comando non chiami Sailor. Verificato: `sailor flow` ha solo `list`, `due`, `check`, `run` | Ogni cosa che una persona deve fare su un flusso è un comando di Sailor. Finché si aggira con `python3`, il sistema non si usa da sé e nessuno se ne accorge dai suoi controlli | **chiuso in parte** il 01/09 — con due mutanti. **Il gesto dell'incidente adesso esiste**: `sailor flow schedule <nome>` legge l'innesco, `sailor flow schedule <nome> <3600s ~ 07:30 ~ nessuno> [leggero~pesante]` lo mette, lo cambia o lo toglie. Passando da Sailor si prendono tre rifiuti che `python3` non conosceva: un flusso di sistema non si riscrive (nascerebbe un omonimo che nessuno sa di avere), un file che non si chiama come il proprio `id` non si riscrive (nascerebbe un gemello), e il grafo si rivalida alla scrittura. **Il peso non si inventa**: su un flusso che un innesco non ce l'ha ancora, senza la parola il comando rifiuta invece di scegliere «leggero» — un peso comparso da sé si legge come una misura. Il perimetro dichiarato si conserva cambiando l'orario, o un comando che parla di orari allargherebbe un permesso. **La riga della tabella era invecchiata su due punti**, e la misura è del 01/09: `sailor flow` aveva già `due`, `resume`, `cost`, `cap` e `relocate`, e due di quelli — `cap <nome> <valore>` e `relocate` — **scrivono davvero il file**. La frase «nessun comando per operare sui propri flussi» era vera il 29/08 e non il 01/09. *Mutanti*: togliere `save_in` da `set_schedule` (il comando dice «fatto» e il file resta com'era) fa cadere quattro prove; togliere `schedule` dalla riga dell'uso ne fa cadere una, che è la guardia contro un gesto invisibile. **Resta aperto ciò che il resto della colonna chiede**: dalla riga di comando non si può ancora **creare** né **cancellare** un flusso, né toccarne grafo e ingressi — quei gesti li ha solo la finestra (`desktop/src-tauri/src/flows.rs`), e chi lavora al terminale li fa ancora a mano |
| 16 | 29/08 | Il flusso di ricerca non è partito e il lavoro l'ha fatto una persona a mano. Tutti e sei i passi hanno `claude-code` scritto dentro, senza ripiego — mentre `agy` rispondeva | Provando i tre motori uno per uno: claude esaurito, codex 401, **agy vivo**. Nessuno li aveva provati | Un passo dichiara una **catena** di motori, non uno. E chi lancia vede su quale è finito | **chiuso** — e la tabella lo raccontava male fino al 01/09/2026. La prima metà era fatta da giorni: i cinque passi a motore di `come-lo-risolvono-gli-altri.flow.json` dichiarano una catena di tre, non un nome, e la seconda metà pure — il deposito scrive `cli`, cioè chi ha risposto, e `retry_chain`, cioè chi era stato provato prima. **Era una chiusura vuota, e a dirlo è stato il guasto 31**: la catena c'era e il ripiego non scattava, perché `agy` stava in mezzo senza dichiarare come si esaurisce. Chiuso quando il 31 lo è stato, con la stessa guardia |
| 17 | 29/08 | Due passi di quel flusso chiedono competenze di Claude Code (`advise-project-approach`, `neuroarxiv`) che stanno in `~/.claude/skills` di una sola persona: il flusso non è portabile e non lo dichiara | Cercando perché non bastasse cambiare motore. `flow check` lo dà per buono: controlla gli strumenti per identificativo, **le competenze no** | Ciò che un passo chiede va dichiarato tutto, e il controllo statico lo verifica — è il guasto 3 su un'altra dimensione. Chi non ha quella capacità deve funzionare peggio, non in silenzio | **chiuso** il 05/09: un passo dichiara le competenze che chiede in `needs_extensions` (`kind:name`, i generi sono quelli dell'inventario), `sailor flow check` le cerca dove il motore le legge e dice per ciascuna presente, assente, irraggiungibile o incerta; il testo di un passo che nomina `skills/<nome>` o `/<nome>` senza dichiararli è segnalato. Un giudice tiene il conto. *Prima, aperto* dal 29/08. |
| 18 | 29/08 | La casa di Sailor (`~/.config/sailor`) contiene **un solo file**, una firma. Ogni server MCP e ogni gancio che Sailor conosce li scopre leggendo la configurazione di Claude Code, Claude Desktop o Cursor: la sua dotazione è quella dei vicini | Cercando dove fossero installate le competenze del guasto 17. Non c'era nessun posto dove installarle | Una dotazione propria sotto la casa di Sailor, che viaggia col prodotto. Il rilevatore torna a dire cosa c'è sulla macchina — che è un'altra domanda | **chiuso in parte** 01/09 — la dotazione ora **arriva ai motori**: `actions` prende `profiles`, e `ExternalEngineAction` sovrappone l'ambiente del profilo attivo (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `GEMINI_CLI_HOME`) **sotto** `spec.env`. Fino a ieri quella sovrapposizione la chiamava un posto solo, `sailor run`: un motore lanciato da un passo di flusso ereditava l'ambiente di chi aveva aperto il terminale. Il profilo risolto finisce nella riga del deposito, dove si scriveva sempre vuoto. *Mutante*: rimettere `env: spec.env.clone()` — il motore vero riparte con `CODEX_HOME` vuota e la prova diventa rossa. **Resta aperto ciò che la cura chiedeva per primo**: `~/.config/sailor/equipment/` esiste su questa macchina — 1,8 MB, `tastemaker` e ventidue competenze fra cui le due del guasto 17 — ma **non viaggia col prodotto**: non è versionata e nessun codice la legge. **Misurato il 04/09/2026, e va saputo prima di ricostruirla:** due flussi la usano già, `decidi-la-direzione` e `costruisci-una-pagina`, con il percorso assoluto scritto in una variabile in cima a ciascun passo — e `sailor flow check` **lo vede e lo dice** («percorsi assoluti dentro un testo: il flusso gira, l'istruzione no»), quindi il segnale c'è già e manca solo dove farlo puntare. **E la parola è occupata:** in `crates/actions` `Equipment` è la dotazione *di credenziali* di un motore — ambiente, identità, rifiuto — quindi la cartella delle competenze ha bisogno di un nome suo, o nasce il guasto 10 in un posto nuovo. |
| 19 | 30/08 | La casa di Sailor era **in due posti**: chi cercava deposito e listino risolveva `~/.config/sailor`, chi cercava i descrittori dell'utente `~/.sailor`, e la documentazione spedita mandava tutti nella seconda | Scrivendoci dentro il listino dei prezzi seguendo la documentazione, e vedendo che nessuno lo leggeva | Una funzione sola che dice dove sta la casa, e i chiamanti che la chiedono invece di riscriverla. Le due copie sbagliavano **insieme**, quindi si confermavano a vicenda | **chiuso** — con mutante |
| 20 | 30/08 | Un `with` che nomina un campo inesistente viene **ignorato in silenzio**: il passo parte, il motore riceve una riga di comando monca, e l'errore che arriva è suo | Scrivendo `prompt` invece di `stdin` in un flusso di prova. Il messaggio era «Input must be provided either through stdin» — di Claude, non di Sailor | Un campo sconosciuto in un `with` è un avviso su quel campo, non silenzio: è il guasto 8 visto dall'altro lato, e `flow check` è il posto dove dirlo prima di spendere | **chiuso** il 31/08 — con tre mutanti. `flow check` nomina passo e campo. La strada ovvia (`deny_unknown_fields`) era **sbagliata e l'avrebbe peggiorato**: a esecuzione l'ingresso di un passo *è* l'uscita della dipendenza, quindi ogni passo con una dipendenza sarebbe morto con «unknown field `status`» — il mutante lo mostra |
| 21 | 30/08 | Le prove di `profiles` si rubavano la cartella temporanea a vicenda: **una esecuzione su venti falliva**, ogni volta su una prova diversa | Eseguendo la batteria molte volte di fila. Misurato: 1 su 20 prima, 0 su 50 dopo | Un contatore che cresce nel nome, invece del solo orologio: `cargo test` manda le prove sullo stesso processo e l'orologio di macOS non ha la risoluzione del nanosecondo | **chiuso** — misurato prima e dopo |
| 22 | 31/08 | Il costo totale di una corsa era la **costante `0`**, scritta a mano in due punti, mentre le sue chiamate portavano il costo giusto una per una. La finestra mostrava lo zero accanto alla somma vera | Cercando su quale numero appoggiare un tetto di spesa. Era già registrato in `docs/da-fare.md` dal 29/08 — «costo totale sulle 16 corse: 0 su tutte» — e nessuno l'aveva letto come un guasto | Un totale non si dichiara: si chiede a chi tiene le righe. E le due copie di quella scrittura diventano una, nel crate che le due strade condividono già | **chiuso** — con mutante |
| 23 | 31/08 | **136 identificatori in italiano** — variabili, moduli, costanti, due file di prova, classi CSS — contro una regola scritta in `AGENTS.md` dal 28/08. Quasi tutti nati dopo di essa | Theo, leggendo il codice: «inaccettabile vedere ancora funzioni, variabili o codice, nomi di file scritti in italiano». Nessun controllo li aveva mai visti | La regola va dove si rilegge (`docs/decisioni.md`) **e** ha un controllo che la rende rossa. Serve anche che sia completa: quella vecchia elencava «funzione, tipi, campi, opzioni» e i nomi di file non c'erano — chi scriveva `smista_il_lavoro.rs` non trovava il proprio caso, e la direttiva di sessione «rispondi in italiano» vinceva | **chiuso** — il controllo è nato rosso su 64 e ora è verde |
| 24 | 31/08 | Un deposito **già esistente** non veniva migrato: la migrazione aveva imparato quattro colonne nuove ma `PROJECTION_SCHEMA_VERSION` era rimasta a 4, quindi `4 < 4` era falso e ogni lettura moriva con «no such column» | Usando Sailor: il primo comando che legge il consumo di una corsa (`sailor flow cost`) è morto sul deposito vero di questa macchina. **517 prove verdi non l'avevano visto** | Il numero di versione si alza **insieme** alle colonne, e serve una prova che apra un deposito che si dichiara già aggiornato e non lo è — un deposito creato in una prova nasce completo e non passa mai dalla migrazione | **chiuso** — con mutante che riproduce l'errore parola per parola |
| 25 | 31/08 | Il flusso `sviluppa-sailor` ha un `"workdir"` assoluto — la casa di chi lo scrisse — **in chiaro su ogni passo**: lanciato da un clone lavora — e commette — nel repository principale, senza dire niente | Eseguendolo da un clone per l'A/B fra flusso e prompt, e trovando il repository principale sporco. L'isolamento dichiarato nel disegno dell'esperimento non c'è stato | Un flusso non deve sapere dove sta il repository: la radice viene da chi lancia, e un percorso assoluto in un flusso è un flusso che si può eseguire in un posto solo. Riguarda anche il guasto 17: ciò che un passo dà per scontato va dichiarato, e `flow check` è il posto per dirlo | **chiuso** il 31/08 — con quattro mutanti. La radice viene da chi lancia (`workspace.root` nello stato condiviso, che arriva a **ogni** azione per costruzione — un rinvio sarebbe passato da `resolve_references`, che il guasto 28 misura chiamata da due azioni su nove) e i percorsi si risolvono in `step_input`, **dove l'ingresso si compone**, così ogni azione futura la eredita. `flow check` distingue un campo di posizione — errore, il flusso gira in un posto solo — da un percorso dentro un testo, che è un avviso perché il flusso gira e a sbagliare è l'istruzione. **La prova che l'ha misurato è nata rossa con 7 errori e 2 avvisi** sul flusso vero, ed è verde perché la migrazione è avvenuta, non perché è stata scritta dopo. I sette `workdir` li ha tolti `sailor flow relocate`, non una mano: è anche un morso al guasto 15 |
| 26 | 31/08 | Una prova che dichiarava di provare la crescita dell'attesa **passava col difetto rimesso**: chiedeva una sequenza «non decrescente, mai sopra il tetto, e ferma sul tetto», e `[50, 50, 50…]` — l'attesa fissa di prima — soddisfa tutte e tre | Un motore che non aveva scritto né la prova né il codice, leggendo due riparazioni **senza sapere quale fosse quale**. L'autore non l'aveva visto; il verificatore nemmeno, e aveva fatto le mutazioni davvero | **Rompere una prova non basta: va rotta nel punto dov'era il difetto originale.** Il verificatore aveva mutato *il tetto* (togliendolo) e visto rosso; nessuno aveva mutato *la crescita* rimettendo l'attesa fissa, che era il difetto vero | **chiuso** — la prova ora chiede una salita *prima* del tetto, e con l'attesa fissa rimessa diventa rossa. Nata da lì una terza prova che interroga la regola di crescita da sola, cosa che un commento prometteva e nessuno faceva |
| 27 | 31/08 | **Il guasto 1 è tornato da un'altra porta.** Gli argomenti del blocco `usage` di un descrittore si accodavano **dopo** quelli di `ask`: per `agy` la riga diventava `--mode plan --print --output-format json «prompt»`, e `--print` prendeva `--output-format` come propria domanda. Il prompt veniva ignorato. **Il secondo motore di ogni catena non ha mai funzionato** | Provando la riga a mano: `agy` stampa il difetto da solo — «Error: --print took "--output-format" as its prompt, so the intended prompt was left as an argument and ignored». Nessun flusso mette `agy` per primo, quindi su questa macchina quel ramo non era mai stato eseguito | I due blocchi erano giusti separatamente e sbagliati **insieme**: una prova per blocco non può vederlo. Un ordine globale non basta — `codex` ha `exec` come sottocomando e vuole stare primo, `agy` vuole `--print` ultimo: due vincoli opposti sullo stesso posto, quindi li dichiara il descrittore (`ask.args_before_prompt`). Resta scoperta la cura scritta accanto al guasto 1: **eseguire davvero** ogni riga composta, per **ogni combinazione** di blocchi che la compongono e non per la sola forma nominale. E resta che una catena di motori dovrebbe **provare il successivo** quando il primo muore per una riga malformata, invece di fermare il passo | **chiuso** — con mutante che mostra la riga sbagliata parola per parola |
| 28 | 31/08 | **Solo due azioni su nove risolvono i rinvii, e il deposito non è fra quelle.** `reference::resolve_references` è chiamato in due punti soli di tutto l'albero — dentro `external_engine` e `shell_check` (`crates/actions/src/lib.rs:1613` e `:1760`). `store_write`, `store_read`, `store_list` e `history_ask` non lo chiamano mai: un `{"$from": "/x"}` arriva loro come oggetto, e `serde_json::from_value` muore perché `key` vuole una stringa. **Conseguenza: il deposito non può ricevere niente da un passo precedente**, cioè non può fare il testimone che `docs/decisioni.md` gli attribuisce — accetta solo valori scritti a mano nel flusso | Con due fixture a costo zero, identiche tranne una cosa: `flows/prova-deposito.flow.json` (chiave presa con un rinvio) **fallisce**, `prova-deposito-letterale.flow.json` (stessa chiave scritta a mano) **passa**. Prima di quelle, la corsa che l'ha rivelato aveva speso 8,95 $ per morire sull'ultimo passo, buttando una diagnosi che tutti e cinque i motori avevano già prodotto | La risoluzione dei rinvii non appartiene alla singola azione: è come i passi si passano le informazioni, e va fatta **una volta sola dove l'ingresso si compone**, così ogni azione registrata — comprese quelle che nessuno ha ancora scritto — la eredita. Finché sta dentro le azioni, ogni azione nuova nasce senza, e nessun controllo lo dice. Serve anche che `flow check` veda un rinvio dentro il `with` di un'azione che non lo risolve: è il guasto 20 su un'altra dimensione | **chiuso in parte** il 01/09 — con quattro mutanti. **Ma prima la misura, perché la riga qui accanto era invecchiata e la cura che nel frattempo era stata applicata era quella sbagliata**: al 01/09 le azioni registrate erano **sedici**, e `resolve_references` era chiamata da **dodici**. Quattro no — `history_ask`, `detect_tools`, `trigger`, `subflow` — e nessun controllo diceva quali. Il sintomo era stato ridotto ricopiando la riga dodici volte, cioè il guasto 10 in dodici esemplari: ogni azione nuova continuava a nascere senza, e la scelta di averla o no restava della singola azione. Adesso la risoluzione sta in `flow::step_input`, dove l'ingresso si compone, ed è **l'unico punto attraversato da ogni passo di ogni corsa**. **L'ordine dentro quel punto è costato un giro di revisione e va scritto**: comporre, risolvere il `workdir`, valutare il `when`, e sciogliere i rinvii **solo se il passo gira**. La prima stesura scioglieva prima della condizione, per far risolvere anche un `workdir` preso dal passo prima; il prezzo era `flows/chiedi-all-indice.flow.json`, passo `leggi` — `when` su `/status`, `with` pieno di `$from` verso l'uscita di `chiedi`, e `chiedi` fra le dipendenze **saltabili**. Quando l'indice non risponde, cioè il caso che quel flusso dichiara essere il più frequente, `chiedi` viene saltato e `leggi` riceve `{}` più il proprio `with`: coi rinvii sciolti prima della condizione il passo **si rompeva invece di essere saltato**, e il flusso passava da «completato» a «terminato con stato failed». Non si vedeva perché `verdetto`, nello stesso fronte, si rompe per primo — cioè per un incidente, non per una proprietà. **Un passo che non gira non paga i rinvii di un lavoro che non farà**, e il `when` continua a valutarsi sull'ingresso non ancora sciolto, come prima del 01/09. Il limite che ne discende è dichiarato accanto al codice: un `workdir` scritto come `{"$from": …}` non viene attaccato alla radice — era già così e non peggiora. Le dodici copie non ci sono più; il modulo si è spostato da `crates/actions` a `crates/flow`, che è il crate a cui la semantica appartiene. **Tre guardie, perché una sola non copre il guasto**: `crates/flow/tests/a_reference_reaches_every_action.rs` fa girare un'azione che **non sa cosa sia un rinvio** e la vede ricevere il valore sciolto, cioè parla anche delle azioni che nessuno ha ancora scritto; `crates/registry/tests/the_store_can_witness_between_two_steps.rs` rifà il sintomo col registro vero, il deposito vero e SQLite, ed è la fixture `prova-deposito.flow.json` che dall'albero era sparita; `crates/sailor/tests/references_are_resolved_in_one_place.rs` **conta i posti** invece del comportamento, che è la sola cosa che vede tornare le copie — una prova di comportamento resterebbe verde con la riga rimessa dentro un'azione. *Mutanti*: rimettere il difetto originale (togliere la chiamata da `step_input`) rende rosse tutte e tre le prove dell'esecutore e riproduce nel deposito vero l'errore del 31/08 **parola per parola**, «invalid type: map, expected a string»; sciogliere i rinvii **prima** del `when` fa cadere la prova del passo saltato con `Failed(["leggi"])`, che è il difetto di cui sopra; rimettere una copia dentro `store_write` rende rossa la guardia che conta i posti; togliere la chiamata rende rossa la sua metà gemella, che pretende che il posto dichiarato sia davvero occupato. **PERCHÉ RESTA APERTO IN PARTE, ED È UNA CORREZIONE DI ROTTA.** Il 01/09 questa riga aveva dichiarato **chiuso**, sostenendo che la seconda metà della colonna qui accanto non avesse più oggetto: alla lettera è vero — «un rinvio dentro il `with` di un'azione che non lo risolve» non nomina più nessun caso, perché non c'è più nessuna azione che non lo risolva. Ma l'**intento** di quella metà era prendere il puntatore morto **prima di spendere**, e quell'intento non è senza oggetto: è diventato più urgente, perché adesso un puntatore morto non spreca una chiamata, rompe il passo. `flow check` continua a non guardare i rinvii. La lettera di una cura non è la cura, e la direzione dell'errore era quella che tranquillizza |
| 29 | 31/08 | Il numero grande dei token sulla plancia era **ingresso + uscita** e si chiamava «Token»: 89.402 contro 5.687.230 davvero consumati, **63,6 volte meno**. La cache scritta — la voce a 10 $/M, il 44,8% del costo di una corsa misurata — non compariva da nessuna parte, mentre il commento del suo campo in `dashboard.rs:94` la dichiarava «tenuta visibile» | Guardando la pagina, e poi ricontando dal deposito con `sqlite3`. Il file stesso porta scritta la regola che violava: «un totale parziale che dichiara di essere parziale è una misura; uno che tace si presenta come completo, ed è la bugia da cui il conteggio dei consumi è nato» | Il totale somma **tutte e quattro** le voci, e le quattro restano separate accanto perché hanno prezzi diversi fino a venti volte. Il costo era già nel dato servito (`cost_micros`) e nessuno lo mostrava: ora ha il suo riquadro. E chi legge decide se può lanciare un'altra corsa: lo decideva sul falso | **chiuso** — verificato sullo schermo prima e dopo, e la prima correzione **sbordava dalla cella**: il numero giusto reso illeggibile vale quanto quello sbagliato |
| 30 | 31/08 | **La tela diceva «in attesa» su ogni nodo di ogni flusso vero, anche mentre il motore lavorava.** Non nascondeva: raccontava il falso. `App.tsx:527-530` passava al disegno una mappa di stati che è quella d'esempio solo fuori dal guscio nativo, e `new Map()` dentro — mentre `executions`, i fatti veri che arrivano per evento ogni secondo, non era dipendenza né del disegno né della tela | Una lettura del codice partita dalla domanda «cosa vede l'utente finale», e poi guardando la finestra vera aperta sui dodici flussi dal disco | È la violazione più netta possibile del vincolo permanente «un'interfaccia che nasconde cosa succede è il contrario del prodotto», e nessun tipo poteva vederla: la tela compilava. Serve che i fatti arrivino ai nodi **da un contesto** e non dai loro `data` — i `data` fanno parte dell'elenco dei nodi, e un elenco ricostruito a ogni fatto ha già mandato questa tela in ciclo infinito una volta | **chiuso** — con cinque mutanti sul calcolo e due sul collegamento, fra cui il difetto originale rimesso. E la finestra ha le **prime prove della sua vita**: `npm test`, 12 verdi, ora dentro `npm run build` |
| 31 | 31/08 | **Una catena di motori senza ripiego.** `agy` non dichiara nessun `unusable_when`, e `says_it_cannot_work` su un elenco vuoto è `false`: il suo esaurirsi passa per un fallimento qualunque, il passo muore lì, e nella catena `claude-code → agy → codex` **`codex` non parte mai**. È il guasto 16 un piano più sotto — allora mancava la catena, adesso c'è e il ripiego no — e vale su tutti e tre i flussi dell'albero che nominano una catena | Leggendo il descrittore spedito, e poi rendendolo un fatto a costo zero: due finti motori identici tranne l'elenco delle parole. Elenco popolato, il secondo parte; elenco vuoto, il passo muore e il secondo non parte (`an_engine_that_declares_no_exhaustion_words_kills_the_chain`, in coppia con `an_engine_that_says_it_is_out_hands_the_work_to_the_next_one`) | La regola scritta: **ogni motore che compare in una catena e non è l'ultimo dichiara almeno una parola di `unusable_when`** — `crates/sailor/tests/a_chain_can_fall_back.rs`, `every_engine_that_is_not_last_in_a_chain_says_how_it_is_exhausted`, che dal 01/09/2026 **gira** invece di essere `#[ignore]`, e la regola che interroga sta in `toolbox::Descriptor::cannot_be_a_fallback`, dove la legge anche `sailor flow check` sui flussi e sui descrittori di chi lo lancia | **chiuso** — 01/09/2026, **due volte, e la seconda è quella che vale**. La regola ha due modi di essere rispettata: misurare le parole di chi sta in mezzo, **oppure non mettere in mezzo chi non le ha**. La prima chiusura ha preso la seconda strada — `agy` spostato in fondo a tutte e dodici le posizioni — e un giudice l'ha approvata come cura, misurandone il prezzo: `codex`, provato prima, è un processo in più su una catena in cui non è autenticato. **Theo ha deciso che la scorciatoia non resta.** Lo stesso giorno `agy` è stato misurato come il 01/09 era stato misurato `codex`: `HOME` su una cartella vuota e la riga che Sailor monta davvero (`agy --mode plan --output-format json --print "…"`), che senza credenziali non chiama nessun fornitore e non costa niente. Esce **1** e dice «authentication failed or timed out» su stdout, «Authentication required. Please visit the URL to log in:» e «Error: authentication timed out.» su stderr — due volte su due, identiche. Quelle tre frasi sono adesso il suo `unusable_when`, `agy` è tornato **in mezzo**, e la regola non ha più nessuna eccezione. Mutanti: svuotato `unusable_when` con `agy` in mezzo, la guardia torna rossa nominando le dodici posizioni; ridotto a `"authentication"`, diventa rossa la prova che difende dall'eccesso opposto — una parola larga fa scivolare la catena su una risposta **riuscita** che parla di autenticazione. **Resta non misurato**, ed è scritto nel descrittore, come `agy` dichiari di aver finito la **quota**: cercato nell'aiuto, nei sottocomandi annidati e nelle stringhe del binario, dove l'esaurimento compare solo come motivo di ritentativo interno |
| 32 | 31/08 | **La stessa verità in due blocchi, di cui uno inservibile.** `gemini-cli` dichiara `capabilities.ask_without_interaction` e **non ha blocco `ask`**: la riga per interrogarlo non si compone, nessuna catena lo nomina, e in 23 chiamate registrate non è mai stato invocato. Una capacità dichiarata che non serve a niente somiglia in tutto a una che c'è | Confrontando i due blocchi dello stesso file, cosa che nessun controllo faceva: `ask` e `capabilities` rispondono alla stessa domanda e potevano dire cose diverse senza che niente diventasse rosso | Non tre riparazioni a mano ma **una funzione di libreria**, `toolbox::Descriptor::contradictions`, che confronta i due blocchi in tutte e due le direzioni, con le opzioni, e nega i frammenti vuoti. La interrogano `no_shipped_descriptor_contradicts_itself` sui descrittori spediti e `sailor flow check` su quelli scritti in `~/.config/sailor/tools.d/` — che è dove una prova sui soli descrittori spediti non arriva, ed è il posto previsto per aggiungere un motore senza ricompilare | **chiuso** — 01/09/2026. `gemini-cli` ha adesso il suo blocco `ask`, **misurato e non indovinato**: `--prompt` con la domanda in un argomento, e `refuses_without_prompt` preso dalla riga vera montata senza la domanda (`gemini --prompt` esce 1 e dice «Not enough arguments following: prompt»), che non costa niente e non chiama nessun fornitore. **La ragione per cui era `#[ignore]` è caduta con la guardia stessa**: si temeva che un `ask` senza `unusable_when` facesse entrare gemini in una catena senza ripiego, cioè un quarto guasto 31 — e adesso la regola del 31 lo impedisce, quindi scrivere l'`ask` è diventato sicuro. Restano fuori, dichiarati nella nota del descrittore, il `usage` e le parole di esaurimento di gemini: non misurati, quindi non scritti. Mutante: tolto il blocco `ask`, la prova torna rossa nominando `gemini-cli` |
| 33 | 31/08 | **Il deposito non sa distinguere «uscita nulla» da «nessuna uscita».** `StepRecord::output` è un `Option<Value>`: `Some(Value::Null)` si serializza nel registro degli eventi come `"output": null`, identico a `None`, e rileggendo torna `None`. Un passo chiuso `Went` con uscita nulla risulta senza uscita, e il passo che ne dipende muore con «non ha uscita tipata» — tre passi dopo, su un passo innocente | Alla **prima corsa vera** del passo consegnato: `sailor step close --outcome went` senza `--output-file`, poi `sailor flow resume`, che è morto con «step implementa has no typed output». Nessuna delle 520 prove lo vedeva, perché nessun'altra strada produce `Some(Value::Null)`: `run_one` scrive l'uscita che l'azione dichiara, e nessuna azione registrata dichiara `null` | La distinzione va **rappresentata**, non dedotta: finché due valori diversi si scrivono uguali, nessun controllo a valle può separarli. Nel frattempo il difetto non deve poter nascere — chi chiude un passo a mano viene fermato **subito** se qualcuno a valle pretende quell'uscita, invece di scoprirlo dove il difetto non è. È la stessa regola dell'uscita validata contro lo schema: un difetto che si manifesta lontano da dove nasce costa il doppio | **chiuso** — con due mutanti. Restava aperto il formato, ed è quello che è stato riparato: `StepRecord` porta `output_was_written`, e il registro degli eventi ha una porta sola in lettura che rimette `Some(Null)` dove il JSON ha detto soltanto `null`. La distinzione è **rappresentata** e non più dedotta: `"output": null` è esattamente ciò che scrive «nessuna uscita», e nessun controllo a valle poteva separarle. Un record scritto prima di questo campo lo legge falso, che è come quei record sono sempre stati letti. La proiezione le distingueva già da sé — il testo «null» nella colonna contro un NULL di SQL — quindi le prove ricostruiscono la proiezione **dal registro**, o passerebbero leggendo il deposito mentre la sorgente di verità continua a perdere il dato. Mutanti: togliere la riparazione in lettura (cade il braccio dell'uscita nulla) e scrivere il campo sempre vero (cade quello del passo senza uscita). *05/09: la porta in lettura è stata tolta — `output` è un campo solo con il suo serializzatore che scrive e legge le due chiavi, e nessuna riparazione a valle; cinque prove con mutante.* |
| 34 | 31/08 | **`cargo` e `git` finivano in `model_calls`**, la tabella delle chiamate ai modelli: `record_the_call` scriveva una riga per qualunque strumento chiesto per identificativo. Nessuno dei due consuma quota, e tutti e due arrivano senza costo — quindi `Spend::is_complete()` era **falso su ogni corsa vera**, e la frase d'onestà del tetto («la spesa vera è più alta») si accendeva sempre. Un avviso sempre acceso non lo legge nessuno, e a perdersi è quello vero: la riga di codex, che il costo davvero non lo dichiara | Contando le righe del deposito per flusso mentre si cercava su quali corse tarare un tetto: su ventiquattro righe, due `git` e una `cargo`. La corsa `sviluppa-sailor` ne aveva sette, tre senza costo, e tutte e tre erano queste | **Chi è un motore lo dice il descrittore, non un elenco di nomi nel codice**: uno strumento è un motore se dichiara come gli si fa una domanda (`ask`). Un elenco a mano invecchierebbe al primo strumento nuovo e nessun controllo lo direbbe — è il guasto 3 dall'altro lato. Serve anche la prova gemella: un motore vero interrogato con le opzioni scritte dal passo **resta** nel conto, o il filtro potrebbe svuotare la tabella e sembrare giusto | **chiuso** — la prova nasce rossa sulla riga vera, e la gemella tiene fermo che i motori restino |
| 35 | 31/08 | **Il listino dei prezzi non è versionato, e senza di lui nessun tetto scatta mai — in silenzio.** `load_pricing()` legge `~/.config/sailor/pricing.json`; in git c'è solo `crates/models/pricing.example.json`. Su una macchina dove quel file manca ogni `cost_micros` è `None`, la spesa di ogni corsa resta zero, e un flusso con `spend_cap_micros` gira fino in fondo senza nessun errore e senza nessun avviso | Leggendo `load_pricing` mentre si costruiva il tetto: il suo stesso commento dichiara che «un listino assente non è un guasto: lascia il costo sconosciuto», e quella frase era vera prima che esistesse un tetto appoggiato su quel numero | **Il listino va versionato, non il tetto ammorbidito.** La cura sbagliata e ovvia sarebbe far fallire una corsa senza listino: punirebbe chi non c'entra e non direbbe comunque quanto costa. La cura giusta è che il listino spedito col prodotto sia quello vero — come i descrittori degli strumenti e i flussi di sistema, incorporati nel binario — e che il file in casa lo **sovrascriva** invece di essere l'unica copia. Serve anche che `flow check` dica «tetto dichiarato, ma su questa macchina nessun listino: il tetto non scatterà», perché un freno che non frena si deve vedere prima di lanciare | **chiuso** 01/09 — `crates/models/pricing.default.json` è incorporato con `include_str!` come i descrittori, e il file di casa lo sovrascrive **per `id`**, la voce intera. Due valute non si mescolano: un listino di casa in un'altra valuta vale da solo, invece di tirarsi dietro voci che finirebbero sommate nella stessa colonna. La frase che il tetto voleva è cambiata di forma perché un listino c'è sempre: `flow check` e `flow cost` nominano ora i **modelli** che il listino non sa prezzare, col perché (voce assente / voce senza prezzi), e con un tetto dichiarato aggiungono che quelle chiamate il tetto non le conterà. *Mutante*: svuotare `models` in `pricing.default.json` — sei prove rosse in tre crate, compresa la catena intera del descrittore spedito |
| 36 | 31/08 | **Nove agenti in parallelo: quattro conflitti che git segnala e quattro che non vede.** Quelli segnalati erano tutti banali — due moduli sulla stessa riga, due funzioni nello stesso punto, l'elenco dei membri del workspace. Gli altri quattro no: *(a)* `spend-cap` costruisce un `ModelCallRecord`, `session-resume` gli aggiunge un campo — zero righe in comune, albero che non compila; *(b)* `workspace-root` scrive il controllo sui percorsi in chiaro, e il file che lo viola sta su un altro ramo; *(c)* due rami sono **avanzati mentre li fondevo**, e uno dei commit nuovi era proprio l'anello che mancava; *(d)* `subflow` e `workspace-root` aggiungono ciascuno una `[dev-dependencies]` a `crates/registry/Cargo.toml`, in due punti diversi: git le fonde, cargo rifiuta l'intero workspace con «duplicate key» | Fondendo gli otto rami uno per uno in un albero usa-e-getta e lanciando la batteria dopo ognuno. Nessuno dei quattro era visibile dal ramo che lo conteneva, e ognuno dei nove agenti aveva la propria batteria verde | **Il confine sui file non basta, perché i legami passano da un tipo, da un file di dati e da un manifesto** — è il 33 di questa tabella su un'altra dimensione: là era la numerazione condivisa, qui sono le strutture condivise. Le tre difese che mancano: la batteria si lancia **sull'albero fuso**, non sul ramo; prima di fondere si ricontrolla che il ramo non sia avanzato, perché un agente vivo committa mentre fondi; e un manifesto TOML non ha nessuna prova che lo guardi. Ma la difesa vera è **che gli agenti si vedano**, ed è `work/coordination` — scritto lo stesso giorno, e ancora fuori servizio mentre questo guasto accadeva | **chiuso** — l'albero fuso è verde, e la difesa del manifesto è scritta dentro il manifesto |
| 37 | 31/08 | **`sailor flow cost` mente di 4,3 volte su una corsa con passi consegnati, e non lo dice abbastanza forte.** Un passo `handed_to_agent` non viene misurato: il consumo lo **dichiara l'agente** con `--turns`, e l'agente non sa contare ciò che il proprio harness consuma per lui. Misurato sull'A/B del 31/08: la corsa ha dichiarato **1,6674 $**, ne è costati **7,2080**; i turni dichiarati erano 33 su **75** veri, cioè il **44%**. Il comando stampa «parziale: 3 chiamate senza costo noto», che è vero e non basta: chi legge un totale legge il numero, non la nota | Rifacendo l'A/B del passo consegnato e sommando a mano il consumo della sessione viva (`claude -p --output-format json`) con quello che il flusso ha avviato da sé. Nessuna prova lo vedeva: tutte usano depositi dove ogni chiamata ha un costo | Un totale che contiene una chiamata senza costo **non è un totale**: o si mostra come intervallo («almeno X»), o si rifiuta di sommare e si elencano le voci. E finché il consumo di un passo consegnato è autodichiarato, `flow cost` deve dirlo **al posto del numero**, non accanto. La cura vera è misurare invece di credere: leggere il consumo della sessione che ha eseguito, non chiederglielo | **chiuso in parte** il 01/09 — con mutante. **La bugia è chiusa**: `Spend` distingueva i tre casi dal giorno in cui è nato e la distinzione non arrivava a chi legge, perché l'unico modo di interrogarla era un booleano che chi stampa poteva scrivere *accanto* al numero. Adesso `Spend::reading()` restituisce il caso, e `flow cost` non ha più dove mettere la cifra secca: sui numeri veri di quella corsa dice «almeno 1,6674, e il vero è più alto — 3 chiamate su 4 non sono misurate», e senza nemmeno una misura dice «sconosciuto» invece di «almeno 0,0000», che sarebbe vero e si leggerebbe come una spesa piccola. Il mutante che conta — `is_complete()` sempre vero, cioè il difetto originale — rimette la cifra secca e fa cadere quattro prove. **Resta aperta la misura**, ed è la metà che vale: un passo consegnato continua a dichiarare i propri turni con `--turns`, e la dichiarazione è ora marcata come tale invece di essere confusa con un conto. Aperto il primo canale che legge invece di chiedere — `models::remaining` legge le finestre di quota dal canale OAuth di Claude Code, HTTP 200 misurato il 01/09, solo lettura e nessun costo — ma **è quota della persona, non consumo di una corsa**, e le due non si sostituiscono: leggere quanto è costata *quella sessione* vuol dire leggerne il diario, che nessuno fa ancora *05/09, la seconda metà della bugia:* il rapporto nomina i passi che non ha potuto misurare e perché (consegnato a un agente e autodichiarato; modello fuori listino; nessun costo dichiarato), i turni autodichiarati sono detti tali nel conteggio, e un passo consegnato non compare più come «modello non dichiarato, nessuna voce nel listino». Sette mutanti. Resta aperta la misura vera del passo consegnato. |
| 38 | 31/08 | **Il grafo delle dipendenze di SocratiCode dà falsi orfani su Rust, e noi ci abbiamo appoggiato una decisione.** Misurato oggi su questo repo: **58 archi per 101 file** (0,6 dipendenze per file), e fra i «file orfani, nessuno dipende da questo» c'è `crates/actions/src/lib.rs`, che è il file più centrale del sistema. Non legge `mod` e `use` di Rust. Il prompt del passo `piano` avvisa già di verificare aprendo il file, ma l'avviso sta in un prompt: chi interroga il grafo da fuori non lo legge | Interrogando `codebase_graph_stats` sul repo mentre si mettevano in ordine le voci aperte, e riconoscendo un nome che non poteva essere orfano | Uno strumento esterno si misura prima di appoggiarci una decisione, e la misura si scrive dove la leggerà chi lo usa — non dentro il prompt di un passo. Il canarino è il sopravvissuto noto che sparisce: qui è `actions/src/lib.rs`, e chiedere il grafo su di lui dice in un colpo se lo strumento è affidabile. **La ricerca semantica dello stesso strumento invece regge** e va usata: 1466 pezzi, sorveglianza attiva | **aperto** |
| 39 | 01/09 | **Il vaglio a secco provava la riga in un mondo diverso da quello in cui gira.** Chiusa la strada del guasto 18, un passo fa partire il motore nella casa del profilo attivo; `RealDryProbe` — quello che `sailor flow check` usa per dire «riga di comando sana» — passava `env: BTreeMap::new()`, cioè ereditava la casa di chi aveva aperto il terminale. Le due strade divergono proprio dove fa più male: il controllo prova un motore **autenticato**, la corsa parte in una casa che può non avere nessuna credenziale. Il verde chiude, la corsa fallisce, e chi ha letto il verde non ha sbagliato niente. Non è un caso di scuola: lo stesso giorno i due profili `codex` dichiarati su questa macchina puntavano tutti e due a cartelle **senza `auth.json`** | Guardando cosa cambiava per il vaglio dopo aver fatto arrivare la dotazione ai motori — cioè cercando l'effetto collaterale della riparazione, non un difetto. Nessuna prova lo vedeva: quelle del vaglio iniettano un finto eseguibile e non guardano mai l'ambiente | **Un controllo che prova un mondo diverso da quello in cui si lavora è peggio di nessun controllo, perché rassicura.** La cura NON è un controllo nuovo sulle credenziali: sarebbe una seconda copia della verità, un «esiste `auth.json`?» da riscrivere per ogni motore. I descrittori dichiarano **già** con quali parole un motore dice di non poter lavorare (`unusable_when`, che nomina proprio le credenziali mancanti). Serve la prova end-to-end con un processo vero che stampa la variabile ricevuta — una prova sulla regola resterebbe verde con la regola scollegata, che è il guasto 18 daccapo | **chiuso** il 01/09, in due tempi, e il secondo dice più del primo. **Primo tempo, la divergenza**: `env: BTreeMap::new()` rimette il difetto e la prova torna rossa con `CASA=` vuota — il vaglio e la corsa partono dalla stessa casa. **Secondo tempo, ciò che restava**: la ragione con cui avevo dichiarato inutile un controllo sulle credenziali — «la diagnosi la dà il motore con le sue parole» — era stata scritta prima di misurarla, ed è falsa. Il vaglio a secco toglie la domanda apposta, quindi non vede niente di ciò che il motore controlla DOPO averla letta, e le credenziali stanno di là: rimisurato il 01/09 nelle due case, `codex exec < /dev/null` risponde «No prompt provided via stdin.» **parola per parola la stessa cosa**, e ne esce 1 tutte e due le volte — *fino a questa riscrittura questa riga diceva «esce zero», ed era falso; la conclusione regge lo stesso perché si appoggia all'identità delle due risposte, non al numero*. **LA CURA NON ERA UN VAGLIO MIGLIORE: ERA UNA DOMANDA IN PIÙ.** I descrittori dichiarano ora `login_status` — le opzioni con cui si chiede, e le parole con cui quel motore dice sì e dice no — e `sailor flow check` e `sailor profiles list` la fanno prima di lanciare, gratis, senza chiamare nessun fornitore. Chi non dichiara il blocco non fa scattare niente: **vuoto vuol dire «nessuno ha guardato», mai «è autenticato»**. Misurato sul comando vero, con i due profili `codex` di questa macchina senza `auth.json`: `flow check dispatch-the-work` è passato da «righe di comando sane: codex» e nient'altro a «CASE SENZA CREDENZIALI … codex (profilo «codex/prove», CODEX_HOME=…): «Not logged in»», e `profiles list` da due righe di soli nomi a due righe che dicono NON AUTENTICATO tutte e due. **Cinque mutanti eseguiti**, e il primo è il difetto originale: leggere le parole del sì prima di quelle del no fa risultare piena una casa vuota — «Not logged in» contiene «logged in»; togliere la chiamata dal rapporto rimette il vaglio muto; togliere `login_status` dal descrittore fa dire «nessuno ha guardato» e mai «autenticato»; far rispondere «autenticato» a un `LoggedOut` in `profiles list`; togliere il ramo booleano al lettore di puntatori, e claude non si legge più |
| 40 | 01/09 | **Uno stato di mezzo toglieva il guasto dal conto degli aperti.** La tabella usa «chiuso in parte» per una riga in cui metà della cura è fatta; `the_fault_table_holds_together` contava aperto **solo** ciò che dice esattamente «aperto», quindi ogni «chiuso in parte» spariva dal totale. La prosa diceva undici quando erano quattordici: il 14 e il 33 erano usciti dal conto il 31/08 e nessuno se n'era accorto, e il 37 stava per uscirne il 01/09 | Aggiungendo un campo `partly` al controllo per verificare il lavoro di un agente, e scoprendo che il campo **esisteva già, calcolato e mai interrogato**. Il conto vero l'ha detto la prova stessa quando le si è chiesto di contarli: 14, non 11 | **La direzione dell'errore non è casuale: è sempre quella che tranquillizza.** Un guasto conta come aperto finché la cura che *dichiara* non è fatta, e lo stato di mezzo serve solo a raccontare quale metà è fatta — la regola sta in `Fault::still_open()`, non nella testa di chi aggiorna la prosa. E un campo calcolato che nessuno interroga non è una difesa: è la forma di una difesa, e passa una revisione | **chiuso** il 01/09 — la prova nasce rossa sul conto vecchio e nomina il numero giusto |
| 41 | 01/09 | **Il vocabolario delle azioni della finestra e il registro del motore divergevano nei due versi contemporaneamente, e nessuno dei due sapeva dell'altro.** Verso uno: `ACTION_KIND` (`desktop/src/flow.ts`) nominava **sei azioni che non esistono in nessun crate** — `pane_until_idle`, `signal_is_gone`, `deposit_write`, `pane_send`, `hand_to_human`, `pane_read` — e quattro stavano nella cassetta dei passi, quindi premere «attesa», «deposito», «gesto» o «a una persona» creava un nodo che poi **non si salvava**. Quattro famiglie su nove erano bottoni che non funzionano. Verso due, peggiore: `flows.rs` costruiva **un quinto registro a mano** — `register_default` più due righe scelte — invece di chiamare `registry::default_registry`, e perciò **rifiutava al salvataggio cinque azioni che dal terminale girano**: `detect_tools`, `tool_needs`, `history_ask`, `subflow` e `handed_to_agent`, cioè il passo che consegna il lavoro a una persona — una funzione centrale del prodotto non si poteva comporre dalla finestra. Verso tre, silenzioso: `kindOf` ripiega su «verifica» per un nome che non conosce, quindi i sette passi `trigger` dei flussi veri si disegnavano da sempre come nodi di controllo | Censendo la superficie del prodotto per rifarne l'interfaccia, non cercando un difetto. **Nessuna prova lo vedeva, e due commenti dichiaravano il contrario di ciò che il codice faceva**: `flow.ts` diceva «il motore ne registra due» (vero fino al 28/08, poi sedici) e `flows.rs` diceva «la stessa lista che usa `sailor flow check`, mai una copia riscritta qui» proprio sopra la copia | **Una regola scritta in un commento e non interrogata da niente non è una difesa: è la forma di una difesa, e supera una revisione.** L'ancora deve stare **fuori da tutte e due le copie** — una prova che legge il sorgente della finestra e lo confronta col registro del motore, **nei due versi**: nessun nome inventato di qua, nessuna azione del motore lasciata senza famiglia di là. Confrontare due mappe scritte a mano le lascia sbagliare insieme, ed è il guasto 19. E il ripiego silenzioso va tolto insieme alla causa: `kindOf` che ricade su un valore plausibile è ciò che ha nascosto il terzo verso per quattro giorni | **chiuso** il 01/09 — con mutante. Le due prove (`the_window_knows_every_action_the_engine_can_run`, `the_window_vocabulary_names_only_actions_the_engine_registers`, in `desktop/src-tauri/src/flows.rs`) sono nate rosse nominando i dieci nomi esatti, cinque per verso. `flows.rs` ora delega a `registry::default_registry`; il vocabolario elenca le sedici azioni vere e la cassetta ne offre sette. Il mutante che rimette un nome inventato — `pane_send` — riporta la seconda prova al rosso. **Restano dichiarati e non riparati** due limiti: i dati d'esempio (`sample.ts`) descrivono ancora la staffetta con cinque azioni che non esistono, ed è scritto in testa al file perché quel flusso racconta come dovrà essere, non come il motore la sa fare; e le due famiglie «attesa» e «ramo» non hanno nessuna azione, quindi la cassetta non le offre — è la lista della spesa vera, ed è corta |
| 42 | 01/09 | **Una risorsa che tutte le sessioni scrivono e nessuna sorveglia fa danno in silenzio, e in un giorno solo ne abbiamo viste quattro facce.** *(a)* **I numeri di questa tabella**: il 31/08 quattro agenti in alberi isolati hanno rivendicato gli stessi due numeri (guasto 33), e l'isolamento non protegge un contatore. *(b)* **La `user_version` del deposito**: un cantiere ha migrato `~/.claude/state/flussi/state.db` alla versione 8 sul file **vero** invece che su una copia, e da quel momento il binario installato — fermo alla 7 — si è rifiutato di aprirlo. Nessun dato perso (`events.db` intatto, la proiezione si ricostruisce), ma la macchina è rimasta senza il suo registro finché non è stata fusa la versione 8. *(c)* **L'albero di lavoro condiviso**: trenta file non committati nel clone principale contenevano il lavoro mescolato di **tre** sessioni — +227 righe in `crates/actions/src/lib.rs` che nessuna delle due sessioni presenti aveva scritto, e in `flow_cmd.rs` un pezzo non più separabile perché sulle stesse righe di contesto. **L'attribuzione è persa per sempre**, e un ramo verde è rimasto fermo ventiquattr'ore perché nessuno poteva fondere sopra quello sporco. *(d)* **E la scoperta che le lega**: la numerazione **non è nemmeno rappresentabile** fra rami. Provando a usare il 43 su un ramo nuovo, `every_fault_has_its_own_number_and_none_is_missing` è nata rossa con «numeri saltati: [41]» — perché su quel ramo la tabella arriva a 40. Un numero «riservato» per accordo fra sessioni vive solo nei messaggi che si scambiano: **non esiste nessun posto, visibile a tutti i rami, dove scrivere che è preso**, e la prova che guarda il file lo rifiuta giustamente | Tre volte per averle sbattute contro: i numeri contesi il 31/08, l'errore `unsupported projection schema version 8` sul deposito vivo, e il diff letto prima di committare che conteneva codice di altri. La quarta — la non rappresentabilità — l'ha trovata una prova esistente, nata rossa su un numero riservato a voce | **Le prime due difese sono di procedura e valgono da subito.** (1) **Nessuna sessione lavora nell'albero condiviso**: ognuna in una copia propria, e si fonde solo da lì — è la difesa che il caso *(c)* rende non negoziabile, perché il suo danno è già irreversibile. (2) **Nessun cantiere tocca lo stato vero della macchina**: il deposito si apre con `SAILOR_LEDGER` puntato a una copia, e chi migra lo fa lì. **La terza è codice, e la parte difficile è che deve guardare fuori dal proprio ramo.** Per la versione dello schema: una prova che confronti `PROJECTION_SCHEMA_VERSION` con le migrazioni presenti, così due rami che definiscono la stessa versione successiva si scontrano **in rosso** invece che in silenzio sul disco di chi lavora. Per i numeri dei guasti la prova interna **non basta e non può bastare** — un numero preso da un altro ramo è invisibile —: serve un registro fuori dalla tabella, o l'ammissione che il numero si assegna **al momento della fusione** e non prima. Finché non c'è, chi rivendica un numero lo scriva **dentro la riga**: alla fusione git mostra un conflitto vero invece di un silenzio | **aperto** — le due difese di procedura sono in vigore dal 01/09; il codice no |
| 43 | 01/09 | **IL NUMERO È RIVENDICATO E CONTESO, come chiede il guasto 42: anche `work/session-tracking` tiene un 43 e un 44, e chi fonde rinumeri.** Detto questo: l'inventario rispondeva «zero repo» con uscita 0 su qualunque macchina che non fosse quella di chi l'aveva scritto. Due metà che si nascondevano a vicenda — `default_roots` cercava i repo sotto due cartelle di lavoro di una persona sola, **compilate nel binario**, e `repos_under` incontrava una base che non si apre, faceva `continue` e restituiva un elenco più corto. Qui quelle cartelle esistono, quindi il difetto non si vedeva; altrove «non ce ne sono» e «non ho potuto guardare» arrivavano a chi legge nella stessa forma. È la **terza faccia del guasto 12 in un giorno solo**, dopo `launchctl` dentro il perimetro e `ps` incanalato che risponde `1` con uscita 0 | Nel censimento di ciò che non rispetta la regola fissata da Theo il 01/09 — nessun codice specifico di prodotto, nessun presupposto sulla macchina. Né `default_roots` né `repos_under` avevano una sola prova: erano scoperte tutte e due | **Le due cure devono viaggiare insieme, perché da sole non bastano.** Togliere le cartelle di una persona senza saper dire «nessuna base dichiarata» spegne l'inventario in silenzio; saper dire «non ho potuto guardare» tenendo le cartelle compilate lascia il difetto dov'è. Le basi si dichiarano (`SAILOR_WORK_ROOTS`, o il file `work-roots` nella casa di Sailor) e `Survey` porta `roots`, `unreadable` e `bases_declared` fino all'`Inventory`, così un elenco magro dice **perché** è magro. La guardia che impedisce il ritorno non prova il comportamento ma **il sorgente**: `no_ones_personal_folders_are_compiled_into_the_binary` legge `lib.rs` dal disco e pretende che il corpo di `declared_bases` non nomini la casa — la violazione non era un difetto di logica ma di abitudine, e le abitudini rientrano dalla porta | **chiuso** il 01/09 — con mutante: rimettere un `home.join(...)` dentro `declared_bases` fa tornare rossa la guardia |
| 44 | 01/09 | **`supervisor::child::Process::start` non sovrappone nessun ambiente al processo che accende, perché `Spec` non ha un campo `env`.** È il guasto 18 in un secondo posto: `Command::new(&spec.command).args().current_dir().stdin().spawn()`, e nient'altro — il figlio eredita in blocco l'ambiente di chi ha aperto il terminale, e non c'è nessuna strada per dargliene un altro. Oggi è **innocuo**, perché da lì partono solo `npm run dev` e la finestra, che l'ambiente di chi li lancia lo vogliono davvero. Smette di esserlo il giorno che il supervisore accende una riga di comando con un profilo: il profilo non arriverà, e il motore dirà «non sono autenticato» invece di «nessuno mi ha passato la casa» | Leggendo `crates/supervisor/src/child.rs`: sette campi in `Spec` e nessuno è `env`; `grep` di `.env(`, `.envs(` ed `env_clear` su tutto il crate non trova niente fuori dai `std::env::temp_dir()` delle prove | Un campo `env` nello `Spec` e **una prova end-to-end che accende un processo vero e legge la variabile che il figlio ha ricevuto** — non una prova sulla regola, che resterebbe verde con la regola scollegata: è esattamente il modo in cui il guasto 18 era sopravvissuto la prima volta. Finché `Spec` non ha il campo quella prova non compila, ed è il modo giusto di nascere rossa | **chiuso** — con due mutanti, e provato come il guasto chiedeva: accendendo un processo vero e leggendo la variabile che il figlio ha ricevuto, non la regola che gliela dà. `Spec` ha il campo `environment`, e `Process::start` lo sovrappone con `.envs()`. Vuoto vuol dire «quello di chi lancia», che è ciò che un server di sviluppo e la finestra vogliono davvero, e il secondo braccio della prova lo tiene: senza, una riparazione che azzerasse l'ambiente di tutti passerebbe. Mutanti: togliere `.envs()` (cade il primo braccio), e aggiungere `env_clear()` (cade il secondo). Le prove stanno in `crates/supervisor/tests/a_process_sailor_lights_gets_the_environment_it_was_given.rs`. |
| 45 | 01/09 | **`sailor session list` e `sailor session census` uscivano 1 pretendendo un tty che non usano.** Le due forme non parlano del terminale da cui le si invoca — parlano di tutti — ma il tty veniva risolto per ogni forma, prima del dispatch. Ovunque l'uscita sia catturata, cioè in ogni script, in ogni gancio e in ogni prova, nessuno dei tre descrittori è un tty: il comando falliva sempre, e il messaggio mandava a cercare l'opzione `--tty` invece del difetto | **Solo eseguendo il binario vero.** Le 133 prove del crate erano verdi: provano `act`, che riceve il tty già in mano, e non `dispatch`, che lo deduce dal processo. Il pezzo non provato era esattamente il pezzo che non si può provare senza eseguire | Una prova che passa da `dispatch` — non dal corpo sotto — per ogni forma che **non** nomina un terminale. Gira dentro la batteria, dove l'uscita è catturata, quindi il tty manca davvero e la prova non deve simularne l'assenza. Più in generale: il confine fra ciò che una prova costruisce e ciò che il programma deduce dal proprio processo è dove i difetti si nascondono, e va attraversato almeno una volta per comando | **chiuso** il 01/09 — con `NEEDS_A_TERMINAL`, e la prova nasce rossa col messaggio del difetto vero |
| 46 | 01/09 | **Il comando che esiste per dire «non lo so» moriva prima di poterlo dire.** `sailor session census` risponde `NON LO SO: non mi è stato permesso guardare la macchina` ed esce 3 — è la cura del `ps` negato in silenzio, cioè il guasto 12. Ma `dispatch` apriva `sessions.db` **prima** di sapere quale forma fosse stata chiesta, e `census` il deposito non lo tocca affatto: un deposito illeggibile — permessi, disco pieno, un file di una versione più nuova — lo faceva uscire **1** con l'errore del file al posto della propria risposta. Chi legge quell'uscita non distingue più tre fatti diversi: «il deposito non si apre», «non ho potuto guardare la macchina», «non c'è nessun terminale». **Distruggere un «non lo so» utile con un «non lo so» che parla d'altro è il modo peggiore di sbagliare** | Segnalato da chi aveva appena scritto il comando, eseguendolo dentro il perimetro; verificato scrivendo la prova, che è nata rossa nominando la forma e l'errore di SQLite. Le prove del crate erano tutte verdi: passano da `act`, che riceve il deposito già aperto, non da `dispatch`, che decide se aprirlo | **È IL GUASTO 45 UNO SCHERMO PIÙ SOTTO, NELLO STESSO FILE, LO STESSO GIORNO.** Il 45 aveva stabilito il principio — «solo chi parla di un terminale ne pretende uno», scritto in maiuscolo sopra la riga del tty — e la riga immediatamente successiva, quella che apre il deposito, non l'aveva ricevuto. Una cura scritta come commento accanto a una riga non si estende da sola alla riga dopo: **un principio vale solo dove c'è un elenco che lo applica**. Quindi `NEEDS_THE_STORE` accanto a `NEEDS_A_TERMINAL`, e `Request::store` diventa un `Option` così che una forma dimenticata sia un difetto nominato invece di un panico. E la prova non guarda `census`: gira su **ogni** forma non elencata, perché quella aggiunta domani nessuno se la ricorderà | **chiuso** il 01/09 — con mutante: aprire il deposito incondizionatamente in `dispatch` fa tornare rossa la prova con «non legge il deposito, eppure è morto perché non si apriva» |
| 47 | 01/09 | **Una prova generica stava per riconfigurare la macchina di chi lancia la batteria, e a ogni `cargo test`.** La prova del guasto 46 gira su **ogni** forma di `sailor session` che non dichiara di leggere il deposito, chiamando `dispatch` per davvero: finché quell'insieme era `["census"]` era innocua. Poche ore dopo è entrata `install`, che senza `--settings` **scrive nella casa vera** — misurato con un `HOME` finto: quattro ganci innestati in `settings.json` e due comandi scritti in `commands/`. Tre file nella `~/.claude` di chi esegue la batteria, ganci compresi, a ogni corsa. **Il danno non è avvenuto**: l'ha visto chi fondeva, prima che le due metà si incontrassero su un albero solo | Fondendo, e provando la cosa con un `HOME` finto invece di ipotizzarla. Nessuna prova poteva vederlo: le due metà erano giuste ognuna per conto suo, e la pericolosa non esisteva ancora quando è stata scritta quella generica | **UNA PROVA GENERICA COPRE ANCHE CIÒ CHE NON ESISTEVA QUANDO È STATA SCRITTA — NEL BENE E NEL MALE.** È la stessa proprietà per cui vale più di una specifica: prende la forma che qualcuno aggiunge domani. E per la stessa ragione prende il **danno** che quella forma porta con sé. La cura non è rendere la prova specifica — sarebbe buttare via il motivo per cui esiste — ma **confinare nella cartella usa-e-getta tutti gli effetti, non solo quelli che si conoscevano**: qui `--settings` accanto a `--store`, dichiarato sopra il ciclo. La regola generale: una prova che invoca un dispatch per nome deve dargli **ogni** ancora al mondo che quel dispatch potrebbe toccare, perché non sa quali toccherà. È il gemello della lezione del 46 — là un principio scritto in un commento non si estendeva alla riga dopo; qui una prova giusta non si estende da sola alle conseguenze di ciò che copre | **chiuso** il 01/09 — `--settings` nel ciclo, col perché scritto sopra la prova; verificato che `~/.claude/commands` non contiene niente di scritto dalla batteria |
| 48 | 01/09 | **Il nome di una persona decideva cosa Sailor considera installato.** In `crates/inventory/src/lib.rs` la raggiungibilità di una competenza si calcolava con `on.contains(&plugin) \|\| plugin.contains("mattpocock")`: chi pubblicava la raccolta era un ramo del codice. Gli altri due siti dello stesso nome erano il percorso da cui leggerla (`discovery.rs:26`) e il prefisso con cui invocarla (`discovery.rs:95`), quindi **ogni altra raccolta installata allo stesso modo era invisibile all'inventario**. Su questa macchina la riga dormiva perché quella cartella non esiste — verificato con `ls -d` — ma sarebbe tornata viva da sé il giorno che comparisse | Nel censimento del non-agnostico chiesto da Theo il 01/09. **Dormiva anche per un secondo motivo, ed è quello che conta**: stava dentro un `if` i cui primi due rami tornavano tutti e due `Reach::Active`. Una condizione il cui esito non cambia niente non si legge, si scorre — ed è per questo che è sopravvissuta a ogni lettura di quel file | **Sotto il nome c'era un principio vero, e il principio è sull'origine.** Una raccolta installata come cartella sotto `.claude/skills/` non è un plugin: non compare in `enabledPlugins`, e chiedere a quell'elenco se sia accesa dà «no» a una domanda che non le si applica — una risposta falsa, non negativa. Ora c'è `discovery::Origin` con tre casi (`Home`, `Plugin`, `Collection`) e un `match` di tre righe: **il ramo morto non è stato tolto, è diventato impossibile**. La guardia che impedisce il ritorno legge i sorgenti e rifiuta quel nome in `lib.rs` e `discovery.rs`, perché la violazione non era un errore di logica ma un'abitudine — far entrare nel codice il nome di ciò che si ha sotto mano | **chiuso** il 01/09 — con mutante (rimettere `\|\| name.contains("mattpocock")` fa tornare rossa la guardia) e con A/B contro la base sulla macchina vera: 55 voci prima, 55 dopo, nessuna raggiungibilità cambiata |
| 49 | 01/09 | **Il prodotto spediva regole che mandano a un flusso che il prodotto non spedisce.** `crates/terminal/descriptors/default.json` viaggia dentro il binario e instrada il lavoro verso `dispatch-the-work`, che stava in `flows/` di **questo** progetto: un flusso nostro. Su questa macchina la regola funzionava perché il file c'era; **su qualunque macchina di qualunque utente puntava al nulla**. E le due prove che avrebbero dovuto vederlo — `the_shipped_rules_all_load_and_name_a_flow_that_exists` e `the_flow_the_shipped_rules_name_starts_with_a_manual_trigger` — cercavano il flusso in `flows/` del repo, quindi erano **verdi qui e false ovunque**. Stessa forma del guasto 41: due elenchi che devono combaciare, uno dentro il binario e uno fuori, e nessuno che li confronti | Togliendo dal repo i flussi di sviluppo, per la richiesta di Theo del 01/09 di pubblicare solo ciò che un utente scaricherebbe. Le due prove sono diventate rosse **perché era sparito il puntello che le teneva in piedi**: nessuno le aveva scritte sbagliate, erano scritte contro un mondo che valeva solo qui | **Ciò che viaggia nel binario si controlla contro ciò che viaggia nel binario.** Le due prove ora cercano in `flow::system::FLOWS` invece che su disco, quindi una regola spedita può nominare solo un flusso spedito e la divergenza è impossibile invece che improbabile. `dispatch-the-work` è stato promosso a flusso di sistema — ha zero righe legate a una macchina, e una regola spedita che lo nomina lo rende prodotto per definizione. E una prova nuova, `no_shipped_flow_carries_a_path_from_one_machine`, tiene i flussi spediti liberi da percorsi di casa: la garanzia esisteva ma sorvegliava i **nostri** flussi, che potevano permetterseli | **chiuso** il 01/09 — batteria 72 bersagli, 876 prove, 0 rosse |
| 50 | 01/09 | **`sailor flow cap` e `sailor flow schedule` scrivevano nel file di un altro flusso, dicevano «fatto» e uscivano zero.** Il controllo che autorizza la riscrittura chiedeva se `<id>.flow.json` **esistesse**, non se fosse *quel* file. Con in cartella un file il cui nome non è il proprio `id` — e un secondo file che si chiama come quell'`id` — la risposta era sì, e `save_in` finiva nel secondo. Il flusso che una persona aveva chiesto di cambiare restava com'era; quello di qualcun altro si ritrovava un innesco o un tetto che nessuno gli aveva messo. **IL NUMERO DI QUESTA RIGA VA RICONTROLLATO IN FUSIONE**: è il primo libero nell'albero `work/guasti-flussi`, e altri rami stanno numerando righe nuove nello stesso momento — è già successo il 31/08 con due 27 e due 28 | Un giudice che rileggeva il comando nuovo, col binario vero: `sailor flow schedule nome-diverso 3600s leggero` rispondeva «fatto» con uscita 0, e lo `schedule` compariva in `prova-innesco.flow.json`. Nessuna prova lo vedeva: tutte quelle su `cap` mettevano **un file solo** in cartella, e con un file solo `exists()` e «è quel file» danno la stessa risposta | **Il file che si legge e il file che si scrive devono essere lo stesso, e lo dice il confronto fra i due nomi — non l'esistenza del secondo.** Una domanda vicina a quella giusta dà la risposta giusta finché il caso è semplice, e smette senza avvisare: la fixture deve contenere **due** flussi, o il controllo non può fallire. Vale per ogni gesto che scrive, presente e futuro, quindi il rifiuto sta nel punto solo da cui passano tutti | **chiuso** il 01/09 — con mutante. Il confronto è `name != flow.id` in `a_flow_i_may_rewrite` (`crates/sailor/src/flow_cmd.rs`), attraversato sia da `cap` sia da `schedule`; accanto restano i due casi in cui il nome coincide e il file **non** è quello che si riscriverebbe: un `<nome>.json` senza `.flow`, e i due insieme, dove quale gira lo decide l'ordine in cui il sistema elenca la cartella. La prova mette due file con lo stesso `id` e pretende che **tutti e due** i gesti rifiutino e che il flusso estraneo resti intatto: un rifiuto che avesse comunque scritto sarebbe peggio del difetto. *Mutante*: rimettere `exists()` — le due scritture riescono di nuovo e la prova cade nominando il testo del «fatto». Verificato anche col binario vero, prima e dopo |
| 51 | 01/09 | **Un passo che non può girare in nessuno dei due rami, e il flusso chiude verde.** `flows/chiedi-all-indice.flow.json`, passo `leggi`: la sua unica dipendenza `chiedi` è dichiarata **saltabile**, e una dipendenza saltabile arriva **nominata** — l'ingresso è `{}` se è stata saltata e `{"chiedi": …}` se è andata. Il `when` di `leggi` chiede `/status` e i suoi `$from` chiedono `/caveat`, `/server_tool`, `/project_root`, `/text`: senza il prefisso `/chiedi`, nessuno dei due rami combacia. Il passo che legge la risposta dell'indice — cioè l'unico che produce qualcosa per una persona — **non gira mai**, e la corsa non diventa rossa: un passo saltato chiude verde. **IL NUMERO DI QUESTA RIGA VA RICONTROLLATO IN FUSIONE**: è il primo libero nell'albero `work/guasti-flussi`, e altri rami stanno numerando nello stesso momento | Scrivendo la prova del guasto 28 sul passo saltato: la stessa forma, copiata dal flusso vero, non girava **nemmeno con la dipendenza eseguita**, e ho dovuto scrivere `/chiedi/status` perché girasse. Poi rifatta dal deposito da un giudice, che ha confermato i due rami. Nessun controllo lo vede: `flow check` guarda azioni, strumenti, capacità e percorsi, mai la **forma** che l'ingresso avrà | **La cura non è correggere quel file.** Un `.flow.json` è un dato di Theo, e cambiarlo per farlo tornare verde sarebbe decidere al posto suo. La cura è che `flow check` sappia vedere un `when` o un `$from` che **non potrà mai combaciare** con la forma che l'ingresso avrà — forma che dipende dalle `deps` e dalle `skippable_dependencies`, ed è calcolabile senza eseguire niente. È la metà mancante della colonna del guasto 28 che si ripresenta da una terza porta: là il puntatore morto si scopriva pagando, qui non si scopre affatto, perché il verde di un passo saltato è indistinguibile da quello di un passo che non serviva | **chiuso** — con due mutanti, e curato dove il guasto diceva di curarlo: non nel file di Theo, ma in `flow check`. Il controllo calcola la forma che l'ingresso avrà — con più dipendenze, o una dichiarata saltabile, è un oggetto con una chiave per dipendenza e il «with» sovrapposto — e rifiuta un puntatore il cui primo segmento non è nessuna delle due cose. Giudica solo dove la forma è certa: una dipendenza sola non saltabile passa la propria uscita, e cosa ci sia dentro è affare dell'altro passo. Sulla macchina di Theo trova **cinque flussi**, non uno: `chiedi-all-indice/leggi` (questo guasto, in `when`: salta e chiude verde), e poi `sviluppa-sailor/scegli`, `esamina-la-repo-ramificando`, `esamina-la-repo-riscoprendo` e `porta-una-superficie-alla-carta`, che portano `/text` o `/where` dentro un valore e si romperebbero al primo giro con `unresolved_reference`. I file restano di Theo: il controllo li nomina, non li riscrive. |
| 52 | 01/09 | **Un commit ha cancellato dieci flussi e novecento righe di prove mentre dichiarava di rinominare tre chiavi in un `.yml`, e la batteria è rimasta verde.** `ae27a21` toglie 3.942 righe — undici `.flow.json` e due file di prova — e il messaggio non ne dice una parola. Verde perché **cancellare una prova non la fa fallire**: è il canarino che sparisce invece di morire, e nessun conteggio la cercava. Che fosse un errore lo dice il codice rimasto, che continua a nominare i file spariti (`crates/flow/src/workspace.rs` cita `flows/sviluppa-sailor.flow.json`, `crates/actions` cita `flows/come-lo-risolvono-gli-altri.flow.json`, `AGENTS.md` cita `flows/prima-corsa.flow.json`) | Alla fusione: `git diff --cached --diff-filter=D` prima di committare mostrava nove cancellazioni **che il merge aveva già messo in scala in silenzio**, senza conflitto, perché nessuno dei due lati le contestava | Un conto dei binari di prova che non può scendere senza che qualcuno lo dichiari, e un controllo che ogni percorso `flows/...` nominato nel codice esista | **chiuso** il 05/09: due giudici, `the_battery_does_not_shrink_in_silence` (binari di prova, funzioni `#[test]` e file di flusso: 111 / 1 490 / 10, un numero sotto il seme è rosso finché il commit che toglie non lo dichiara) e `every_flow_path_the_code_names_exists` (ogni `flows/….flow.json` nominato nel codice esiste). Mutanti: un file di prova e un flusso spostati fuori → i tre conti rossi; un percorso inventato in una sorgente → rosso con file e riga. *Prima, aperto:* i file sono ripristinati, il controllo che lo impedirebbe no |
| 53 | 01/09 | **Il pannello di modifica cancella la catena dei motori dal file, in silenzio, e dice «salvato».** `StepEditor.tsx` legge il motore con `toolOf`, che sa leggere una stringa e non un elenco; ma il campo `tool` in `crates/actions` è `One(String)` **oppure** `Chain(Vec<String>)` — «in ordine di preferenza, il migliore per primo». `tool` sta in `MANAGED_KEYS`, quindi viene tolto dai parametri liberi; `toolOf` su una catena risponde `""`; e chi ricompone scrive il campo **solo se non è vuoto**. Basta toccare il modello o uscire dal riquadro e premere «Salva»: `{ tool: ["claude-code","agy","codex"], model: "sonnet" }` diventa `{ model: "opus" }`. Riguarda **20 dei 25 passi `external_engine`** dei flussi di questo repo — cioè quasi tutti quelli che hanno un ricambio | Da un giudice che ha eseguito il salvataggio nella propria copia, non leggendo il codice: la lettura mostrava un riquadro vuoto, il salvataggio mostrava il campo sparito | Una prova che carica un flusso con la catena, lo passa dal pannello e ne ricontrolla il **file**: ciò che non si sa leggere non si può riscrivere, e chi non sa un campo deve lasciarlo dov'era invece di ometterlo | **chiuso** — con mutante, e provato come questo guasto chiedeva. `splitToolParams` tiene fra i parametri liberi ogni campo gestito che il pannello **non sa reggere** (`panelCanHold`), quindi una catena torna su disco com'era invece di essere omessa: ciò che non si sa leggere non si riscrive. Tre prove in `StepEditor.test.tsx` montano il pannello vero sui flussi veri, toccano il campo del modello e ricontrollano cosa finisce nel file — non una simulazione, che sbaglierebbe accanto al codice. Il mutante, cioè togliere `panelCanHold` dallo smistamento, ne fa cadere tre su sei. |
| 54 | 01/09 | **Le 23 prove del guscio della finestra non stanno nel gate, e una è rimasta rossa in silenzio.** `desktop/src-tauri` dichiara un `[workspace]` suo, quindi `cargo test --workspace` **non lo compila**: chi guarda la batteria vede 902 prove e crede di aver visto tutto. Quando la riga di comando è passata all'inglese, `the_manual_crosses_the_bridge_as_json` — che cercava `sailor flow run <nome> [mandato]`, ricopiato a mano — è diventata falsa e rossa, e **chi ha tradotto ha creduto di aver finito**. Un rosso invisibile è peggio di un verde | Alla fusione, eseguendo a mano `cargo test` dentro `desktop/src-tauri`: fuori dal gate non lo esegue nessuno | Il gate esegue anche i workspace annidati — o `desktop/src-tauri` smette di dichiararne uno proprio. Finché non accade, ogni conto di prove che non lo nomina è parziale | **chiuso** il 01/09 — la prova è riparata (adesso conta le forme invece di ricopiarne una, e un `serde(skip)` su `usage` la fa cadere) e la cecità del gate anche: la CI ha un lavoro `desktop` che installa le librerie di sistema di Tauri ed esegue `cargo test --manifest-path desktop/src-tauri/Cargo.toml`. Il conto onesto è 936 + 23 |
| 55 | 01/09 | **Lo statuto fondativo era scritto e nessun controllo lo eseguiva: 35 percorsi della casa di chi scrive e 17 menzioni del nome di un datore di lavoro erano già pubblici.** Lo statuto dice, dal primo commit, «nessun percorso assoluto da una macchina di sviluppo, nessun nome di datore di lavoro o cliente, nessun nome di repository interno». Erano dentro fixture di prova, un segnaposto della finestra, un flusso spedito (`"who": "theo"`), sei documenti e — la peggiore — **la guardia stessa** che impediva di ricompilare quelle cartelle: cercava tre nomi di cartella, e per cercarli doveva scriverli, cioè pubblicare esattamente ciò che teneva fuori | Convertendo `crates/profiles` all'inglese: una nota di prodotto conteneva `/Users/…/.local/bin/agy`. Un `grep` sull'albero ha trovato le altre 34 | Un cancello che **si arma dalla macchina**, non da un elenco: quello che non deve comparire è `$HOME`, che ogni macchina conosce di sé, quindi vale per chiunque contribuisca e non nomina nessuno. I nomi che un elenco deve pur contenere vivono **fuori dal repo** (`~/personal/.sailor-notes/private-names`), e la prova dice a voce se è armata o no | **chiuso** il 01/09 — `crates/sailor/tests/nothing_from_this_machine_is_published.rs`, nato rosso con 35 e verde a zero. **La storia di git li porta ancora**: ripulire l'albero non ripulisce i commit già spinti |
| 56 | 01/09 | **Due file della finestra scrivono in prosa che i terminali sopravvivono alla chiusura, e il Rust accanto dichiara che non è vero.** `desktop/src-tauri/src/terminal.rs` ha un capoverso intitolato «**COSA QUESTO NON DÀ**»: un `static` non viene mai lasciato cadere, quindi `Drop for Terminals` non gira, i processi muoiono per conseguenza del sistema operativo e non per chiusura ordinata, «la sopravvivenza vera è un cantiere a sé… e non è questa». Ma `desktop/src/terminal.ts` scrive «un terminale **sopravvive** a chi lo guarda: chiudere la finestra non uccide la sessione dentro, e al riavvio è questo elenco a ritrovarla», e `Terminals.tsx` lo ripete — **usandolo come la ragione per cui l'elenco sta nel motore invece che nella tela**. Una proprietà non implementata è diventata la giustificazione di una scelta architetturale, e chi legge la tela non ha modo di sapere che il Rust la smentisce sei file più in là | Da un censimento che confrontava le tavole di design col codice, non da un guasto in esecuzione: nessuna prova lega le due prose, e tutte e due sono verdi perché nessuna delle due è codice | Una prova che legga le due sorgenti e pretenda che ciò che la tela promette sul ciclo di vita di un terminale sia dichiarato dal guscio — o, meglio, che la promessa venga dal guscio invece di essere ricopiata. È il guasto 10 (la stessa verità in più posti) applicato alla prosa invece che ai dati | **chiuso** — con mutante. Le due prose adesso dicono la stessa cosa vera (i terminali stanno in `sailor terminal host`, un processo che sopravvive alla finestra) e il paragrafo del guscio che lo negava non c'è più; il gesto A2 del 04/09 l'ha misurato a mano, chiudendo la finestra con ⌘Q e ritrovando le due schede vive. Ma il rimedio che questo guasto chiedeva — qualcosa che leghi le due prose — non esisteva, ed erano coerenti solo a mano. Adesso `the_window_promises_no_lifetime_the_shell_denies` legge i commenti dei tre file insieme, con le righe riunite in una sola frase perché una promessa avvolta su due righe è la stessa promessa, e pretende che chi promette la sopravvivenza nomini chi la tiene. Il mutante è il difetto storico in persona: rimettendo il paragrafo «cosa questo non dà» la prova nomina in rosso tutti e due i file della finestra. |
| 57 | 01/09 | **Due elenchi scritti a mano sullo stesso tipo chiuso divergono — e a volte nascono già divergenti, quindi nessuna cronologia ne conserva il «prima».** Tre istanze in un giorno. `KIND_LABEL` era dichiarato in `StepNode.tsx` e in `StepEditor.tsx`: nove chiavi identiche, **quattro valori diversi** — un passo era «agente» sulla tela e «engine» nel pannello, «a una persona» qui e «person» là. `KIND_ICON` in `StepNode.tsx` e `MARK` in `Toolbar.tsx`: **nove specie su nove disegnate diversamente**, e non per sciatteria — la barra dichiarava un proprio sistema («la freccia uscente vuol dire che il lavoro se ne va di qui») che il nodo non aveva, quindi **chi imparava un segno nella barra e poi lo cercava sulla tela non lo trovava**. E `MARK` aveva nove voci perché il tipo è totale mentre `TOOL_GROUPS` ne offre sette: due disegnate e mai mostrate. Nessuna prova poteva vederne una: tutte compilano, tutte rendono, e **ogni prova guardava la propria metà dello schermo** | Non da un guasto in esecuzione: convertendo la finestra al catalogo, e poi applicando di proposito la guardia «cerca i `Record<TipoChiuso, …>` scritti a mano; se ce ne sono due sullo stesso tipo, o una è derivata o divergeranno» | `desktop/src/parallellists.test.ts`: **nessun tipo chiuso può avere due mappe di disegni scritte a mano**, cercato per forma e non per difetto, col proprio canarino — una ricerca che smette di trovare passa per aver guardato niente. La regola generale: *una cosa scritta due volte non diverge perché qualcuno sbaglia, diverge perché è scritta due volte; il tempo separa quelle che nascono uguali e non serve a quelle che nascono diverse* | **chiuso** il 01/09 — `KIND_LABEL` unificato, `MARK` cancellato in favore di `KIND_ICON` (tavole, date e merito convergono, e l'informazione che `MARK` portava era già detta a parole dalle etichette dei gruppi), e la guardia è eseguibile |
| 58 | 01/09 | **Tradurre un commento non è tradurlo: è riassumerlo — e il metro con cui si giudica la traduzione è la perdita di chi l'ha scritto.** Sette agenti hanno convertito sette crate all'inglese, ciascuno da solo, e **tutti e sette hanno perso fra il 23% e il 31% delle righe di commento** (74,1% di ritenzione media). Sbagli indipendenti non convergono così. Ciò che sparisce ha una forma sola, **la regola sopravvive e la ragione no**: resta «un nome di prodotto in un'etichetta, mai in una condizione» e sparisce «stampare *gira in Orca* va bene, `if host == "orca"` è vietato», che era la metà controllabile a occhio; «95 competenze scritte, cinque raggiungibili» diventa «dozens written, a handful reachable» dentro un commento che si apre promettendo di mostrare perché non è un caso limite; «è il guasto 8, che parlava dei descrittori e vale identico qui» diventa «fault 8». Sono spariti anche **i limiti dichiarati di un meccanismo di sicurezza** (che cosa il tetto di spesa non può promettere) e **una convenzione di proprietà dei dati** (perché il prefisso è `workspace.` e non `flow.`) | Da sette giudici che non avevano tradotto, con file e riga — non dai rapporti degli agenti, che dichiaravano «nessuna conoscenza persa» e in tre casi **citavano accoppiamenti inesistenti** per giustificare ciò che non avevano fatto | Un'onda di riparazione guidata dai reperti concreti, con un secondo giudice che cerca **il riempimento**: testo inglese senza corrispondente nell'originale, scritto per alzare il conto. Un numero raggiunto con testo inventato è peggio della perdita che nasconde. E **il bersaglio va giudicato anche lui**: era l'81,3% dell'unica crate convertita a mano, e giudicata è salita a 94,7% — non era il costo della traduzione, era la perdita di chi l'aveva fatta | **chiuso** il 01/09 — 74,1% → 83,7%, e i due difetti concreti riparati a mano: un'asserzione tautologica in `ui/src/gather.rs` (chiedeva a un valore se era uguale alla costante che l'aveva prodotto) e una frase inventata e falsa in `toolbox/src/descriptor.rs`. **Resta il vincolo**: il tetto di 6 righe per blocco costringe a scambiare una frase base con un'altra, quindi il 100% non è raggiungibile finché il tetto regge — i due cricchetti tirano in direzioni opposte |
| 59 | 01/09 | `SAILOR_HOME` significava due cose opposte. Ovunque nel workspace — `ledger::sailor_home`, `toolbox`, `trigger`, il listino, i documenti — vuol dire la casa della CONFIGURAZIONE di Sailor, e c'è persino una prova che lo impone (`one_home_for_everything`). In `release_cmd::sources_root` veniva letta come l'ALBERO DEI SORGENTI da cui compilare. Chi avesse dichiarato la propria casa nel modo che tutto il resto del prodotto gli chiede avrebbe fatto compilare il rilascio da lì — e la casa della configurazione è anch'essa un repo git, quindi il clone di HEAD ci rimette in servizio l'albero che ci trova: uscita 0, timbro al suo posto, e il lavoro della mattina disinstallato. È il guasto del rilascio del 28/08 raggiunto da una seconda porta, e la cura di allora aveva cambiato la costante lasciando ambigua la variabile. | Non si è mostrato: nessuno ha `SAILOR_HOME` impostata su questa macchina. È emerso misurando gli usi della variabile mentre si toglieva `.claude` dal codice, perché la cura di un altro difetto — far installare il rilascio nella casa di Sailor invece che in quella di Claude Code — passava proprio da `sailor_home()` e avrebbe collegato le due letture. Un guasto latente non è un guasto più piccolo: è uno che nessuno ha ancora incontrato. | `one_variable_means_one_thing`: legge i sorgenti spediti e nega che qualcuno fuori da `crates/ledger` e `crates/toolbox` legga `SAILOR_HOME`. Un secondo lettore non contraddice ad alta voce — obbedisce alla stessa parola e va altrove, e chi ha impostato la variabile ha fatto ciò che gli era stato detto. Provata rimettendo il difetto: rossa con cinque righe, e ne ha trovate tre che la riparazione aveva lasciato indietro. | **chiuso** il 01/09 — con mutante. `sources_root` legge `SAILOR_SOURCES`; `install_root` chiede `ledger::sailor_home()`, quindi il binario e il timbro escono da `~/.claude` ed entrano nella casa di Sailor. Il timbro lasciato nella casa vecchia si legge ancora una volta, così il primo rilascio dopo lo spostamento non perde il conto dei commit. |
| 60 | 01/09 | `sailor session install` innesta i ganci in **una sola** riga di comando su tre che li sostengono. Il percorso `~/.claude/settings.json` è cablato in `session_cmd::default_settings()`, e il commento sopra la funzione dichiara che non è un accoppiamento perché «è l'indirizzo di ciò che stiamo innestando». L'indirizzo È l'accoppiamento: per Codex e per Gemini CLI il comando non innesta niente e **non dice di non averlo fatto**. Il descrittore degli strumenti non ha nessun campo per «come questa riga di comando si fa dire che una sessione è iniziata», quindi il dato non ha dove stare. | Misurato il 01/09 sui binari installati su questa macchina, non dalla documentazione. **Codex**: `codex --help` espone `--dangerously-bypass-hook-trust`, e il binario elenca gli eventi `PreToolUse, PermissionRequest, PostToolUse, PreCompact, PostCompact, SessionStart, SessionEnd, UserPromptSubmit, SubagentStart, SubagentStop, Stop, Interrupt` — **lo stesso vocabolario di Claude Code**, tutti e quattro quelli che Sailor usa compresi; si dichiarano sotto la chiave `hooks` di `$CODEX_HOME/config.toml`. **Gemini CLI**: ha `gemini hooks`, con dentro `gemini hooks migrate --from-claude` che traduce i ganci di Claude Code nei suoi; si dichiarano sotto la chiave `hooks` di `~/.gemini/settings.json`, con la stessa struttura JSON (`{type: "command", command: …, timeout}`) e quattro nomi diversi — `SessionStart` resta, `UserPromptSubmit`→`BeforeAgent`, `Stop`→`AfterAgent`, `PreCompact`→`PreCompress`. **agy**: nessun gancio e nessun evento in `--help`. **ollama**: non è un agente. **openrouter-cli**: non installata. | Un blocco `session_hooks` nel descrittore: il file delle impostazioni, e la mappa dai quattro momenti di Sailor ai nomi di evento di quella riga di comando. `session_cmd` scorre i descrittori che lo dichiarano invece di nominarne uno, e per chi non lo dichiara **dice che non ha innestato**, che è l'informazione che oggi manca. Non è un'ipotesi: due dei tre usano nomi identici, il terzo ha una tabella di traduzione misurata e scritta qui sopra. | **chiuso** — costruito come il guasto chiedeva, e provato sui file veri. Il blocco `session_hooks` sta nel descrittore: file (con `root_var` per chi ne ha uno), formato — JSON o TOML — e la mappa dai quattro momenti di Sailor ai nomi che quella riga di comando usa. `sailor session install` scorre i descrittori che lo dichiarano e non ne nomina nessuno. Provato il 04/09/2026 con una casa finta: tre file scritti in un colpo — `.claude/settings.json`, `.codex/config.toml` in TOML, `.gemini/settings.json` con i nomi suoi (`SessionStart`, `AfterAgent`, `BeforeAgent`, `PreCompress`) — e per `agy`, `openrouter-cli` e `ollama` la riga «lasciato stare: non dichiara come gli si dice che una sessione è iniziata», che è l'informazione che mancava. Dal 04/09 ogni riga innestata porta anche `--cli <id>`, quindi il gancio dice al deposito quale motore lo ha battuto. Mutante: tolto `session_hooks` dal descrittore di codex, l'innesto lo lascia stare e lo dice, invece di scrivergli il file di un altro. |
| 61 | 01/09 | **Il deposito dei guasti accettava celle che una riga di tabella non può contenere, e il guasto spariva dal registro senza che nulla fallisse.** Una riga markdown è una riga sola con sei colonne: un ritorno a capo dentro una cella la spezza in pezzi che hanno il numero di colonne sbagliato, e il separatore dentro una cella ne aggiunge una. In tutti e due i casi `faults::parse` scarta la riga — e la scarta in silenzio, perché scartare le righe che non hanno sei colonne è esattamente ciò che le fa saltare l'intestazione e il tratteggio. Il guasto 60 nel deposito di questa macchina aveva quattro capoversi dentro la colonna «stato»: reso e riletto, sarebbe uscito dal registro senza una parola. | Non da un guasto in esecuzione: contando le righe di un dump del deposito mentre si misurava lo scarto col file — 70 righe per 60 guasti. La prova di andata e ritorno che avrebbe dovuto coglierlo (`every_row_survives_the_move_word_for_word`) era verde e non poteva vederlo, perché **riempie il deposito partendo dalla tabella**, e la tabella può contenere solo ciò che la tabella sa esprimere. È un controllo che si misura su un ingresso che ha già passato il controllo. | `nothing_that_breaks_a_row` a tutte e tre le porte di scrittura — `record`, `restore` e `set_status` — e non alle due che si trovano subito: lo stato è prosa anche lui, e una guardia su due porte su tre si legge come una guardia. Provata rimettendo il difetto una porta alla volta. E accanto, `sailor faults check <file.md>`, che confronta il deposito col file e esce diverso da zero: **non può essere una prova**, perché il deposito sta fuori dal repo e una prova che lo leggesse sarebbe rossa sulla macchina di chiunque altro a codice invariato, che è il guasto 5. Il controllo che nessuna prova può portare si dà a chi ha tutte e due le metà. | **chiuso** il 01/09 — con mutante. Le tre porte rifiutano, la prova gemella dichiara la conseguenza (una riga con un capoverso sparisce davvero nel giro di andata e ritorno) così che se un giorno la resa imparasse a proteggere i capoversi la guardia si possa riaprire **di proposito e non per sbaglio**. I due registri sono allineati lo stesso giorno: `check` dice sessanta e sessanta. |
| 62 | 01/09 | **Un passo che risponde `Waiting` non torna mai pronto, e la staffetta è costruita esattamente su quello: non si completa mai.** In `decision_from` (`crates/flow/src/executor.rs:1201`) l'esito `Waiting` finisce nel secchio `waiting` e **non rientra in `ready` da nessuna strada**: solo `Broke` e «mai eseguito» ci tornano. `take_mandate` è costruito su `Waiting` — la pausa *è* il suo scopo — quindi la staffetta arriva alla raccolta, parcheggia, e resta parcheggiata: un mandato fresco scritto dopo la pausa non viene mai riletto, e `flow resume` non registra nessun secondo tentativo. **E c'era già scritto**, in un commento dell'esecutore lasciato da chi aveva fatto il recupero dai crash: *«l'unica scelta sicura era “waiting” — e un passo in attesa non torna mai pronto, quindi una ripresa vedeva il passo interrotto e non lo rilanciava»*. Il recupero fu spostato **via** da `Waiting` per quel motivo; poi la staffetta ci è stata costruita sopra. **Il buco vero è che il motore non ha un terzo esito che significhi «non ancora, riprova al battito dopo»**: `Waiting` parcheggia per sempre, e `Broke` rientra in `ready` **dentro lo stesso ciclo e senza nessuna attesa** — non c'è backoff da nessuna parte, quindi i tentativi bruciano tutti insieme | Facendo girare la staffetta contro uno pseudo-terminale **vivo**, cosa che nessuno aveva mai fatto: le prove del flusso controllano che le azioni esistano, non che la corsa arrivi in fondo | Un terzo esito nel motore. La sua forma la ereditano **tutte** le azioni, quindi non è una riparazione locale: va disegnata, non improvvisata a fine giornata su un motore scritto da altri. Nel frattempo la descrizione del flusso **dice che non si completa**, invece di promettere una ripresa che non avviene | **chiuso** — e chiuso dalla misura che mancava. Il terzo esito che questo guasto chiedeva di disegnare esiste: `Outcome::NotYet` rientra in `ready` portando l'istante in cui torna — che è ciò che lo distingue da `Waiting`, dove non torna niente — e `take_mandate` risponde `NotYet`, non più `Waiting`. La prova che nessuno aveva mai fatto l'ho fatta il 04/09/2026 sera: un terminale che Sailor tiene, riempito a 1.500.000 byte cioè oltre la soglia dichiarata dal passo, e `sailor flow run passa-il-testimone`. La catena ha preso tutti e sei i passi: misura oltre soglia, richiesta del mandato battuta dentro, raccolta a `NotYet` perché nessuno l'aveva ancora lasciato, e dopo `sailor terminal mandate` la corsa ripresa ha azzerato la sessione e riconsegnato il mandato. Le tre righe le ho rilette dall'uscita del terminale, in ordine. La descrizione del flusso non promette più che non si completa. |
| 63 | 01/09 | **La guardia contro i percorsi personali cercava `$HOME` in forma assoluta, e lo stesso identico posto scritto col tilde le passava davanti.** `nothing_from_this_machine_is_published` rifiuta esplicitamente di cercare la forma `/Users/<x>/` — con una buona ragione scritta nel commento: le fixture portano case inventate a ragione, e `flow_cmd` deve nominare i prefissi che riconosce. Ma la stessa cartella scritta col tilde non è né l'una né l'altra cosa, ed è la forma che si usa nei documenti. Nove occorrenze le sono passate davanti. La peggiore stava in `crates/flow/system/smista-il-lavoro.md`, documento che accompagna un flusso compilato dentro il binario con `include_str!`: diceva a chiunque scarichi Sailor di fare `cd` in una cartella che ha solo il suo autore. Non è una fuga di informazione, è **un'istruzione sbagliata per ogni lettore che non sia una persona sola**. | Cercando a mano il nome personale nell'albero appena riscritto, dopo che la riscrittura della storia aveva ripulito i percorsi assoluti: la storia era pulita, l'albero no. Cinque delle nove stavano in `docs/censimento-non-agnostico.md`, che **quelle stesse occorrenze le segnalava già** il 01/09 — le aveva trovate un censimento precedente, scritte in una tabella, e nessuno le aveva tolte. Per segnalarle il documento ha dovuto ricopiarle, che è la trappola del divieto per elenco applicata a un referto invece che a una guardia. | `the_repository_does_not_name_its_own_place_on_this_machine`, che chiede a git dove sta l'albero principale — un worktree risponde con l'albero a cui appartiene, non con se stesso — e ne ricava la scrittura col tilde togliendo `HOME`. **La forma continua ad arrivare dalla macchina e non da un elenco**: nessun nome è scritto da nessuna parte, e ogni collaboratore è coperto dalla propria. Provata rimettendo il difetto. E il referto che le aveva trovate adesso descrive la forma invece di ricopiarla. | **chiuso** il 01/09 — con mutante. Nove occorrenze tolte: tre fixture, un documento spedito, cinque nel censimento. **CORREZIONE DI CLASSE, dalla sessione che ha riscritto la storia:** il tilde **non nomina la macchina di nessuno**, quindi lo statuto sulla riservatezza non era violato — quello parla di percorsi assoluti col nome utente, e la riscrittura li aveva tolti davvero. Questo è un difetto di **correttezza**, non di riservatezza, e merita la riparazione lo stesso: un `cd` verso una cartella che ha solo il suo autore è una bugia per ogni altro lettore, e un perimetro predefinito che punta lì è la stessa bugia in forma eseguibile. **Un difetto non ha bisogno di essere anche una fuga per meritare la riparazione.** Resta la lezione più generale: un referto che elenca ciò che va tolto lo pubblica, e un elemento trovato e scritto non è un elemento rimosso — queste stavano lì da un giorno col numero di riga accanto. |
| 64 | 01/09 | **Un elenco di cosa leggere è un elenco di cosa saltare, e il sorvegliante contava invece di coprire.** La guardia che tiene fuori dal repo pubblico i percorsi della macchina percorreva `design/` e ne scartava ogni file: `.html` non era fra le estensioni che apre. Quattro tavole di design invisibili, tre delle quali nominavano l'albero di chi le ha scritte. Nel frattempo il controllo del controllo — `the_check_can_still_see_the_files_it_reads` — asseriva «più di cento file scansionati» ed era verde: **un conteggio non è una copertura**. Il numero resta vero mentre un intero tipo di file sparisce, perché centinaia di altri lo tengono su. | Riparando il guasto 63 e poi confrontando la propria misura con quella di un'altra sessione: nove occorrenze contro quattordici. La differenza non era un errore di una delle due, era **ciò che la guardia non guardava**. Nessuna esecuzione poteva trovarlo, perché il controllo passava: si è visto solo chiedendo a `git ls-files` che cosa esiste nelle stesse cartelle e confrontandolo con ciò che il camminatore aveva aperto. | `nothing_git_tracks_in_those_places_goes_unread`: il sorvegliante non conta più, chiede a git l'elenco dei file tracciati nelle cartelle scansionate e pretende che ognuno sia stato aperto. Ciò che non è palesemente binario o si legge o dichiara perché no, quindi **un formato di testo nuovo esce rosso invece che in silenzio**. `.svg` sta di proposito fuori dall'elenco dei binari: è testo, e un percorso scritto dentro si pubblica come ogni altro. Provato togliendo `.html` dall'elenco: nomina le quattro tavole. | **chiuso** il 01/09 — con mutante. Due difetti trovati scrivendo la riparazione, che valgono quanto quello riparato. Il confronto fatto con `starts_with` semplice fa sembrare `desktop/src-tauri` un file dentro `desktop/src` e segnalava un buco inesistente: **si confronta sul confine di cartella**. E la prova aveva ripreso a scrivere la stringa che vieta, rientrata dal commento che spiegava il difetto — la trappola del divieto per elenco applicata a chi la stava riparando. |
| 65 | 02/09 | **Un ripiego che mostra la chiave nuda fa sembrare una voce mancante una scelta di disegno.** `desktop/src/RunConsole.tsx:292` rende una classe di guasto con `tryT(…) ?? classe`. Il ripiego è giusto — inventare una frase per una classe sconosciuta sarebbe peggio — **ma è anche ciò che nasconde il buco**: il motore emette **52 classi distinte e il catalogo ne copriva 15**, quindi le altre 37 arrivavano all'utente come `subflow_too_deep`, che si legge come un nome tecnico deliberato. Due elenchi scritti a mano sopra un insieme chiuso, e nessuno li confrontava. **E cinque di quelle classi non erano trovabili nemmeno cercandole**: `crates/flow/src/subflow.rs` costruiva il nome con `format!("subflow_{status}")`, quindi `subflow_failed` esisteva, raggiungeva gli utenti, e un grep per il suo nome tornava vuoto. | Chiudendo l'anello a mano durante la conversione all'inglese: estratte dai sorgenti le classi passate a `ActionError::new` e confrontate con le chiavi `run.failure.*`. **Nessuna esecuzione poteva trovarlo**, perché il ripiego è progettato per non fallire: la finestra rende la stringa e sembra aver funzionato. | `crates/catalogue`, che legge gli stessi `i18n/en.json` e `i18n/it.json` della finestra con `include_str!` — incorporati e non cercati su disco, perché un binario rilasciato è un file solo che può stare ovunque. Accanto, una prova che accoppia i due elenchi **nei due versi**: ogni classe ha una frase, e ogni frase risponde per una classe raggiungibile. Vive in Rust e non nella finestra perché `cargo test --workspace` gira a ogni cancello e `vitest` no. Il punto cieco che resta è **dichiarato e non chiudibile**: `crates/actions/src/mcp.rs` prende la classe dallo stato che gli manda il server degli strumenti, cioè una parola che viene da fuori questo repo. | **chiuso** il 02/09 — con quattro mutanti, e senza seme. Tolta una chiave viva (39 su 38), aggiunta una chiave orfana, rimessa la classe costruita da uno stato (punto cieco da 1 a 2), e un file di sorgente lasciato non aperto dal camminatore. **Il quarto mutante è passato verde al primo tentativo**, perché toglieva una ricorsione che questo albero non usa — tutti gli 89 sorgenti sono piatti — e il difetto vero era «un file smette di essere aperto». La guardia che lo prende chiede a **git** quali file esistono invece di contare i propri, e fallisce nominando il file. |
| 66 | 01/09 | **`sailor faults check` dava un consiglio giusto per metà dei casi e non diceva che erano due.** Davanti a qualunque differenza rispondeva «la tabella è il registro, quindi il deposito è ciò che deve muoversi». Ma **il deposito è uno per macchina e la tabella è una per albero di lavoro**: una differenza ha due letture — lavoro non ancora pubblicato, oppure un albero più vecchio del deposito — e il comando non può distinguerle. Chi avesse seguito il consiglio alla lettera dal ramo sbagliato avrebbe importato un registro più vecchio sopra uno più nuovo, e `import` diceva soltanto quante righe erano entrate: **un conteggio di ciò che entra si legge uguale che le righe fossero nuove o che metà avessero sostituito testo scritto altrove più tardi**. | Un'altra sessione ha eseguito il comando dal proprio albero, entro un'ora da quando era stato scritto, e ha riferito ciò che leggeva: due guasti «nel deposito e non nella tabella» che erano semplicemente quelli di un ramo che il suo albero non aveva. Il comando funzionava come progettato ed è il consiglio a essere monco. **Non l'avrebbe trovato nessuna prova**: sulla macchina di chi scrive le due metà sono quasi sempre allineate, ed è l'asimmetria di ambito fra i due depositi a creare il caso, non un difetto di logica. | Il messaggio nomina tutte e due le letture e non sceglie per chi legge; e `import` dichiara **quali righe ha sovrascritto**, per numero, raccolte **prima** della scrittura, perché dopo il testo vecchio non c'è più e il rapporto sarebbe onesto e vuoto insieme. Due prove nel modulo, e la seconda tiene l'avviso raro: un avviso che compare anche quando non è successo niente smette di essere letto. | **chiuso** il 01/09 — con mutante. La lezione oltre il comando: **quando due copie della stessa verità hanno ambiti diversi — una per macchina, una per ramo — la differenza fra loro non ha un significato solo**, e uno strumento che ne sceglie uno indovina al posto di chi legge. La sua sicurezza è indistinguibile dalla conoscenza. |
| 67 | 02/09 | **«Chi scrive una regola scrive anche ciò che la rende rossa» non ha nessuno che lo interroghi, e due regole sono entrate con il proprio controllo scritto solo in prosa.** `docs/2026-09-01-il-tempo-e-l-ultima-scelta.md` chiude specificando una prova che scorre i descrittori di innesco e fallisce se un innesco periodico non dichiara cosa fa con una corsa persa e qual è il suo limite di concorrenza — «nasce verde oggi». `docs/2026-08-31-le-quattro-superfici.md`, ripetuto in `docs/decisioni.md`, ne specifica un'altra che scorre il registro delle azioni e fallisce se un'azione non dichiara superficie e poteri — «nasce rossa sulle nove azioni di oggi», e aggiunge «senza non entra». Nessuna delle due è stata scritta, e le regole sono entrate lo stesso. Quattro documenti dichiarano un proprio controllo: due nominano una prova che esiste, due descrivono una prova che non c'è. **Il costo si è visto lo stesso giorno.** In un repo di lavoro che non è questo è stato costruito un risvegliatore periodico fuori dal sistema degli inneschi — un processo che riceve gli eventi da fuori, lancia i flussi corrispondenti e ogni tanto chiama il battito — che decideva in silenzio proprio le due cose che quel controllo avrebbe preteso di veder dichiarate. Niente è diventato rosso, perché la sola guardia che poteva vederlo era una frase. Ed era la seconda volta: il 29/08 lo stesso ripiego era stato scritto e cancellato in giornata, e `docs/da-fare.md` ne porta la ragione — «un cerotto fuori dal sistema su un buco dentro il sistema, e i cerotti restano» | Cercando il controllo prima di aggiungerne uno, invece di fidarsi della pagina che lo specifica: fra i sorgenti non compare nessuna occorrenza di corsa persa, limite di concorrenza o innesco periodico, e la parola superficie non compare né nel registro delle azioni né nel catalogo. **Nessuna esecuzione poteva trovarlo**: una prova che non esiste non fallisce, quindi la pagina che la promette resta verde per definizione. È la stessa famiglia del difetto che questo progetto insegue da giorni — non una regola che nessuno interroga, ma una regola che nessuno poteva leggere; qui diventata una guardia che nessuno ha scritto | Una prova che legge i documenti sotto `docs/`, trova ogni sezione che dichiara il proprio controllo e pretende che nomini una prova esistente nell'albero — come già fanno `comments_do_not_crowd_out_the_code` e `the_fault_table_holds_together`, che si citano per nome — invece di descriverla in prosa. Nasce rossa sulle due specificate e mai scritte, e **questo è il motivo per cui non l'ho aggiunta insieme a questa riga**: una guardia che nasce rossa ferma il cancello di chiunque altro stia lavorando nell'albero, e scegliere fra scrivere le due prove o ritirare le due regole non è di chi ha trovato il difetto | **chiuso** il 05/09: `every_declared_check_names_a_test_that_exists` legge `docs/*.md`, riconosce le forme con cui una sezione dichiara il proprio controllo (l'intestazione che comincia con «Il controllo», la riga di prova in corsivo del mandato, il comando di cargo che nomina il binario), e pretende che ogni nome di prova nominato esista; le sezioni che dichiarano il controllo in sola prosa sono contate contro un seme (37 oggi, solo in discesa) con file e riga. Mutanti: la ricerca del nome sostituita da «vero» → rosso; il contatore spento → rosso. *Prima, aperto:* Theo ha scelto il 04/09 per le quattro superfici: né la prova né il ritiro, il documento resta dichiarato e non in vigore finché non arriva il modello dei poteri di un passo, ed è scritto lì e in `decisioni.md`. Resta aperta la metà sugli inneschi periodici: quella prova non esiste ancora |
| 68 | 02/09 | **Functions and tests exist only because the tree is written in Italian**: a test that checks numbers are spelled the way Italian spells them, word lists of Italian function words, gates that count comment lines not in English. No repository anybody has worked in carries such things; they exist to police a choice that should not have been made, and they cost every reader a detour | Theo, reading the tree: «funzioni specificatamente italiane o test italiani che non sono best practice viste in nessuna repo e non hanno senso di esistere» | Finish the move to English and delete the machinery with it: when no Italian is left in the tree, the ratchets on Italian comments and the Italian spelling test are dead code, and one gate at zero on the whole tree replaces them | **aperto** — named, not yet done; outside the MVP's scope by its own list |
| 69 | 03/09 | **A test compared two clocks and called the difference a defect.** `the_text_is_left_alone_before_the_enter_follows` measured the gap between the two deliveries of a typed line and demanded it be at least the 50 ms the sender sleeps. Both instants are stamped by the *receiving* thread once the system wakes it, so the reading is the pause plus the difference between two wake-ups — which can be negative. It came out at 49.994 ms and the battery went red on a tree nobody had touched | Running the whole battery on a loaded machine: one red among 930 tests, six microseconds short, with the code unchanged since the last green run | **A test that can fail on the scheduler measures the scheduler, not the code.** The reading now allows 5 ms of wake-up drift, named in a constant that says why. The tolerance does not blunt it: with the pause removed the gap is 17 µs and it goes red | **chiuso** on 03/09. The defect put back — the sleep replaced with zero — turns it red naming the 17 µs; five runs in a row are green with the sleep back |
| 70 | 03/09 | **Three window tests went red on a green tree, and the measure was the laptop.** The contrast checks paint the whole window and read every text/background pair, which takes tens of seconds; vitest's default limit is five. On a loaded machine one took 37 s and the suite reported three failures with nothing wrong in the code — the second time in one day that a test measured the machine instead of the tree, after fault 69 | Running the window suite while twenty node processes of other sessions were building: three red, and the same three green when run alone | **A limit that a slow machine crosses is not a limit on the code.** The suite now allows two minutes per test, written in `vite.config.ts` with the measurement that set it. The check itself is unchanged: what it refuses, it still refuses | **chiuso** on 03/09. The three go green under the same load that made them red; the twelve of that file pass in 38 s, well inside the new limit |
| 71 | 03/09 | **Tre copie di `sailor` sulla macchina, tre provenienze, e quella che la shell trova non è quella che il rilascio ha messo in servizio.** `crates/release` esiste per una ragione scritta in testa al file: `cargo build --release` compila l'albero di lavoro, quindi ricostruire mette in servizio le righe non committate di chiunque. Il rilascio clona HEAD, compila lì, esegue la batteria e scrive il binario in `<casa>/bin/sailor` con accanto il **timbro** del commit da cui viene — «l'unico posto dove quel fatto esiste». Tutto questo è stato aggirato da una copia a mano in `~/.local/bin/`, che in `PATH` viene **prima**. Il risultato: `~/.config/sailor/bin/sailor` timbrato `391b2edf`, `~/.claude/bin/sailor` della casa precedente fermo al 31/08, e su `PATH` un terzo file di cui **nessun timbro dice da dove viene**. Chi ha eseguito `sailor` in questi giorni non sa quale codice ha eseguito. | Theo, che non vedeva Sailor installato: «io non vedo neanche sailor installato su macchina per poterlo avviare in autonomia». La riga di comando c'era — ed è proprio questo il punto, perché c'era **tre volte**. Confrontate le tre impronte: tutte diverse. Nessuna prova poteva vederlo: la copia in `PATH` non è nell'albero, il timbro che il rilascio scrive è coerente con la copia che il rilascio governa, e il difetto sta interamente in ciò che quel timbro **non copre**. | Il rilascio, dopo aver messo in servizio il binario, chiede alla shell quale `sailor` troverebbe e **rifiuta di dirsi finito** se non è quello: due file diversi con lo stesso nome sono la stessa famiglia del guasto 19 — la casa scritta in due posti, con nessuno dei due che si dichiari quello sbagliato. Il controllo nomina i due percorsi e la differenza, e non ripara da sé, perché una copia in `PATH` può essere stata messa lì di proposito. | **chiuso** il 04/09/2026, con la cura che la riga chiedeva. Il rilascio, dopo aver messo in servizio il binario, chiede al percorso di ricerca quale copia risponde a quel nome e non si dice finito se è un'altra: `release::on_path` distingue i tre casi — la stessa copia, un'altra copia che viene prima, nessuna copia sul percorso — e `release::ends_with` fa uscire il rilascio con 4 nel caso di mezzo. Non 1, perché la costruzione e la batteria sono andate; non 0, perché tutto ciò che cerca il nome esegue ancora il vecchio. Due prove con mutante: togliere il confronto fa dire «stessa copia» a due file diversi, togliere il codice 4 fa finire verde un rilascio scavalcato. Misurato lo stesso giorno: `~/.local/bin/sailor` è un collegamento a `~/.config/sailor/bin/sailor`, quindi i due percorsi sono lo stesso file e il rilascio tace; le due copie a mano restano sul disco con nomi propri e non rispondono più al nome. Il binario in servizio è passato da `5ea2636a` a `fa9aae84`, 31 commit, e il deposito si rilegge. |
| 72 | 2026-09-04 | Una porta cablata a mano in un file di configurazione che nessuno verifica ha reso muta la ricerca semantica su tutta la macchina. «~/.claude/settings.json» dichiara OLLAMA_MODE=external e OLLAMA_URL=http://localhost:11434; su quella porta non risponde nessuno. L'Ollama vivo è il container gestito da SocratiCode, che espone 11435->11434, e l'Ollama nativo (/opt/homebrew/bin/ollama) è installato ma spento. Conseguenza: codebase_search è muto in OGNI sessione Claude di questa macchina, e non da oggi — «external» è scritto a mano, presumibilmente quando un Ollama nativo girava. Il difetto non è «Ollama è giù»: è che l'indirizzo di un servizio sta in un file che nessun controllo legge, e che il fallimento arriva travestito da risposta. | Una codebase_search lanciata da una sessione in un altro spazio di lavoro è tornata CON SUCCESSO il testo «Ollama is not reachable at http://localhost:11434» — cioè il quarto esito che «chiedi-all-indice» chiama «non ho potuto guardare», che chi giudica sul solo contenuto scambia per «il progetto non è indicizzato». Misurato subito dopo: curl su 11434 dà http=000, su 11435 dà http=200 con nomic-embed-text e un embedding a 768 dimensioni verificato; «ollama ps» risponde «could not connect». L'inventario diceva intanto 11 progetti indicizzati, fra cui uno di essi con 3.619 file: l'indice era sano, irraggiungibile il motore degli embedding. | Un sensore che verifichi l'indirizzo dichiarato PRIMA di fidarsi di una risposta, e che dichiari «non ho potuto guardare» invece di lasciare che il fallimento arrivi dentro il testo della risposta. Esiste già ed è «chiedi-all-indice», che separa verifica e domanda proprio per questo: nessuno lo esegue prima di interrogare l'indice. Nella forma più stretta: un controllo che, per ogni URL di servizio scritto in una configurazione che Sailor legge, provi che qualcuno risponda — perché un indirizzo cablato a mano invecchia in silenzio e il suo fallimento non è distinguibile da un dato assente. | **aperto** — la riga cablata non è stata corretta ma TOLTA, il 04/09/2026 alle 13:48 da un altra sessione (dottyback), che ha rimosso OLLAMA_MODE e OLLAMA_URL da ~/.claude/settings.json: senza di esse torna in vigore OLLAMA_MODE=auto, che rileva il nativo su 11434 e altrimenti ripiega sul container gestito. È la cura giusta, perché non cabla nessuna porta. Da sapere: due sessioni hanno diagnosticato lo stesso difetto nella stessa ora senza sapere luna dellaltra, e la seconda ha sovrascritto la correzione della prima (11435 scritto alle 13:29, tolto alle 13:48) — nessuna delle due se ne era accorta. Il controllo che impedirebbe il ripetersi del guasto non esiste ancora. |
| 73 | 2026-09-04 | Il flusso «what-this-machine-has» chiude verde dicendo «Nothing is missing: every tool a flow asks for is on this machine» dopo aver guardato 10 passi su 53. Il passo «cosa-chiedono-i-flussi» raccoglie gli strumenti che i passi chiedono, ma conta solo quelli in cui «tool» è una stringa; dove è un ELENCO — la forma normale, quella che dichiara i motori alternativi — non ne conta nessuno. Misurato sui 25 flussi che il censimento stesso dichiara di aver letto: 43 passi dichiarano «tool» come elenco, 10 come stringa, e i cinque strumenti riportati (agy, cargo, codex, git, npm) vengono tutti e soli dai dieci a stringa. Conseguenza in chiaro: «claude-code» non compare da nessuna parte nel censimento pur essendo chiesto da 30 passi, e «ollama» — chiesto da «sweep-the-tree/rewrite» — non compare pur non rispondendo su questa macchina, dove l'eseguibile nativo è installato e spento. Il flusso che esiste per dire cosa manca è cieco proprio sui passi che offrono alternative, cioè su quelli scritti per essere portabili. | «sailor flow check chiedi-all-indice» dichiara «strumenti chiesti: agy, claude-code, codex». La corsa di «what-this-machine-has» sugli stessi 25 flussi riporta cinque strumenti, nessuno dei quali è claude-code, e nessun «asked_by» cita un passo che dichiari un elenco: «chiedi-all-indice/leggi», che chiede tutti e tre, non compare. Il campo «missing» è vuoto, «unknown» è vuoto, «steps_naming_a_binary» è zero, e il rapporto finale afferma che non manca niente. Le due letture vengono dallo stesso albero, nello stesso minuto. | Una prova che dia in pasto al raccoglitore un flusso il cui unico passo dichiara «tool» come elenco, e pretenda che quello strumento compaia fra i chiesti: nasce rossa oggi. Più a fondo, la regola che il difetto viola è già scritta in casa — un sensore che confonde «zero» con «non ho guardato» è peggio di un sensore assente, perché chi sta a valle si fida. Il rapporto non dovrebbe poter dire «non manca niente» senza dire anche su quanti passi lo ha verificato: un conteggio dei passi esaminati accanto a quello degli strumenti trovati avrebbe reso il buco visibile a chiunque leggesse, senza bisogno di aprire il codice. | **chiuso** — con mutante. `tools_named_by` legge adesso anche l'elenco, contando ogni nome come chiesto da quel passo, e il rapporto dice su **quanti passi** ha guardato: «non manca niente» senza quel numero non si poteva discutere. Misurato sulla macchina prima e dopo, sugli stessi 25 flussi: prima 5 strumenti da 10 passi, adesso **7 strumenti da 134 passi**, con dentro `claude-code` e `ollama`, cioè i due che il guasto nominava come invisibili. Il mutante — l'elenco che torna a cadere — fa cadere la prova. Resta una domanda che non è di questo raccoglitore: `ollama` compare fra i presenti, e il guasto lo dava installato ma spento; chi decide «presente» è il rilevatore, non questo passo. |
| 74 | 2026-09-04 | Due meccanismi del tempo convivono senza conoscersi, e uno dei due afferma il falso. «trigger::Kind::Periodic» rifiuta ogni innesco periodico con l'errore «nobody_keeps_the_time», la cui prosa dice che l'orologio che farebbe scattare la forma non esiste e che chi lo carica è una decisione che nessuno ha preso (crates/trigger/src/action.rs, ramo Kind::Periodic; crates/trigger/src/descriptor.rs, commento su Kind). Il tempo però qualcuno lo tiene: «desktop/src-tauri/src/beat.rs» è un battito acceso, EVERY = 60 secondi, che a ogni colpo giudica OGNI flusso conosciuto con «flow::is_due» — la stessa funzione di «flow tick» — e quello che risulta dovuto lo fa partire davvero con «run::start», origine «window · schedule», per tutto il tempo in cui la finestra è aperta. Conseguenza in chiaro: un flusso con «schedule» parte da solo, mentre un flusso il cui primo passo è un «trigger» di forma periodica non può nemmeno caricare, e chi legge l'errore conclude che Sailor non sappia contare il tempo — quando lo conta, un modulo più in là. Le tre decisioni che il documento del 01/09 chiedeva prima di scrivere il nodo del tempo sono peraltro già chiuse nel descrittore come campi obbligatori senza valore predefinito («missed_run», «every», «at_most_at_once»): la forma è pronta, e a rifiutarla è una frase che descrive un mondo superato. | Cercando dove Sailor ascolti GitHub, la lettura di «trigger::Kind» dà tre forme di cui una sola scatta, e la prosa di «nobody_keeps_the_time» porta a concludere — e a riferirlo a Theo — che nessun orologio esista in Sailor e che la decisione sia interamente aperta. La sessione che lavora sulla finestra ha indicato «beat.rs», letto nello stesso minuto: battito ogni minuto, «flow::is_due», «run::start», acceso. Le due letture vengono dallo stesso albero e si contraddicono. | Una prova che pretenda una sola sorgente di verità sul «chi tiene il tempo»: se «beat.rs» fa partire ciò che «flow::is_due» dichiara dovuto, allora un innesco periodico caricato non può essere respinto con «nessuno tiene il tempo» — o il battito lo serve, o l'errore dice cosa manca DAVVERO (che il battito vive solo a finestra aperta, e che nessuno guarda le corse perse a finestra chiusa), invece di negare l'orologio. Più a fondo, la regola già scritta in casa che il difetto viola: un messaggio d'errore è un'affermazione sul mondo, e un'affermazione che invecchia senza che nessuna prova la rilegga mente esattamente come un sensore che confonde «zero» con «non ho guardato». Il controllo che la terrebbe viva è una prova che, per ogni forma d'innesco respinta con «non c'è chi lo fa», cerchi nel codice chi lo fa: nasce rossa oggi su Periodic. | **chiuso in parte** il 04/09/2026, e conta cosa resta aperto. La bugia non c'è più: il rifiuto di un innesco periodico nomina chi il tempo lo tiene — il battito della finestra ogni minuto e `sailor flow tick`, entrambi sulla `schedule` del flusso e non su questo descrittore — dice che la via è **spostare** la ricorrenza e non costruire un guardiano, nomina le due cose che lo spostamento perde (`catch_up_each_one` diventa `once_for_all_of_them`, la larghezza scende a una corsa per volta) e dichiara il buco che quel guardiano lascia: il battito vive solo a finestra aperta, nessuno recupera le occorrenze passate mentre era chiusa. Due prove, con mutante: rimettendo la vecchia prosa `the_clock_that_is_kept_is_named_next_to_the_one_that_is_not` va rossa. **Resta aperto il controllo generale** che la riga chiedeva: una prova che, per OGNI forma d'innesco respinta con «non c'è chi lo fa», vada a cercare nel codice chi lo fa. Oggi la prova è scritta sul caso periodico, quindi la prossima forma che nascerà con una frase invecchiata non la troverà nessuno. *05/09:* la classe `nobody_keeps_the_time` non esiste più: un innesco periodico è rifiutato con `periodic_source_not_read`, «niente legge la ricorrenza che questa sorgente dichiara: l'orologio è la `schedule` del flusso, ed è quella che il battito avvia»; il readme del descrittore dice lo stesso. Un giudice, `every_trigger_refusal_is_checked_against_who_does_it`, accende ogni forma di innesco e chiede all'albero se ciò che il rifiuto afferma è vero (nessun lettore della ricorrenza fuori da `trigger`; `flow::is_due(schedule` in entrambi i battiti). Cinque mutanti. |
| 75 | 04/09 | **Una prova che vieta i nomi di prodotto leggeva il percorso del binario, cioè il disco di chi la esegue.** `what_the_install_writes_names_no_product` scorreva tutto `settings.json` cercando «orca», «warp», «vscode»: dentro c'è il percorso assoluto del binario di prova, che la macchina sceglie e noi no. Su un albero tenuto sotto una cartella che si chiama come un prodotto la prova è **rossa a comando**, e su un altro albero è verde — stesso codice, due verdetti. È la famiglia del guasto 17: una prova che misura la macchina invece del codice | Eseguendo la batteria in un worktree la cui radice contiene il nome di un prodotto: rossa lì, verde nel checkout principale | La prova sostituisce il percorso del binario con un segnaposto **prima** di cercare, e asserisce che la sostituzione ha avuto effetto — così una prova che guardasse la cosa sbagliata cadrebbe invece di passare. Il difetto rimesso (un nome di prodotto nel segnaposto) la fa tornare rossa | **chiuso** il 04/09 — con mutante, verde ripristinato |
| 76 | 04/09 | **Il deposito dei guasti accetta testo che il repository non può pubblicare, e i due controlli non si parlano.** `sailor faults add` prendeva qualunque testo; il cancello che legge l'elenco dei nomi privati della macchina gira solo sui file dell'albero. Quindi chi scrive un guasto non incontra nessun controllo, e il testo arriva nel repository più tardi, per mano di un'altra persona, quando qualcuno rende la tabella con `sailor faults render`. | Una sessione ha reso la tabella per riconciliarla col deposito, e il testo di un guasto registrato quel giorno ci ha portato dentro il percorso della casa di chi lavora e uno dei nomi che quella macchina dichiara privati. Il cancello `nothing_from_this_machine_is_published` è diventato rosso e l'ha fermata prima del commit — cioè ha funzionato, ma alla fine di una catena in cui il testo era già stato scritto, letto e reso. | Una regola sola, letta da chi scrive e da chi giudica. `toolbox::privacy` tiene dove sta l'elenco, cosa conta come nome e cosa un testo non può portare; `sailor faults add` e `sailor faults status` la interrogano e rifiutano prima di scrivere; il cancello legge la stessa cosa invece di una copia sua. Il rifiuto dice DOVE, mai COSA: un messaggio che ripete il nome per spiegarsi lo ha pubblicato su un terminale che può essere registrato. Cinque prove, due mutanti: togliendo il confronto senza maiuscole un nome scritto diversamente passa, togliendo il percorso della casa passa un percorso vero su una macchina sola. | **chiuso** |
| 77 | 04/09 | **Il rilascio mette in servizio una finestra senza pagina dentro, e finisce verde.** `sailor release window` costruisce la pagina nel clone, poi compila il guscio con `cargo build --release`. Ma lo script di costruzione di Tauri decide `dev = !custom-protocol`: senza quella funzionalità il guscio nasce in modalità sviluppo, non incorpora `dist/` e va a leggere il servitore di sviluppo su una porta che in servizio non risponde a nessuno. La finestra si apre, ha il titolo giusto, il battito gira, il motore risponde — e resta bianca. Nessuna riga di errore, da nessuna parte. | Provando a fare a mano i gesti che chiudono la condizione di terminazione: la finestra installata si apriva bianca dopo 6, 11 e 26 secondi. Lo stesso binario preso dal pacchetto costruito con `cargo tauri build` si apre pieno, fuori dal pacchetto e dentro: quindi non era il modo di lanciarlo. `strings` sul binario in servizio non trova nessun nome di file di `dist/`, e l'uscita dello script di costruzione dice `cargo:rustc-cfg=dev`. Tutti gli undici guscii costruiti oggi dal rilascio hanno zero pagina: il rilascio non ne ha mai fatto uno buono. | Due cose, e la seconda è quella che conta. La prima: il bersaglio dichiara le funzionalità che gli servono, e per una finestra sono quelle che la tolgono dalla modalità sviluppo. La seconda: il rilascio **guarda dentro** quello che ha costruito prima di metterlo in servizio — se il bersaglio dichiara una pagina, il binario deve contenerne i file, altrimenti il rilascio si ferma. È la stessa forma del rilascio ombreggiato: qui la cosa costruita c'è, funziona, risponde, e l'unica cosa che non fa è la cosa per cui esiste. | **chiuso** il 04/09 — con mutante. Il bersaglio dichiara `tauri/custom-protocol`, e il rilascio rilegge il binario appena costruito cercandoci i file di `dist/assets` prima di sostituire qualcosa. *Mutante*: tolta la funzionalità dal descrittore, il rilascio esce 1 con «il binario che è stato costruito non porta la pagina dentro: 9 file su 9 non ci sono» e il binario in servizio resta quello di prima, byte per byte. La finestra installata adesso si apre piena. Da sapere: la costruzione riusciva, il programma partiva, il battito girava e il motore rispondeva — nessuna uscita diversa da zero da nessuna parte. Un controllo che guarda solo il codice di uscita non poteva vederlo. |
| 78 | 04/09 | **Il pannello dichiara «ogni tasto va al processo così com'è, Invio compreso», e Ctrl-C non ci arriva.** Una shell rimasta su un prompt di continuazione — una virgoletta non chiusa — non si può più interrompere dalla finestra: i tasti stampabili e l'Invio passano, la combinazione con Control no. L'unica uscita è chiudere il terminale, cioè buttare via la sessione. Su un agente a metà di un lavoro lungo, Ctrl-C è il gesto che serve di più ed è l'unico che non c'è. | Facendo a mano il gesto A6: la sostituzione automatica delle virgolette del sistema ha trasformato le virgolette dritte in virgolette curve, zsh è finito su «dquote>», e né `keystroke "c" using control down` né il codice tasto grezzo hanno prodotto un `^C`. Subito dopo, dallo stesso campo, righe di testo normali sono arrivate al processo: quindi non era il fuoco, era proprio quel tasto. | Una prova che pretenda che **una combinazione con Control attraversi il ponte come byte di controllo**, non come testo — e la dichiarazione del pannello ridotta a ciò che è vero finché non lo è. La frase è la parte peggiore: promette tutti i tasti, quindi chi legge non va a cercare il gesto che manca, e lo scopre quando ne ha bisogno. Da guardare anche la sostituzione automatica delle virgolette: un campo che riscrive ciò che una persona batte, prima di mandarlo a una shell, cambia il comando senza dirlo. | **chiuso** il 04/09/2026, e i due difetti che si erano incontrati sono riparati tutti e due. *Il primo riscriveva il comando*: il campo sotto il riquadro è un campo di testo normale, quindi la sostituzione automatica del sistema trasformava le virgolette dritte in curve mentre si digitava e alla shell arrivava un argomento che nessuno aveva quotato. Adesso il campo dichiara di non volerla (`autoCorrect`, `autoCapitalize`, `spellCheck`) e `asTyped` disfa in uscita ciò che il sistema fa lo stesso: da questo campo non esce nessuna virgoletta curva, che è uno scambio stretto e voluto contro una riga di comando che non si può quotare affatto. *Il secondo toglieva la via d'uscita*: Ctrl-C premuto nel campo non è un tasto del terminale — la riga la tiene la finestra, e il programma non lo sente mai — quindi a un prompt di continuazione si poteva rispondere solo chiudendo il terminale e la sessione dentro. Adesso quell'unico tasto esce dal campo e arriva al processo, e tutto il resto continua ad aspettare Invio, il gesto di copia di questa macchina compreso. Due prove con mutante in `desktop/src/terminal.test.tsx`: resa identità `asTyped`, le virgolette curve passano; tolto il vincolo sui modificatori, l'interruzione ruba la copia a chi ha selezionato dell'output. |
| 79 | 04/09 | **L'elenco degli spazi di lavoro presenta righe indistinguibili, e chi sceglie non può sapere quale sta scegliendo.** Su questa macchina l'elenco a discesa mostra tredici voci, di cui **sei con la stessa identica etichetta** e una settima ripetuta più sotto. Sono progetti diversi con cartelle diverse: il dato che le distingue — il percorso — c'è, e non viene mostrato. Le righe degli alberi di lavoro invece portano il ramo, quindi si distinguono: è la stessa schermata che sa fare la cosa giusta per metà delle sue voci. | Facendo a mano il gesto A4 della condizione di terminazione: aprire l'elenco e leggere cosa offre. La pretesa era «elenca i progetti e gli alberi di lavoro, e «…un altro percorso» accetta ancora un percorso battuto», ed è vera alla lettera — l'elenco c'è, l'ultima voce c'è. Ma una pretesa sul contenuto non dice niente su una scelta che una persona non può fare. | Una prova che pretenda che **due voci dell'elenco non possano avere la stessa etichetta**, sulla lista che la schermata costruisce e non sui dati che riceve. Non è un problema di ordinamento né di raggruppamento: è che l'etichetta di un progetto è il suo nome quando il nome non è unico. Il rimedio più corto è quello già usato per gli alberi di lavoro: accanto al nome, la cosa che lo distingue. | **chiuso** — con mutante. `placesOf` costruisce le etichette con le code più corte che distinguono i percorsi fra progetti e alberi di lavoro insieme, e non con il nome della cartella. Il mutante — tornare al nome — fa cadere la prova che pretende etichette tutte diverse. |
| 80 | 04/09 | **«Apri nell'editor» apre ciò che il sistema associa a quel tipo di file, che per un file di testo formattato è spesso un lettore in sola lettura.** Senza `SAILOR_EDITOR` né `VISUAL` nell'ambiente da cui la finestra è stata accesa, il gesto è riuscito e ha aperto un'anteprima non modificabile. Il pulsante dice «editor» e la consegna finisce in un programma che non lascia scrivere: il gesto riesce e non serve a niente, che è peggio di un rifiuto. | Facendo a mano il gesto C2 della condizione di terminazione, su un file elencato fra le modifiche. La finestra ha consegnato il file, un'applicazione è passata in primo piano e ha mostrato un'anteprima dichiarata «di sola lettura». | Il ripiego non è «l'applicazione predefinita per questo tipo di file», perché quella associazione non parla di editing: è la scelta di chi ha installato qualcosa per ultimo. O si dichiara un editor — e in mancanza si dice che non c'è, invece di indovinare — oppure il pulsante non promette un editor. La variabile che risolve il caso è documentata in un solo punto del codice e in nessun posto che una persona legga prima di premere. | **chiuso** — con mutante. La pagina chiede `who_opens_files` prima di disegnare il pulsante: con un editor dichiarato lo nomina («open in code»), senza nessuno la parola smette di dire «editor» e sopra i file compare, una volta, chi risponde al posto suo e come dichiararne uno. Il mutante — la parola che torna a promettere un editor — fa cadere tre prove di pagina; quello Rust, il ripiego di sistema che viaggia come editor dichiarato, ne fa cadere due. |
| 81 | 04/09 | **I terminali della riga di comando si aprivano senza il profilo in forza, mentre quelli della finestra lo ricevevano.** `sailor terminal run` passava un ambiente vuoto al pseudo-terminale: dentro, `CODEX_HOME` e `CLAUDE_CONFIG_DIR` erano vuoti, e il motore acceso lì rispondeva come chiunque fosse la shell esterna. Due strade per aprire un terminale, una sola che rispetta la scelta di chi guarda l'elenco dei profili — e nessun modo di accorgersene dal codice di uscita, perché il motore parte lo stesso. | Provando il carburante a mano dopo aver adottato le case autenticate: `sailor terminal run -- sh -c 'echo $CODEX_HOME'` ha stampato una riga vuota, con un profilo attivo che nominava una casa precisa. | Una prova che apra il binario vero in uno pseudo-terminale vero, con un deposito dei profili suo, e legga quello che il programma dentro stampa: è l'unico controllo che non si può superare compilando. La forma del guasto è quella del 77 — un gesto che riesce e non fa la cosa per cui esiste — e vale ovunque due strade portino alla stessa cosa: se una la fa e l'altra no, va provato che la fanno tutte e due. | **chiuso** — con mutante. `sailor terminal run` apre lo pseudo-terminale con `profiles::active_environment_with` del deposito in forza, e dice a voce alta quello che non ha potuto dare (un endpoint rifiutato) invece di lasciarlo cadere. Il mutante — tornare all'ambiente vuoto — fa cadere la prova che legge quello che il programma dentro stampa. |
| 82 | 04/09 | **Le prove del supervisore lasciavano acceso il processo che accendevano, quando fallivano.** Quattro `/bin/sh -c while true; do sleep 1; done` sono stati trovati vivi alle 21:18, accesi fra le 14:22 e le 15:31 dalle corse della suite: sei ore e mezza di processi orfani sulla macchina di chi sviluppa. Il tipo di produzione `supervisor::child::Process` ha la sua guardia — «chi non lo spegne esplicitamente lo spegne comunque» — ma il tipo `Child` locale alla prova l'aveva copiato senza copiare la guardia, e spegneva solo in fondo, sul percorso felice. Quando un'asserzione cade, il panico salta lo spegnimento: cioè proprio nel momento in cui la prova la rilanci più spesso. | Theo ha chiesto se le shell aperte stessero lavorando o fossero ferme. Guardando i processi vivi per rispondere sono comparsi quattro `sleep` di sei ore, e la stringa che li accendeva stava solo in due file di prova. | La forma è quella del guasto 4 commessa dalla prova che dimostra il guasto 4 riparato: la pulizia non può stare sul percorso felice. Un `Drop` sul tipo che tiene il processo spegne mentre lo stack si srotola, e vale anche per il panico. Più in generale: quando una prova copia il comportamento di un tipo di produzione, va copiata anche la protezione, e il modo di accorgersene è guardare i processi vivi dopo una suite rossa, non dopo una verde. | **chiuso** — il tipo `Child` della prova ha adesso il suo `Drop`, che spegne mentre lo stack si srotola, quindi anche quando un'asserzione cade. I quattro orfani trovati vivi sono stati spenti. Non ha un mutante nel senso solito: il difetto si vede solo su una corsa che fallisce, e la prova non può fallire apposta — quello che si può controllare è che il tipo che tiene un processo abbia la guardia, ed è quello che questo commit rende vero in tutt'e due i file. |
| 83 | 04/09 | **`sailor flow check` rifiuta un flusso e nomina `sailor flow relocate` come cura; `relocate` gira, stampa un rapporto, non cambia niente ed esce zero.** Il controllo cerca i campi di luogo assoluti sia nei passi sia negli **ingressi dichiarati**; il rimedio percorreva soltanto `graph.steps[].with.workdir`. Su `decidi-la-direzione`, dove il percorso sta in `inputs.riferimenti.workdir`, il giro era chiuso: il rifiuto indicava un gesto che non arrivava dove il rifiuto guardava, e il campo incriminato non compariva nemmeno nell'elenco delle cose che il rimedio dichiarava di non toccare. | Provando `sailor flow check` su tutti i trentanove flussi della macchina per vedere cosa si rompesse. Uno solo era rosso; il rimedio che il messaggio stesso proponeva ha lasciato il file identico byte per byte, e il controllo dopo era ancora rosso sullo stesso campo. | La cura deve arrivare dove guarda il giudice, e la prova che lo dice è una sola funzione pura provata sul documento: un campo di luogo dentro `inputs` va riscritto relativo come quello di un passo. La forma del guasto è quella del 77, dell'80 e dell'81 — un gesto che riesce e non fa la cosa per cui esiste — e qui era peggio, perché a proporlo era il prodotto stesso. Un campo di ingresso pari alla radice diventa «.» e non si toglie: un ingresso si legge per nome, e toglierlo cambierebbe quello che il flusso riceve. | **chiuso** — con mutante. `relocate` percorre adesso anche gli ingressi dichiarati, con le stesse regole dei passi: solo un testo, solo sotto il prefisso, e fuori dal prefisso non decide lui. Il mutante — elencare il campo senza riscriverlo, che è esattamente il difetto vero — fa cadere la prova. Verificato anche sul flusso vero di questa macchina: `decidi-la-direzione` è passato da rosso a uscita zero, e il file è identico all'originale a parte il campo spostato. |
| 84 | 04/09 | **Un giudice verde da solo e rosso in compagnia.** `an_engine_that_said_its_quota_was_spent_is_not_started_again` portava avanti l'ora di un rinvio cercando nel file il testo «adesso + 3600»: cioè indovinando cosa dicesse l'orologio quando il file era stato scritto. Con il modulo intero in esecuzione passa più di un secondo fra le due letture, la ricerca non trova niente, il file resta com'era e l'ultimo braccio diventa rosso dicendo che il motore è ancora messo da parte. | `cargo test -p actions --lib what_it_cost` rosso due volte su due su HEAD pulito; lo stesso test da solo, verde. Trovato mentre si misurava la suite per un'altra modifica, e per un momento attribuito a quella. | Nessun test ricalcola un istante che il codice sotto prova ha già scritto: si legge dal file. Un giudice che dipende da quanto ha impiegato il giudice prima di lui insegna a rilanciare la suite invece di leggerla, ed è il modo in cui un rosso vero passa inosservato. | **chiuso** — il controllo legge `until` dal file e ci scrive sopra; modulo verde tre volte su tre. |
| 85 | 05/09 | **Il repository non aveva un ciclo di vita con il remoto.** 64 rami locali, di cui 56 con albero identico al tronco; cinque copie di lavoro aperte da giorni, pulite e già fuse; il tronco `sorgenti` 196 commit avanti a `origin/sorgenti` senza che nessuno avesse spinto; nomi in italiano che raccontano una sera e non un lavoro (`work/fusione-sera-guasti`, `innesto-toml-codex-ricucito`). E `origin/main` è la storia precedente alla riscrittura: chi apre GitHub vede un progetto fermo al 01/09. | Theo lo ha visto guardando il remoto e gli alberi aperti. Misurato: `git branch --merged` dice 56 fusi; `git merge-tree --write-tree` dice 56 superati per contenuto, col controllo assurdo che esce portante; `git push` rifiutato con 403 perché le credenziali di questa macchina sono di un altro account GitHub (`matteodimattia`, non `SIRTHEO`). | Una regola scritta e un gesto che la esegue: il ramo si chiama `work/<cosa-fa>` in inglese, nasce da `sorgenti`, torna in `sorgenti` e muore nello stesso gesto; la copia si toglie con `git worktree remove`; il tronco si spinge **a ogni rilascio**, così un binario in servizio non esiste mai su una macchina sola. E il rilascio dovrebbe dirlo quando il remoto è indietro: oggi non guarda il remoto. | **chiuso in parte** — 56 rami cancellati dopo la prova per contenuto, cinque copie chiuse, la regola scritta in `AGENTS.md` («Il ciclo di un ramo, e come si chiama»). **Restano**: la spinta, che rifiuta finché le credenziali git di questa macchina non sono dell'account che possiede il repository (gesto di Theo: `gh auth switch -u SIRTHEO && gh auth setup-git`, poi `git push origin sorgenti`); `origin/main`, da decidere; e sei rami che portano contenuto (`innesto-toml-codex`, `work/fusione`, `work/nodo-porte`, i due `worktree-agent-*` e `matteodimattia/chore-…`, che sono la storia vecchia) da esaminare uno a uno. |
| 86 | 05/09 | **Una forma «oggetto qualunque» potava tutto.** `answer_shape` con `properties: {}` e `allow_extra: true` — l'unico modo di dire «un oggetto, qualunque cosa contenga» — passava dalla potatura che inoltra solo i campi dichiarati, e ne usciva `{}`: il flusso che il motore aveva scritto intero arrivava al passo dopo vuoto. | Eseguendo `draft-a-flow` con uno schizzo vero: `author` verde, `draft` rosso su un puntatore a un oggetto che c'era ed era `{}`. Nel ledger l'uscita di `author` lo mostrava. | La potatura lascia intero un oggetto la cui forma non dichiara campi e ammette extra; una prova con quella forma e un oggetto pieno. | **chiuso** la stessa sera |
| 87 | 05/09 | **I riferimenti dentro un dato venivano risolti come se fossero del passo.** Il flusso in bozza porta i suoi `{"$from": "/text"}` per una corsa futura; il risolutore, che cammina ogni oggetto dell'ingresso di un passo, li ha presi per riferimenti del passo `draft` e si è rotto su `/text`. | Terza corsa di `draft-a-flow`: `unresolved_reference` su un puntatore che nessun passo del flusso di bozza scrive. | Il risolutore cammina solo il `with` — ciò che scrive chi scrive il flusso — e lo risolve contro l'ingresso intero; l'uscita di una dipendenza arriva com'è, `$from` compresi. Una prova nell'esecutore con un `$from` dentro l'uscita del passo prima, che arriva intatto mentre quello del `with` si risolve. E `flow_draft` accetta la bozza anche come testo. | **chiuso** la stessa sera, nell'esecutore |
| 88 | 05/09 | **Il rilascio rifaceva tutto a ogni chiamata.** Clonava in una cartella `mktemp` nuova; cargo giudica per percorso e mtime, quindi ricompilava i crate e rilegava con LTO i cento binari di prova anche per un commit di sola documentazione. | Cronometrando un rilascio con `date` prima e dopo: 7 min 44 s per un commit che toccava solo `docs/`, 42 s dei quali di compilazione di crate in cui niente era cambiato. | L'albero di rilascio vive in `target/release-tree` e viene portato a HEAD con un checkout, che riscrive solo i file cambiati; una prova tiene il mtime di un file non toccato attraverso due rilasci. Il numero dopo va misurato allo stesso modo. | **chiuso** la stessa notte, misura dopo in sospeso |
| 89 | 05/09 | **Due alberi in una sola cartella `target`.** `sailor ratchet` compilava il suo albero in `target/from-head`, la stessa cartella in cui il rilascio compila e prova il suo; con i due in corso insieme, il rilascio è uscito rosso — «1 target failed» — senza il nome del bersaglio, e la stessa suite rieseguita da sola sullo stesso albero era verde, 122 binari. | Un rilascio rosso mentre il cricchetto girava, e la coda stampata che finiva sul conteggio di cargo e non sui nomi. | Il cricchetto ha la sua cartella, `target/ratchet`; e un rilascio rosso stampa le righe che nominano le prove rosse, non solo la coda. La causa è la più probabile, non dimostrata: si dimostra se non ricapita. | **chiuso** la stessa notte, causa probabile |
| 90 | 05/09 | **Una serie di fallimenti doveva un guasto a ogni battito.** La regola scritta da un agente ricordava l'ultima corsa fallita su cui il guasto era stato scritto; alla corsa dopo la serie era «cresciuta», l'ultima corsa non era più quella ricordata, e il flusso dei guasti — che chiama un motore — sarebbe partito di nuovo: un flusso a orologio che fallisce ogni minuto avrebbe pagato un guasto al minuto, per tutta la notte, mentre nessuno guardava. La prova dell'agente lo dichiarava perfino come comportamento voluto («la stessa entità è di nuovo dovuta»). | Rileggendo il codice fuso prima di rilasciarlo, non eseguendo: il ledger in servizio non aveva ancora tre corse rosse di fila. | La serie porta tutte le sue corse; una serie che contiene una corsa già scritta non deve niente, per quanto cresca; deve di nuovo solo una serie nata dopo un successo. Il mutante (la regola vecchia) fa rosso la prova nuova. | **chiuso** prima del rilascio |
| 91 | 05/09 | **Una prova scriveva la variabile dei profili fuori dalla serratura che le altre rispettano.** `a_profile_whose_endpoint_is_refused_holds_the_engine_back_before_spending` impostava `PROFILES_STATE_PATH` e poi prendeva la serratura del listino; le prove sorelle, che leggono i profili sotto la stessa serratura, potevano vederla a metà. Il rilascio 23, con la suite in `--release` sotto il carico di cinque agenti che compilavano (127 s per il crate `actions`), ha avuto quattro rossi in `what_it_cost` e non ha sostituito niente. | Il rilascio: rosso una volta sotto carico, verde otto volte su otto a macchina meno carica, prima e dopo la correzione. | La prova usa `with_profiles_state`, che imposta la variabile dentro la serratura. Causa probabile, non dimostrata: il messaggio dei panici non era nel log — il rilascio stampava la riga del panico e non quella dopo, e l'albero temporaneo con la suite intera era già cancellato. Ora stampa anche il messaggio. | **chiuso** la stessa notte, causa probabile |
| 92 | 05/09 | **`sailor ratchet` misurava le sorgenti in servizio, non l'albero in cui veniva lanciato.** La radice veniva da `SAILOR_SOURCES` o dalla cartella delle sorgenti sotto la casa; cinque agenti in worktree lo hanno lanciato ciascuno dal proprio albero, e ognuno ha archiviato il checkout principale e scritto nello stesso `target/ratchet-tree`: alberi a metà estrazione, «file not found for module», «failed to load manifest», rossi che non erano di nessuno. | Tre cricchetti rossi sul tronco con file che a HEAD c'erano, e due agenti che hanno scoperto da soli di aver misurato l'albero di un altro. | La radice è il `git rev-parse --show-toplevel` della cartella corrente; fuori da un checkout si torna alle sorgenti in servizio. Prova con mutante. | **chiuso** la stessa notte |
| 93 | 05/09 | **L'albero di rilascio persistente ha ricevuto i moduli della pagina una seconda volta, dentro i primi.** Il rilascio prende in prestito `node_modules` dall'albero di lavoro con `cp -Rc`; da quando l'albero di rilascio sopravvive fra un rilascio e l'altro (guasto 88), la cartella di destinazione esiste già e `cp` copia *dentro*: `node_modules/node_modules`, due copie di React, e la pagina non si costruisce — 79 prove rosse con «Cannot read properties of null (reading 'useState')», tutte verdi nell'albero di lavoro. | Il primo rilascio della finestra dopo il passaggio all'albero persistente. | I moduli lasciati dal rilascio precedente vengono tolti prima del prestito (`clear_for_the_borrow`), con prova e mutante. | **chiuso** la stessa mattina |
| 94 | 05/09 | **Un flusso spedito dichiara `data: private` e nomina tre motori, e nessuno dei tre può eseguirlo.** Il `data_pact` di quei motori non è dichiarato, quindi vale «unknown», e un passo privato rifiuta un motore sconosciuto: `take-the-next-fault` non è mai stato eseguibile dal vivo su questa macchina. Nessuna prova lo diceva, perché le prove del flusso dichiarano un finto motore che risponde `DoesNotTrain` — il finto era più permissivo del vero. | Alla prima corsa dal vivo, delegando il lavoro ai motori: chiusa `failed` in due secondi, zero chiamate, «none of the engines the step asks for can be used here» per tutti e tre | Un controllo statico che confronti il `data` di ogni passo con il `data_pact` dei motori che quel passo nomina, e rifiuti un flusso che non può girare da nessuna parte; e il patto dichiarato nel descrittore di ogni motore, perché assente non vuol dire pubblico. **Nel deposito questo guasto porta il numero 86**: il deposito è indietro rispetto a questa tabella, ed è il guasto 42(d) visto un'altra volta | **aperto** |
| 95 | 05/09 | **Un motore ha consegnato la risposta giusta e Sailor l'ha buttata via dicendo che aveva finito la quota.** Le parole di `unusable_when` si cercano in tutta l'uscita, e un motore che legge questo repository stampa questo repository: i nostri documenti parlano di quote, quindi la parola dichiarata compare **nel materiale letto**, non in un rifiuto. Misurato: 37 occorrenze di «quota» nell'uscita di un passo che aveva prodotto ventisette punti di ricognizione corretti, nella forma dichiarata. | Alla prima delega vera di lavoro a un motore: il passo si è chiuso `exhausted` con la risposta completa già stampata sul terminale, e il flusso è finito `failed` avendo in mano il lavoro fatto | Un motore che ha risposto **nella forma dichiarata** ha lavorato: il rifiuto non si cerca dentro una risposta valida. Le parole vanno cercate quando la risposta manca o non sta nella forma. **Nel deposito porta il numero 87** | **aperto** |
| 96 | 05/09 | **Un motore spedito, invocato con la riga che il suo descrittore dichiara, non ha risposto niente**: uscita vuota, e il passo è caduto sulla forma con «the answer is not JSON; it saw «»». La riga era quella provata a secco, e la prova a secco risulta sana perché giudica il rifiuto **senza** domanda, non la risposta **con** la domanda. | Nella stessa corsa in cui l'altro motore ha risposto: due incarichi paralleli, uno consegnato e uno vuoto | Una prova che dia a ogni motore spedito una domanda minima e verifichi che qualcosa torni: una riga che parte non è una riga che risponde, e il silenzio di un motore oggi non si distingue da un motore assente. **Nel deposito porta il numero 88** | **aperto** |

## Cosa dice questa tabella, letta tutta insieme

**Quasi la metà si è vista solo eseguendo o guardando.** Non dal codice, non
dai tipi, non dalle prove: cronometrando, aprendo il deposito, facendo uno
screenshot, controllando il peso di una cartella. È la ragione per cui una prova
che non poteva venire diversa non è una prova.

**Diciassette sono ancora aperti** su novantasei, e due di quelli (10 e 28)
sono la stessa cosa vista da due lati: **la stessa verità scritta in più posti,
e nessun controllo che li confronti** — due copie di un elenco di componenti,
e la risoluzione dei rinvii che stava in dodici punti prima di tornare in uno.
Era in terzetto col 32 fino al 01/09/2026, quando un descrittore ha smesso di
poter dichiarare una capacità in un blocco e smentirla nell'altro.

**I numeri 50 e 51 sono nati altrove**, in un ramo che lavorava in parallelo
mentre `sorgenti` prendeva 41-47: erano un 41 e un 42, poi un 48 e un 49, e
adesso un 50 e un 51 — **rinumerati due volte nello stesso giorno**, perché la
seconda fusione ha trovato `sorgenti` che nel frattempo aveva preso anche quei
due. È la terza cicatrice della stessa ferita — il 43 porta la prima — e la cura
non è coordinarsi meglio: è che **il numero di un guasto lo dia il sistema**,
come già fa il conto degli aperti, invece di farlo scegliere a chi scrive la
riga.

*Sette di quei diciannove (14, 15, 18, 28, 33, 37, 39) dicono «chiuso in parte», e fino
al 01/09/2026 uno stato di mezzo li toglieva dal conto: il controllo contava solo
la parola «aperto», e la prosa diceva undici. Nessuno l'aveva deciso — il 14 e
il 33 erano usciti dal conto il 31/08 senza che nessuno se ne accorgesse, e la
direzione dell'errore era quella che tranquillizza. Adesso un guasto conta come
aperto finché la cura che dichiara non è fatta, e lo stato di mezzo serve solo a
raccontare quale metà è fatta.*

*Erano cinque, e il quinto era il 39, entrato fra i mezzi lo stesso giorno in cui
era nato chiuso: la riparazione reggeva, la **ragione** con cui dichiarava chiuso
anche il resto era stata misurata dopo ed era falsa. **È uscito dallo stato di
mezzo il 01/09**, ed è la prima volta che uno ne esce. Vale la pena dire come:
non riparando meglio ciò che c'era, ma accorgendosi che la metà mancante non
stava lì dentro. Il vaglio a secco era sano; gli mancava una domanda, e la
domanda si fa al motore — mai al disco, che sarebbe stata la seconda copia della
verità di cui questa tabella parla in tre righe diverse.*

*Fino al 31/08 questa riga nominava il 4, il 10 e l'11 e diceva «il sistema non
sa cosa sta facendo di sé». Il 4 e l'11 sono stati chiusi lo stesso giorno — il
deposito ha una tabella `processes`, e `crates/supervisor` tiene viva la
finestra — quindi la frase è stata riscritta su chi è aperto adesso. **Il
conteggio lo sorveglia una prova; quali numeri nomina, no**: è invecchiata per
un giorno senza che nulla diventasse rosso.*

**Il 31 e il 32 dicono che la seconda copia non si controlla da sola.** Tutti e
due sono la stessa verità scritta in due posti dello stesso file, con uno dei
due inservibile: `agy` dichiara la catena e non il ripiego, `gemini-cli`
dichiara la capacità e non la riga. Nessuno dei due rompe niente — è questa la
parte da leggere. Un ripiego che non scatta somiglia a un ripiego che non è
servito; una capacità mai usata somiglia a una capacità che c'è. Il decimo dice
la stessa cosa sul codice; questi due la dicono sui dati, che è dove il prodotto
è andato a mettere la sua parte modificabile senza ricompilare.

**E tutti e due restano aperti per la stessa ragione, che va detta.** La cura di
entrambi è un dato che nessuno ha misurato — come `agy` dichiara di aver finito
la quota, come `gemini` dichiara la stessa cosa — e i due dati si possono
scrivere in trenta secondi indovinandoli. Sarebbe peggio del difetto: una parola
sbagliata in `unusable_when` fa scendere un mandato malformato per tutta la
catena finché qualcuno risponde comunque, e nessun controllo lo vedrebbe più.
Un guasto aperto con la sua misura mancante scritta accanto vale più di un
guasto chiuso su un'invenzione.

**Quattro guasti nuovi hanno litigato per due numeri, ed è il decimo di nuovo.**
Il 31, il 32, il 33, il 34 e il 35 sono nati lo stesso giorno in quattro alberi
isolati, e tre di quegli alberi hanno scritto «31» e «32» senza vedersi.
L'isolamento serviva a non far sovrascrivere il codice, e ha funzionato: ma la
numerazione di questa tabella è una risorsa **condivisa e sequenziale**, e
l'isolamento non la protegge — la peggiora, perché dentro un albero nessuno vede
gli altri. Le prove che sorvegliano questo file hanno preso il duplicato al
primo montaggio, che è la ragione per cui esistono. Chi ripartisce del lavoro in
alberi separati deve chiedersi **quali risorse restano comuni anche là dentro**,
e questa è la prima.

**L'undicesimo dice quanto costa una causa scritta male.** La voce diceva «un
errore di compilazione uccide la finestra», e chiunque l'avesse letta avrebbe
cercato di gestire meglio l'errore — dove non c'era niente da gestire. Il difetto
era l'ordine di due righe dentro `tauri-cli`, e si vede solo leggendo il
sorgente di uno strumento di cui ci si fida. **Una voce di questo file è una
diagnosi, e una diagnosi sbagliata manda a riparare il posto sbagliato**: qui è
costata la riga «cosa è successo», riscritta il 31/08 insieme alla riparazione.

**Il ventiduesimo era già scritto, e non contava come guasto.** Il numero «costo
totale sulle 16 corse: 0 su tutte» stava in `docs/da-fare.md` dal 29/08, in una
tabella di stato del deposito. Nessuno l'ha letto come un difetto finché non è
servito appoggiarci sopra una decisione: uno zero in una colonna di misure
sembra un sistema che non ha ancora speso, non un sistema che non sa contare. Un
dato mancante si traveste da dato tranquillo, e l'unico modo per smascherarlo è
provare a usarlo per decidere qualcosa.

**Il 24 è il primo trovato usando il prodotto invece di leggerlo.** Non da una
lettura del codice, non da una prova: dal primo comando che chiedeva a Sailor una
cosa che nessuno gli aveva mai chiesto — quanto è costata una corsa. Il deposito
di questa macchina era illeggibile da un giorno intero e nessuno lo sapeva,
perché nessuno lo leggeva. È l'argomento più forte a favore dell'usare Sailor per
sviluppare Sailor: **le prove coprono il codice, non l'installazione.**

**Nessuno è stato trovato da un controllo automatico.** Tutti da una persona che
guardava. Questo è il numero che deve cambiare — e il 23 è il primo che lascia
dietro di sé un controllo capace di ritrovarlo: `cargo test -p sailor --test
identifiers_are_in_english` è nato **rosso su 64 identificatori** ed è diventato
verde riparandoli. Non è una prova scritta dopo per certificare il lavoro: è la
misura che ha detto quanto lavoro c'era.

**Il 30/08 ne sono arrivati tre in un giorno solo, e tutti e tre eseguendo.** Il
19 si è visto scrivendo un file dove la documentazione diceva e guardando che
nessuno lo leggeva; il 20 lanciando un flusso con un campo sbagliato; il 21
rieseguendo la batteria venti volte di fila invece di una. Nessuno dei tre
sarebbe uscito leggendo il codice: due erano d'accordo con se stessi, e il terzo
si vedeva solo ripetendo.

**Il quindicesimo dice perché.** Sailor non ha comandi per operare su se stesso,
quindi chi ci lavora lo aggira con script esterni — e uno strumento aggirato non
registra niente di ciò che gli succede intorno. È lo stesso difetto del quarto e
del decimo, alla radice: **il sistema non sa cosa sta facendo di sé perché non
passa da sé.**

**Il ventiseiesimo dice quanto è lunga la catena.** Tre passaggi per trovarlo:
l'autore ha scritto la prova, il verificatore l'ha mutata — davvero, su una
copia — e ha visto rosso, e solo un terzo che leggeva **senza sapere di chi
fosse il lavoro** si è accorto che la mutazione non era quella giusta. Il
verificatore aveva tolto il tetto; il difetto vero era l'attesa fissa, e su
quella la prova restava verde. «Chi crea non giudica» non basta: **anche chi
giudica va giudicato**, e il modo è togliergli il nome dell'autore.

**Il tredicesimo era già scritto, e non è servito.** La trappola di zsh stava in
una memoria da giorni: chi ha riscritto la sorveglianza — cioè chi aveva appena
finito di riparare il guasto 12 — non l'ha consultata e ci è ricaduto dentro
venti minuti dopo. È la prova che una lezione scritta non vale niente se non c'è
il momento in cui qualcuno la va a leggere. Per i flussi quel momento adesso
esiste, ed è `docs/decisioni.md`, letto in tre punti del flusso di sviluppo. Per
chi scrive a mano no.
