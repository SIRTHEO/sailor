[package]
name = "claude-hooks"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "claude-hooks"
path = "src/main.rs"

[dependencies]
hook-io = { path = "../hook-io" }
guards = { path = "../guards" }
inventory = { path = "../inventory" }
# Il deposito serve alla staffetta, che da lì legge su che lavoro si e' —
# invece di dedurlo dall'ultimo mandato nominato in un transcript.
ledger = { path = "../ledger" }
serde_json.workspace = true
regex.workspace = true
