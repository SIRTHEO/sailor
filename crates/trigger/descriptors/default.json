{
  "_readme": [
    "The signal sources shipped with the product. This is data: rewrite it,",
    "extend it and switch entries off without recompiling anything.",
    "",
    "To add one: put a `.json` file in `~/.config/sailor/triggers.d/` (or wherever",
    "SAILOR_TRIGGER_DESCRIPTORS points) holding an array of descriptors, or an",
    "object with a `triggers` field. An `id` already present here is replaced by",
    "yours; to remove one, rewrite its `id` with `\"disabled\": true`.",
    "",
    "`kind` is `manual` (somebody presses and it starts, carrying a text) or",
    "`terminal` (the signal would appear in a terminal session). A terminal must",
    "say in `listen` where it would be seen appearing: `appended_lines` for a file",
    "that grows and is never rewritten, one JSON line per message;",
    "`cursor_command` for a command that, given a cursor, prints what appeared",
    "after it.",
    "",
    "CAREFUL, AND IT HOLDS FOR EVERY `terminal` ENTRY: Sailor listens to no",
    "terminal today. These entries declare the shape, and a step using one stops",
    "and says what is missing. Only `manual` really works."
  ],
  "triggers": [
    {
      "id": "manual",
      "kind": "manual",
      "label": "By hand",
      "note": "Somebody presses and it starts, carrying a text. This is the launch button's source: the signal arrives together with the run, so there is nothing to wait for."
    },
    {
      "id": "sailor-terminal",
      "kind": "terminal",
      "label": "Sailor's own terminal",
      "listen": {
        "kind": "appended_lines",
        "files": ["~/.sailor/terminals/*/messages.jsonl"],
        "text_pointer": ["text"],
        "who_pointer": ["who"],
        "where_pointer": ["session"]
      },
      "note": "The terminal engine exists now — `crates/terminal` — but NOBODY WRITES THIS FILE, and the path above is still the shape it will have rather than a measurement. That is a choice, not an oversight: a terminal running in the same process hands its routed request straight to the `manual` trigger, which carries the signal with it, and going through a file would be writing in order to read yourself back. This entry serves the case that does not exist yet: a terminal in ANOTHER process, which has to get its request to a Sailor living elsewhere. For that, the two things written in `trigger::action` are still missing — a process that stays up waiting, and a reader that keeps the point it reached."
    },
    {
      "id": "orca-terminal",
      "kind": "terminal",
      "label": "Orca's panes",
      "listen": {
        "kind": "cursor_command",
        "tool": "orca",
        "args": ["terminal", "read", "--json"],
        "cursor_argument": "--cursor"
      },
      "note": "Measured, not assumed. That product records pane output in `terminal-history/*/output.log`, which is NOT an honest source: it is a binary frame format (magic OCKL) holding ANSI terminal bytes — screen redraws, not messages — truncated in place past 5 MB and written in batches every ~5 seconds, so a reader tailing it loses content without noticing. The only supported road is this cursor command, which returns text. It needs a tool descriptor with id `orca`, which is not shipped: add it in ~/.config/sailor/tools.d/."
    }
  ]
}
