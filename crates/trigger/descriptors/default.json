{
  "_leggimi": [
    "Le sorgenti di segnale, spedite col prodotto. È un dato: si riscrive, si",
    "estende e si spegne senza ricompilare niente.",
    "",
    "Per aggiungerne una: metti un file `.json` in `~/.config/sailor/triggers.d/` (o",
    "dove punta SAILOR_TRIGGER_DESCRIPTORS) con dentro un array di descrittori,",
    "o un oggetto con un campo `triggers`. Un `id` già presente qui dentro viene",
    "sostituito dal tuo; per toglierne uno, riscrivi il suo `id` con",
    "`\"disabled\": true`.",
    "",
    "`kind` è `manual` (qualcuno preme e parte, portando un testo) oppure",
    "`terminal` (il segnale comparirebbe in una sessione di terminale). Un",
    "terminale deve dire in `listen` dove si vedrebbe comparire: `appended_lines`",
    "per un file che cresce e non viene mai riscritto, una riga JSON per",
    "messaggio; `cursor_command` per un comando che, dato un cursore, stampa ciò",
    "che è comparso dopo.",
    "",
    "ATTENZIONE, E VALE PER TUTTE LE VOCI `terminal`: oggi Sailor non ascolta",
    "nessun terminale. Queste voci dichiarano la forma, e un passo che le usa si",
    "ferma dicendo cosa manca. Solo `manual` funziona per davvero."
  ],
  "triggers": [
    {
      "id": "manual",
      "kind": "manual",
      "label": "A mano",
      "note": "Qualcuno preme e parte, portando un testo. È la sorgente del pulsante di lancio: il segnale arriva insieme alla corsa, quindi non c'è niente da attendere."
    },
    {
      "id": "sailor-terminal",
      "kind": "terminal",
      "label": "Il terminale di Sailor",
      "listen": {
        "kind": "appended_lines",
        "files": ["~/.sailor/terminals/*/messages.jsonl"],
        "text_pointer": ["text"],
        "who_pointer": ["who"],
        "where_pointer": ["session"]
      },
      "note": "Il motore dei terminali adesso esiste — `crates/terminal`, dal 31/08/2026 — ma QUESTO FILE CONTINUA A NON ESSERE SCRITTO DA NESSUNO, e il percorso qui sopra resta la forma che avrà, non una misura. Il motivo è una scelta, non una dimenticanza: un terminale che gira nello stesso processo consegna la richiesta smistata direttamente all'innesco `manual`, che porta il segnale con sé — passare da un file sarebbe scrivere per rileggersi. Questa voce serve al caso che ancora non c'è: un terminale in un ALTRO processo, che deve far arrivare la propria richiesta a un Sailor che sta altrove. Per quello mancano ancora le due cose scritte in `trigger::action`: un processo che resti in piedi ad aspettare, e un lettore che tenga il punto raggiunto."
    },
    {
      "id": "orca-terminal",
      "kind": "terminal",
      "label": "I pannelli di Orca",
      "listen": {
        "kind": "cursor_command",
        "tool": "orca",
        "args": ["terminal", "read", "--json"],
        "cursor_argument": "--cursor"
      },
      "note": "Misurato il 28/08/2026. Orca registra l'uscita dei pannelli in `terminal-history/*/output.log`, che NON è una sorgente onesta: è un formato binario di frame (magic OCKL) con dentro byte di terminale ANSI — ridisegni di schermo, non messaggi — svuotato sul posto oltre i 5 MB e scritto a lotti ogni ~5 secondi, quindi un lettore in coda perde contenuto senza accorgersene. L'unica via supportata è questo comando a cursore, che restituisce testo. Richiede un descrittore di strumento con id `orca`, che non è spedito: si aggiunge in ~/.config/sailor/tools.d/."
    }
  ]
}
