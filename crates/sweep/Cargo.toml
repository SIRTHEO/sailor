[package]
name = "sweep"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
flow = { path = "../flow" }
guards = { path = "../guards" }
ledger = { path = "../ledger" }
serde = { workspace = true }
serde_json = { workspace = true }

