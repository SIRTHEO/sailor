[package]
name = "gesture"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"

[dependencies]
serde.workspace = true
serde_json.workspace = true
hook-io = { path = "../hook-io" }

[dev-dependencies]
guards = { path = "../guards" }
